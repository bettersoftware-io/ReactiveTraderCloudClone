# RN mobile-v1 — open items, wrinkles and follow-ups

Every known gap, deferred decision and unresolved wrinkle in the React Native
workstream, in one place. [STATUS.md](STATUS.md) remains the authoritative
*pending-work backlog* and links here; this page is the detail behind its RN
bullets, including items too small or too cross-cutting to earn their own
backlog entry.

Nothing here is a blocker on shipped work. Items are grouped by what kind of
thing they are, because that determines who fixes them and where.

**Last updated: 2026-08-08** (close-out sweep rounds 1–6, a round-7 correction
to T9, a round-8 JDK resolution, and rounds 9-10 closing T5 and P8 — see below)

## 0. The 2026-08-05 close-out sweep

The open count went **31 → 12** over six rounds, and most of that was not code. The list had
grown into something nobody could act on, because three different kinds of
entry were sharing one status column:

- **7 fixed** — T16, T28, T31, T38, T40, T41, T42.
- **6 reclassified** — P2, P3, T19, T20, T21, T43 were **already-accepted
  decisions** still displayed as open work. An accepted deviation is not a
  backlog item; leaving it looking like one is how a list becomes noise.
- **2 corrected as stale** — T18's goldens already existed, and T6 turned out
  to need scenarios *authored*, not captured.

**Round 2** fixed four more — T37 (the Exposure bubbles finally re-pointed at
the mobile design), T39 (the P&L delta chip), T30 (the FPS clock resync) — and
re-scoped two that only *looked* small: **T15** is 49 call sites across 8
files, seven inside worklets, for the sake of quieting a log warning; and
**M3** turns out to be a decision rather than a defect, because the 120 s the
domain uses is documented fidelity to the reference implementation while the
45 s is the design's. Neither should be closed by an agent on a whim.

**Round 3** closed **P5** (a three-level power-saver control — the plumbing had
honoured Freeze all along; only the 2-state toggle could not select it) and
re-diagnosed two more that had been recorded against the wrong facts: **T5**'s
CI tolerance covers an upstream `worklets@0.10.0` x86 crash, not the `#289` leak
it was filed against — though the "still fires on every `main` run" half of that
re-diagnosis was itself wrong, and round 8 closed T5 by measuring properly; and
**T6** splits — `shell` is buildable today, `rates` is
blocked on a pinned-price seam that does not exist.

**Round 4** closed **T6** outright — `shell/chrome` joins `rates/grid`, both
verifying at **0.00%**, so the HUD frame the user looks at all session finally
has pixel coverage — and settled **M2** as an accepted deviation (its recorded
fix would have weakened a field two web clients populate from a real dealer
picker, to delete three commented lines in the third). It also **re-scoped M7**
after measuring both sides: the "1 s vs 10 s" premise is true, but the change
also moves the chart's *window* from 15 minutes to 48 seconds, which makes it a
decision like M3 rather than a cadence tweak — the Phase 5 spec said as much
and the row had lost that.

**Round 4 did not shrink the list, and that is the result, not a failure.**
`shell/chrome` found **P7** and **P8** on its first capture — `Sign out`
clipped 5 points off the right edge, and the dock's FAB sitting on top of the
status strip's FPS cell. Both are real, both are in the shipped app, and
neither was findable before, because no golden had ever contained the chrome.
An instrument that finds nothing on first use is usually not an instrument.

Round 4 held the count at **14** — one closed, one accepted, two found — and
round 5 then took it to **12** by closing both of the rows round 4 had opened
questions about. Of those 12, **4 are the M-series** — `@rtc/domain` / wire
changes shared with the web client and frozen by Phase 5 §4 — leaving **8
RN-specific**, none blocking.

**Round 6** removed a defect that had been in EVERY golden since the tier
existed and that nobody had named: a grey `gearshape.fill` bubble near the
top-left of all 18 shots. It is expo-dev-menu's floating action button, mounted
in a `DevMenuFABWindow` — a passthrough `UIWindow` layered above the app, which
is why it drew over the Rates filter chips and the shell header and appeared in
the a11y tree as if it were ours. It never destabilised a diff (same place
every run, so captures still reproduced at 0.00%), **which is exactly why it
survived**: the tier's own question is "did this change?", and the answer was
always no. The cost fell on the corpus's OTHER use — reading these PNGs beside
the design prototype's shots to judge fidelity, with dev-tooling chrome baked
into every comparison. `tests/visual/shared/devMenuFab.ts` now writes the
dev-menu's own `EXDevMenuShowFloatingActionButton` preference off around a run
and deletes it afterwards, so it is hidden for captures and back for ordinary
dev. All 18 re-captured and verified. **The Maestro tier's 3 goldens still
carry it** — that runner never learns a UDID, so it has nothing to pass.

**Round 7 closed no rows and retracted a claim**, which is the useful kind of
sweep. Round 6's closing line — that Maestro is a "bake-off remnant" against
simctl's complete 18 — was **wrong**, and T9 now carries the correction. The
bake-off was a **3-scenario viability spike** on one device with **no Android
run at all**; that establishes which tiers can run here and cannot rank them.
The word "bake-off" invited the Playwright-vs-Cypress reading, where two suites
ran the same specs for months before one was retired — a different quantity of
evidence entirely. Two concrete defects fell out of re-reading it: Maestro
**pins no device** while its goldens sit under a path claiming one, and it
**could not start without a JDK ≥ 17**, which is why its golden set stalled
(resolved in round 8, below). Also
recorded: the dev-menu gear has an **Android** twin under the same preference
key, and a **build-time** default that no runner can forget — the per-runner
fix shipped in round 6 is the version Maestro silently missed. Nothing is
retired; all three tiers stay on the table until they can be compared at full
coverage, which waits on visual fidelity settling.

**Round 5** closed **P7** within hours of finding it, because the prototype
already answered it — the header's two controls are glyphs and it has no
sign-out, so the fix was a restoration rather than a layout negotiation. It
also closed **M7** on the maintainer's own reasoning: the prototype is a
*visual* reference with no server and no timestamps, so it was never an
authority on data cadence. Both are the same shape as M2 — rows that dissolved
once someone asked which authority they were appealing to.

**Round 8** made the Maestro tier runnable and, in doing so, corrected a number
this file helped propagate. The recorded blocker was "install JDK 17". Read from
source, `~/.maestro/bin/maestro:250` is `if [ "$JAVA_VERSION" -lt 17 ]` — a
**floor**, and its own error string says *"Java 17 or higher is required"*. Five
files had rendered that floor as a pin, one of them three lines below a verbatim
quote of the string, and `ios-visual-spike.yml` then hardcoded
`brew install openjdk@17` from the summary rather than the source.

The measurement that followed inverted the obvious correction. `maestro --version`
on 2.6.1: JDK **21 → 0 warnings**; **25 → 4** (jansi's restricted `System::load`);
**26 → 7** (jansi *plus* picocli mutating a final field on `TestCommand`). Both
warn that the behaviour "will be blocked in a future release", and picocli is the
parser for `maestro test`'s own flags — so the newest JDK is the one accumulating
scheduled breakage, and **21 is the newest that runs clean**. `openjdk@21` is now
installed and pinned in the workflow; keep the *versioned* formula, since bare
`openjdk` floats onto the next release.

**Round 9** closed **T5**, and the lesson is about the instrument rather than
the bug. T5 had been "not closeable — the tolerance is live and load-bearing;
deleting it turns CI red immediately" since 2026-08-05. It was never live. The
evidence for it was a grep over `gh run view --log` — which **echoes a step's
`run:` script before executing it**, so the tolerance's own
`grep -qF "[Worklets] Babel plugin exception…"` line matched *itself*. The same
wrong reading was made twice, three days apart, by two passes that each believed
they were verifying rather than assuming. Echoed lines carry the `[36;1m`
cyan-bold prefix; filter them and main run 31259176893 shows zero exceptions,
zero warnings, `Exported: dist`. The real fix had already landed in
`packageExtensions` and nobody noticed, because the tolerance made success and
failure look identical from the outside — a green step either way.

**Round 10** closed **P8**, and it completes a set worth naming. Three defects
were closed in two days whose common property is that *the instrument that
should have caught them was structurally incapable of it*: the dev-menu gear
survived 18 goldens because a golden compares the app to **itself**; T5's
tolerance made a passing and a crashing bundle **look identical**; and P8 was
invisible to both the golden **and** the design reference, because the reference
shot is a ~89%-scaled render in which the collision does not occur. Each was
found only by measuring something the instrument does not measure — a11y frames,
echo-filtered CI logs, pixel dimensions of the reference itself.

The practical rule this leaves: **when a check has been green for a long time
over an area nobody has looked at directly, that is not evidence of health.**
Ask what the check compares against, and whether the defect could survive it.

**The open roster, by id**, so any count here can be checked rather than
believed:

- **RN-specific (7)** — `R1` `P4` `T1` `T15` `T18` `T24` `T44`
- **M-series (4)** — `M1` `M3` `M4` `M5`

Total **11**.

That list is spelled out because the count has drifted in conversation more
than once, and always the same way: a row's open/closed state is written in
PROSE, so nothing can parse it and two readers get two totals. Quoting "9" and
"14" in the same discussion is not a moving backlog, it is one number counted
with the M-series excluded and the other with it included. State the filter
with the figure, every time. (A parseable status token per row would end this
properly; ~40 rows would need one, so it has not been done.)

**The whole golden set was re-captured** on iPhone 17 / iOS 26.5, most recently
in round 6 (all **18** at once, to strip the dev-menu bubble). Every scenario
was then verified reproducing: **16 at 0.00%**, with `analytics/dashboard` at
**0.17%** and `credit/sell-side` at **0.08%**.

Those two are the tier's noise floor, and WHICH scenario carries it moves
between runs — `blotter/seeded` was the 0.06% outlier before round 6 and now
sits at 0.00%. So read a sub-1% number as cross-run jitter (sub-pixel layout
settle, ghosting glyphs in content-identical frames), not as a signal about one
scenario; all of it sits far under the 6% tolerance. A number that does NOT
move between runs is the one worth looking at.

The set is now a comparison instrument rather than a regression gate: paired
against [`reference-shots/`](design/mobile/v1/reference-shots/DRIFT.md) it is
the cheap way to judge fidelity in bulk, which is what the next refinement pass
will do. **No human PNG-promotion gate** — that was costing more than it caught,
and `--update` after a green verify pass is the standing flow now.

---

## 1. Data-model mismatches — shared by BOTH clients

These live in `@rtc/domain` / `@rtc/client-core`, below the UI and above both
clients, so they are **not** RN-only: the web client has the same gaps, it just
does not exercise them. Full table with per-item workaround vs. model change in
the [Phase 5 design §8.1](superpowers/specs/2026-07-25-rn-mobile-v1-rehaul-phase-5-design.md).

| # | Gap | Fix direction |
|---|---|---|
| M1 | No "quotes I submitted" history projection for sell-side | Add a durable submitted-quote projection keyed by quote id |
| M2 | `CreateRfqInput.dealerIds` required, but the prototype has no dealer picker | **ACCEPTED 2026-08-05 — not open work, and the recorded fix direction is the wrong move.** "Make it optional, server defaults to all dealers" would weaken a field that two clients populate from a REAL picker (`DealerChecklist`, in both `client-react` and `client-solid`) in order to delete three lines in the third. The mobile client already states its intent explicitly — `NewRfqForm.tsx:40` sends `dealers.map(d => d.id)` under a comment saying so — which is strictly more readable than an implicit server-side default, because the broadcast is visible at the call site instead of inferred from an absent field. It also keeps `canSubmit` honest: the form declines to submit before the dealer panel has loaded, which an optional field would silently permit. The deviation is real, deliberate and documented; there is nothing to build. |
| M3 | RFQ window is 120 s in the domain vs 45 s in the prototype | **RE-SCOPED 2026-08-05: this is a DECISION, not a defect, and it is not ours to settle.** `rfq.ts:23` documents 120 as *"Mirrors rtc-original CREDIT_RFQ_EXPIRY_SECONDS = 120"* — fidelity to the reference implementation this repo exists to recreate. The prototype's 45 s is a *design* choice. So the two authorities genuinely disagree and 'pick one' costs one of them; it also moves both clients, the credit goldens and any e2e waiting on expiry. Needs an explicit call before anyone edits the constant. Pick one; a 120 s ring barely moves on a phone |
| M4 | No equities tick-history stream (sparklines derive from OHLC candles) | Add one mirroring the FX `usePriceHistory` |
| M5 | A fill is a binary animation intent — no lifecycle event carrying qty/price/outcome | Emit a proper order-lifecycle event |
| ~~M6~~ | ~~`AnalyticsSimulator` emits a frozen `STATIC_POSITIONS` constant every time; two of the three Analytics widgets read it~~ | **FIXED 2026-07-26 (5c Task 1).** Positions now drift each tick — a mean-reverting step around the original values, so the book wanders without walking away. The web client gains it for free, since the simulator sits below both. Consequence worth knowing: Analytics is now doubly non-deterministic (this drift *plus* the pre-existing `Math.random` history seed), which is why its visual scenario mounts `AnalyticsDashboard` over a literal book rather than the simulator. |
| M7 | Analytics history appends every 10 s vs the prototype's 1 s mock | **CLOSED 2026-08-05 — no work required, and the row was asking the wrong authority.** The prototype is a **visual** reference: no server, no real feed, and its `pnlHist` is a bare number array with no timestamps, ticked once a second purely so a static HTML demo looks alive. It has no standing over cadence, window or retention — those are domain concerns, and the numbers verified here (48 points at 1 s = a 48-second window, against our 90 at 10 s = 15 minutes) show "raise the cadence" would have quietly rescaled what the chart MEANS by ~19x on the strength of a demo artifact. A 15-minute P&L window is the more credible readout, and our timestamps are real where the prototype's do not exist. What survives is only the visual observation that the line steps rather than glides — cosmetic, optional, and a **view-layer** job if anyone ever wants it (never a data-model one). Note that smoothing would draw intermediate P&L values that never existed, which for a trading readout is a worse property than a visible step. Same disposition as M2: a real deviation, deliberately kept. |

## 2. Gaps against the reference implementation

Not against our design prototype — against Adaptive's actual ReactiveTraderCloud,
the product this repo recreates.

| # | Gap | Notes |
|---|---|---|
| R1 | **Exposure bubbles are draggable upstream; ours are inert in both clients.** Verified: no pointer/mouse/touch handler in either `PositionsPanel`, and RN's are statically shelf-packed. | Cheaper than first recorded. An earlier draft claimed drag threatens pinned visual goldens — **that was wrong, and is corrected here**: what threatens a golden is a *force-directed simulation settling over time*, not drag. A golden never drags, so an un-dragged layout is exactly as stable as today's, and the harness could disable drag outright if it ever mattered. Keep the deterministic shelf-packed layout and add drag on top. |

## 3. RN rendering wrinkles (no web analogue)

| # | Wrinkle | State |
|---|---|---|
| **P1** | **Skia text rendered NOTHING in the boot scenes.** `Skia.Font()` was built with no typeface; on real iOS that draws zero glyphs silently, with no throw. `core`'s banner/telemetry and `docking`'s ~20 readouts were absent from the on-device goldens — **and from Phase 6a's golden too, so it shipped unnoticed.** | **FIXED 2026-07-26.** Fonts now come from `bootSceneFonts.ts` — bundled JetBrains Mono (the web boot canvas's own stack), built in React-land and captured by the draw closure, never inside the worklet. The real 700 face is loaded too, so the three `bold` sites no longer render regular; that closes the separate weight gap as well. Verified on device; `boot/core` and `boot/docking` re-pinned. |
| **P1a** | **U+25C9 `◉`, the telemetry bullet every boot variant uses, is in NO bundled face** — verified against the cmap of JetBrains Mono, IBM Plex Mono, IBM Plex Sans and Chakra Petch. Every other symbol the scenes draw is covered. The web renders it because a CSS font stack falls back **per glyph** to the system monospace; a Skia font has one typeface and no fallback chain, so it drew a tofu box. | **FIXED 2026-07-26** — substituted with U+25CF `●` via `bootGlyphs.ts`, shared so 6b-2's five scenes inherit it. Worth knowing before adding any new symbol to a Skia-drawn string: check the cmap, because the web is not evidence. |
| P2 | `ctx.shadowBlur` bloom is not ported (5 sites). A per-frame `MaskFilter.MakeBlur` on a stroke is the mobile equivalent of the `filter` traps in [performance.md](performance.md). | **ACCEPTED — not open work.** Accepted non-goal. Revisit only if a scene reads flat on-device. |
| P3 | Skia has no `textBaseline: "middle"`; docking's range-dial rows sit baseline-anchored. | **ACCEPTED — not open work.** Accepted cosmetic gap — but no longer blocked behind P1, and now visible on device for the first time. Judge it against the re-pinned `boot/docking` golden before deciding whether to correct the offset. |
| P4 | RN Aurora ambient renders far too faint on-device. | Pre-existing; raise `t.aurora` / per-band opacities, revisit sway/skew geometry. |
| P5 | Power-saver **Freeze** renders the same as Calm on RN — no RN-side motion gating equivalent to the web's CSS catch-all. | **RESOLVED 2026-08-05 — and the recorded description pointed at the wrong layer.** This read "Freeze renders the same as Calm on RN — no RN-side motion gating equivalent to the web's CSS catch-all", which suggested the gates were missing. They were not: **54 files** read `useShellMotionEnabled` / `useBootMotionEnabled` / `isFreeze`, and the visual harness seeds `freeze` directly — that is precisely how `boot/static` is pinned (T16). The gap was the **CONTROL**: `AppearanceScreen` was a 2-state Off/On toggle whose "on" wrote `"calm"`, with a comment saying Freeze was "deferred to a later mobile-UI phase", so **no phone user could ever select the level the plumbing already honoured**. Replaced with a three-segment control over `POWER_SAVER_LEVELS`, matching the web's, with a per-level caption ("reduces motion" and "stops all motion" are different promises). The regression test presses **freeze first** — it is the level the old control could not reach, so a slide back to a boolean toggle fails there rather than shipping quietly. Pre-existing. Means Freeze could not be verified on-device for Phase 4a. |
| **P7** | **`Sign out` was clipped off the right edge of the HUD header.** Measured from the a11y tree at the iPhone 17 pin, not eyeballed: the label's frame was `x=353.3 w=53.7`, so its right edge landed at **407.0** on a **402.0**-point screen. | **RESOLVED 2026-08-05, and it was a fidelity defect, not a layout one.** The prototype's header (`Reactive Trader Mobile.dc.html:88-94`) carries exactly TWO controls on the right, both fixed 40x44 glyph buttons — `◐` and `⌖` — and **no sign-out at all**. The port had substituted text labels for both glyphs and added a third affordance the design never budgeted width for; ~70pt of text against an 80pt budget is what ran the row off the screen, and the control that fell off was the one we had added. So the fix is a restoration: both buttons are glyphs at the prototype's box, and sign-out moved into the Appearance sheet — where the web client keeps its account actions too, making it the closer analogue as well as the one that fits. Two consequences worth recording. **(1)** A glyph cannot be read aloud, so `LockButton` gained an explicit `accessibilityLabel` it never needed while it said "Lock" — swapping text for an icon trades a visual bug for an accessibility one unless the control is named. **(2)** `AppearanceScreen` now renders a child that touches `useAuth`, so two test fakes needed the seam; the covering test asserts sign-out **on the sheet**, because `LogoutButton.test.tsx` renders the button directly and would still pass if the screen stopped mounting it — leaving the app with no way to sign out at all, and the header's own test no longer knows the control exists. |
| **P8** | **The radial dock's hex FAB covers the status strip's FPS readout.** The dock is drawn after `StatusStrip` in `Chrome`, and their frames overlap: FAB `x=172 y=756 w=58 h=58` against `60FPS` at `x=195.7 y=766.7 w=31.7` — so the cell is fully occluded at rest, on every screen, in the app's normal state. The strip renders it correctly; it is simply painted over. | **RESOLVED 2026-08-08 — and the prototype could not be read for the fix, which was the finding.** Every number involved is already a verbatim port of it (FAB `bottom:26px` / 58×58; strip `gap:11px` / `padding:4px 0 3px`; module row `height:60px` / `padding:0 18px`), yet [`reference-shots/rates/grid.png`](design/mobile/v1/reference-shots/rates/grid.png) shows the hex merely *nicking* the strip. The mock renders inside a phone frame at **~89% scale** — its hex measures 155px where a true 1:1 iPhone 17 render of 58pt gives **174px** — so at real device scale the same numbers collide. The design carries a latent geometry bug visible only at 1:1. The FAB therefore does **not** move: clearing the strip would need `bottom <= 2` (on the home-indicator gesture bar), and shrinking the hex to ~34pt breaks the 44pt touch-target floor. Instead the telemetry row reserves `DOCK_FAB_CLEARANCE` (hex + one 11pt inter-cell gap each side) down its centre, both halves `flex: 1` so the gap stays exactly centred — centring the five cells as one run puts it ~17pt off, because the two sides carry unequal text (~106pt vs ~140pt). FAB size moved to a shared `dockMetrics` module so the two cannot drift apart. **The `shell/chrome` golden is intentionally stale** until the next native session: it shows the occluded strip, because a golden compares the app to *itself* and so has been green on this defect since the scenario existed. |
| P6 | `useMemo`'d `SkPath` captured in a worklet closure was flagged unproven (every other scene builds paths inside the worklet). | **RESOLVED 2026-07-25** — `DockingScene` uses it and renders correctly on-device. The documented fallback was not needed. |

## 4. Test and tooling wrinkles

| # | Wrinkle | State |
|---|---|---|
| **T1** | **The RN visual + e2e tiers gate nothing in CI** — iOS pixels need macOS, and every other workflow runs `ubuntu-latest`. The web visual tier *does* run post-merge. | **IN PROGRESS. The blocking premise was false and is now disproved by measurement.** This repo is PUBLIC, so GitHub-hosted macOS runners are free — access was never the obstacle. A `workflow_dispatch` spike ([`ios-visual-spike.yml`](../.github/workflows/ios-visual-spike.yml)) proved the environment on 2026-07-26 in **6 min 27 s**: `macos-26` instantiates and boots **iPhone 17 × iOS 26.5**, an exact match for the goldens' `ios-iphone17-26` pin. Two assumptions fell — **97 GB free, not 14 GB** (disk is a non-issue), and the image's `python3` is **3.14.6**, above `fb-idb`'s ≤ 3.13 ceiling, so **idb genuinely would not have installed** and Maestro is the right CI tier for a now-verified reason. Full numbers: [mobile-ci-testing-options.md §7.0](mobile-ci-testing-options.md). **Still open:** whether CI pixels match the Mac-local goldens (§5 predicts not, and the answer is a second golden bucket, never a wider tolerance — the 6% tolerance already dwarfs the 0.04% that the real #147 regression moved), and promotion to a scheduled non-gating workflow. **Never a PR gate** — see §4. |
| T2 | A capture failure was indistinguishable from a visual regression — a failed deep link screenshotted the Expo launcher and reported a large diff. | **FIXED** (#350 + #353): the driver asserts readiness via the a11y tree and throws rather than returning an unverified screenshot. |
| T3 | `testID` only surfaces in the iOS a11y tree for nodes that are themselves accessibility elements — a plain container `View` never appears. Marking the wrapper `accessible` surfaces it but collapses the subtree (measured 41 nodes → 3). | **FIXED** (#353): the readiness marker is a 1×1 empty accessible sibling. Worth remembering for any future harness marker. |
| **T7** | **Every scenario after the first in a multi-scenario capture run was driving a dead app.** Re-opening the dev-client URL against a running app tears it down to the iOS home screen instead of re-navigating it; `waitForAppBoot` then passed that as "booted" because it only rejected the *Expo* launcher. Same class as T2 — inferring a good state from the absence of one known-bad state — reproduced inside the fix for T2. | **FIXED 2026-07-26** (#TBD): each scenario cold-starts from an explicit `simctl terminate`, and SpringBoard is rejected alongside the Expo launcher. Cold-starting then exposed the dev client's "Tools" hint in frame, which is now waited out as a condition rather than slept past. |
| T8 | `blotter/seeded` and `shell/appearance` reproduced at **2.9%** and **4.0%**, not the 0.01–0.05% the other six sat at. Both passed on tolerance, so nothing was red, but that much run-to-run movement meant something time- or data-dependent was in frame. | **RESOLVED 2026-08-01 — it was the iOS status bar.** Its clock advances every minute and it is in frame on every capture, so each golden baked in a wall-clock that could never reproduce; battery level and the cellular glyph drift too. The simctl driver now pins all of it (`simctl status_bar override`, 09:41, charged, full bars) before each capture. Measured: with the bar pinned, two consecutive runs of the boot scenes differ by **0 pixels** in the body and only in the top 120 px before the fix. Whole suite went from 0.01–0.05% (and 2.9–4.0%) to **0.00%**. |
| T4 | `expo start` rewrote `packages/client-react-native/tsconfig.json` and removed `expo-env.d.ts` after every native session; the rewritten formatting failed `biome ci`. Any worktree that captured goldens picked it up. | **FIXED 2026-07-26** (#361). **The revert was the cause** — the churn was *convergent*, not oscillating, and reverting kept pushing the tree away from the fixed point Expo was steering toward. `@expo/cli`'s type generation takes its removal arm because `experiments.typedRoutes` is unset, and its `writeUpdates` is `if (updates.size)`-guarded: once those entries are absent there is nothing left to remove, so Expo stops writing the file entirely. Committing the converged state ended it permanently. Generalisable: check a tool's writer for an idempotence guard before assuming a permanent fight. |
| **T9** | **The Maestro tier had silently drifted and nothing noticed.** `tests/visual/maestro/flows/` held **3** committed flows against **8** `SCENARIO_IDS`, while `maestro/run.ts` iterates all 8 — so the tier could not complete a run. `generateFlows.test.ts` exercised only the pure `flowYaml()` function and never looked at the directory, so CI could not see it. Same class as the parked-Gherkin step-tree drift: a generated artifact committed beside its generator with no test tying the two together. | **DRIFT FIXED 2026-07-26** (found while scoping T1). The 5 missing flows were regenerated — the 3 pre-existing ones came back byte-identical, so the drift was purely *missing files*, not stale content — and `generateFlows.test.ts` gained two guards: the committed `.yaml` set must equal `SCENARIO_IDS`, and each file must match `flowYaml(id)` byte-for-byte. Both were verified to fail when they should: deleting a flow trips both, tampering with one trips only the byte check, so they are non-redundant. **RESIDUE — still open, WIDER, and re-framed 2026-08-06.** Only **3 of 18 `maestro` goldens exist**, and those three are **STALE as of 2026-08-01** — captured before the harness gained its themed background and safe-area inset, so they still pin the white-background / occluded-content frames the simctl tier shed (T14, T17), and they additionally still carry the **dev-menu gear** the simctl set lost in round 6. Because `compareToGolden` returns `ratio: 1` for an absent golden rather than throwing, running the tier today reports **15 failures at 100%** — a missing baseline, not a regression. Two further gaps found while explaining this: the runner **pins no device** (it calls `maestro test` and lets Maestro choose, while its goldens live under a path claiming `ios-iphone17-26` — so a second booted simulator can silently be shot against the pinned baseline), and Maestro **cannot start on this machine at all** (`ERROR: Java 17 or higher is required`), which is the real reason the set stalled rather than any verdict. **The earlier framing of this tier as a "bake-off remnant" was wrong and is retracted**: the bake-off was a 3-scenario viability spike on one device with no Android run, which is enough to say which tiers *can* run and nothing like enough to rank them — and `simctl` is `xcrun`-based, so it can never take the Android shots the maintainer intends to keep. **Deliberately not fixed now:** capturing 15 more goldens against a UI whose visual fidelity is still moving buys churn, not a baseline. Re-baseline the whole tier in one pass **after fidelity settles**, then compare all tiers at full coverage. Fixing the device pin is worth doing first and independently — it also hands the runner the UDID `hideDevMenuFab` needs. Full write-up: [BAKEOFF.md](../packages/client-react-native/tests/visual/BAKEOFF.md). |
| T5 | The CI "Expo bundle smoke" tolerated a react-native-worklets Babel-plugin crash. | **RESOLVED 2026-08-08 — the crash was already fixed and the tolerance had become dead code.** The recorded state ("live and load-bearing; the `::warning` fires on every one of the last three `main` runs"; "deleting it turns CI red immediately") was **a measurement artifact**. `gh run view --log` echoes a step's `run:` script before executing it, so grepping the log for the tolerance's own `grep -qF "[Worklets] Babel plugin exception…"` line matches the SCRIPT, not any runtime output — the same wrong reading was made twice, on 2026-08-05 and again on 2026-08-08 while re-verifying. Filtering out the echoed lines (they carry the `[36;1m` cyan-bold prefix) on main run [31259176893](https://github.com/bettersoftware-io/ReactiveTraderCloudClone/actions/runs/31259176893): **zero** exceptions, **zero** `::warning`s, `Exported: dist`. The four real causes are all fixed in `pnpm-workspace.yaml` (worklets plugin/runtime skew; `@babel/core@8` in the worklets subtree; `@babel/generator`+`traverse@8` via `makeWorkletFactory`'s direct `require()`; `@expo/metro-config` on Babel 8) — the last two via `packageExtensions`, which is what actually closed it. The tolerance is deleted; the step is now a plain `expo export`. A dependency bump was NOT required: an experiment on worklets 0.11.3 / reanimated 4.5.3 (PR #506) was raised on the bad baseline and reverted, since it would have left Expo SDK 57's blessed native set for no gain. |
| T6 | Phase 3's `shell` golden and Phase 4a's `rates` golden are still not pinned. | **RESOLVED 2026-08-05.** Both halves have goldens now, and neither was the capture job the row implied — each scenario had to be *authored*, and each was blocked on a different missing pin. **`rates`** was blocked on the DATA: `PricingSimulator` moved every cell twice over (a `Math.random()` seed walk at construction plus a live tick loop), so `PortFactoryDeps.pricingPinMs` now threads a host-wide pin through `VisualScenarioHost` — under it the walk is skipped and the loop never schedules, so the grid renders at its `KNOWN_CURRENCY_PAIRS` reference prices, at rest. Two wall-clock leaks were closed, not one: `createTick` also stamped `valueDate` from `new Date()`, the same defect class as T32. **`shell`** was blocked on the CHROME: every other scenario mounts a module's *content* via `ScreenContentFixture`, which exists precisely because no chrome sits above it — so the header, connection banner, status strip and radial dock, the frame on screen for the entire session, had zero pixel coverage. `ShellChromeFixture` mirrors `app/(app)/_layout.tsx`'s `Chrome` with an empty body, pinning the strip's live FPS meter through `ShellTelemetryContext` (the production seam built for exactly this, until now referenced only from its own unit test) and the header's pulsing connection dot through `powerSaverLevel="freeze"`. Neither pin alone suffices — `forceReduceMotion` seeds `animatedBackground`, which gates the ambient layer only. The dock is captured COLLAPSED: `open` is internal `useState` with no prop seam, and adding one so a screenshot could open it would put an affordance in production for a test's benefit; the expanded satellite fan is left to the Maestro tier, which can tap. Both pins are host-wide rather than per-scenario, so a scenario that starts rendering prices later inherits the pin instead of silently becoming unreproducible — the lesson `boot/topo` taught (T10). `rates/grid` and `shell/chrome` both verify at **0.00%**. **The shell scenario earned its keep on first run: it immediately surfaced P7 and P8, two real chrome defects no test could previously see.** |
| **T16** | **`boot/static` is the one golden that does not reproduce exactly (0.14%).** `BootEmblem` runs a 900 ms opacity pulse gated ONLY by the OS-level `AccessibilityInfo.isReduceMotionEnabled()` — the harness's `forceReduceMotion` seeds `animatedBackground` (the ambient layer alone) and power-saver `freeze` does not reach it either, so the emblem is captured at a random point in its pulse. | **RESOLVED 2026-08-05.** `BootEmblem` now reads `useBootMotionEnabled()` instead of probing `AccessibilityInfo` directly, so Freeze reaches it like every other surface and the doctrine holds. `boot/static` seeds `powerSaverLevel="freeze"` and re-captured at **0.00%** (was 0.14%, and per T33 that number was a phase sample, not a still). The regression test asserts the wiring only — the fade is `useNativeDriver`, so jest cannot see the value; the pinned golden is the real witness now. **OPEN, and it is really a slice of P5.** The repo's Freeze doctrine is that Freeze kills ALL motion; this pulse ignores it, so the fix is to route the emblem through `useBootMotionEnabled()` (which already encodes "Freeze always wins") rather than to special-case the harness. Deliberately NOT done in the golden session: it changes production motion behaviour and belongs with P5. **Measured 2026-08-04 (T33), and the "random point in its pulse" reading was too kind**: the driver's fixed `postReadySettleMs` phase-*locks* the sample, so captures reproduce and the scenario looks stable right up until a timing perturbation moves the phase. That is why **T33 was opened as a suspected code regression against this very scenario** — read T33 before touching this row. Six raw back-to-back screenshots off one mount climb 24 → 61 → 93 → 118 → 133 in max channel delta; the emblem's own region matches its golden at 0 pixels. |
| **T17** | **Three scenarios mounted screen content with no safe-area inset, so the status bar and dynamic island covered their first rows** — and the goldens pinned that as correct. `blotter/seeded` lost its **`PENDING` filter chip entirely** (the golden asserted three filters where the app has four); `shell/connection-banner` lost the "Live" pill behind the clock; `analytics/dashboard` lost its "P&L" title. Separately `lock/hold` mounted the ring uncentred, so the island covered all but a sliver of the arc — while `LOCK_HOLD_PROGRESS = 0.55` exists precisely so the golden proves a PARTIAL fill. | **RESOLVED 2026-08-01.** New `ScreenContentFixture` applies the top inset (`flex: 1` on it is load-bearing — without it `BlotterModule` collapsed to an empty screen, which the first capture caught); `LockHoldFixture` now centres the ring like `LockScreen` does. Full-bleed surfaces (`boot/*`, `lock/hold`, `shell/appearance`) are deliberately NOT wrapped. **Found only by looking at the PNGs** — a diff against an equally-wrong baseline is green, so neither the tier nor its reproduction percentage could ever have surfaced these. Recipe recorded in the harness README. |
| **T10** | **`boot/topo` had no visual scenario** — the scene prints a **live wall-clock timestamp** bottom-left, so two golden captures minutes apart differ and the golden could never reproduce itself. | **RESOLVED 2026-08-01** via route (a), the preferred one: `BootSceneProps` gained an optional `now`, `BootSceneFixture` passes a fixed instant, and `TopoScene` prefers it over its mount-time `new Date()`. Production passes nothing and still samples the clock, so the live splash is unchanged. `boot/topo` is now registered (14 scenarios) and reproduces at **0.00%**. The footer stamp is KEPT rather than dropped, so the golden asserts the frame the app really draws. `now` is passed to every scene, not just `topo`, so a scene that starts printing the clock later inherits a pinned one instead of silently becoming unreproducible. |
| **T13** | **FIVE of the eight boot scenes rendered BLANK on device** — `hologram`, `layers`, `geo`, `jarvis`, `topo`. A white screen plus a continuous `TypeError: undefined is not a function`; one session logged 253. The live boot splash was affected too, not only the harness. Observed 2026-07-27, iPhone 17 / iOS 26.5. | **RESOLVED 2026-07-31 — all eight variants render on device.** There were **three** causes, not one, and only the first was visible to the detector as it stood: (1) 24 worklet→worklet forward references across five files; (2) a worklet reading a module-level **`const`** declared later (`LayersScene`'s `CORNER_UVS`) — the same capture-by-value rule applied to a constant, which is why `layers` stayed blank through a complete function reorder and why a function-only detector could never be sufficient; (3) a missing `"worklet"` directive on `topoGeometry.topoHeightAt`, found by the 2026-07-27 audit but never fixed (only its `geoPointInside` peer shipped, in #413). **The earlier "reordering does not fix it" finding was a measurement error, not a missing cause**: the detector's regex anchored on `^function`, so the `export function` `gyroYawPitch → clampUnit` reference in the shared `boot3dCamera.ts` was never reported, and `hologram` was reordered in its own file only. `check-worklet-order.mjs` now covers all three classes, resolves imports across the tree, strips comments, exits non-zero, and **is a CI gate** — verified to fail on the pre-fix sources (28 findings) and pass on the fix. Full write-up: [research/2026-07-27-rn-boot-scenes-blank-on-device.md](research/2026-07-27-rn-boot-scenes-blank-on-device.md). |
| **T14** | **The boot visual goldens were captured on a WHITE background.** `VisualScenarioHost`'s wrapper set no `backgroundColor`, so every `boot/*` golden showed its scene over white. | **RESOLVED 2026-08-01 — and the recorded scope was too narrow.** It was never a `boot/*` problem: **every fixture that mounts a LEAF rather than a screen** fell through to RN's default white — `lock/hold`, `boot/static` and `shell/connection-banner` were captured almost entirely white, and `blotter/seeded` had a dark header over a white body (mean 0.88). Fixed once, at the host, with `backgroundColor: t.bgPrimary` — theme-resolved, so the light-pinned `shell/connection-banner` gets classic-light's own background rather than a hardcoded dark. `shell/appearance` moved 0.5405 → 0.5399, the control proving full-screen scenarios were unaffected. |
| **T15** | **`SkPath.moveTo()` / `SkPath.lineTo()` are deprecated** in the installed react-native-skia (2.6.2), in favour of `Skia.PathBuilder.Make()`. Every boot scene building a polyline path hits them; ~22 warnings per session. | **NOT SMALL — re-scoped 2026-08-05, deliberately NOT attempted.** Measured: **49 call sites across 8 files**, seven of them boot scenes drawing *inside worklets*, which is the most jest-blind code in the repo (a mistake renders a blank scene silently). The payoff is quieting ~22 log warnings; both deprecated methods still work. The pinned goldens would catch a regression, so this is doable — but it is a focused session with a golden re-capture, not a drive-by. **OPEN, cosmetic.** Investigated during T13 and explicitly ruled OUT as a cause: both methods still exist and still work, and no scene calls a method that was actually removed (checked every path method used against the installed `Path.d.ts`). Worth a mechanical migration pass to quiet the log, no urgency. |
| **T12** | **No golden existed for `analytics/dashboard`, or for any of the five projected boot scenes.** | **RESOLVED 2026-08-01.** All **14** simctl goldens captured and eyeballed in one session, then verified: **13 of 14 reproduce at exactly 0.00%**, `boot/static` at 0.14% (see T16). Captured via `--scratch` and promoted by copying the reviewed bytes, never a second `--update` — re-capturing would commit pixels nobody looked at. |
| **T11** | **Four of the five projected scenes carry per-frame mutable state in the web** that a worklet cannot reproduce — it captures values rather than sharing a live closure. `geo` accumulates a `trades[]` array, `jarvis` shares a `ringZPlane` and writes `fragment.currentZ`, `topo` mutates `lastTickIdx`/`val`/`dir`/`flashStart` per peak. | **RESOLVED in the ports, worth remembering.** Each is now derived purely from `elapsedSec`, following `coreArcs.ts`'s `activeFlowArcs`. The failure mode is never a crash — it is silent divergence (rings sitting flat, cross-links at wrong depths, prices frozen), which is why each has a test pinning the *motion* rather than just the range. `geo`'s reconstruction is provably exact because the web's concurrent-trade cap can never bind; that is asserted. |

### Phase 5a Credit — carried forward (2026-08-01)

| # | Item | Detail |
|---|---|---|
| T18 | **5a's on-device sign-off has not run** | **CORRECTED 2026-08-05 — the golden half of this was already DONE and the row was stale.** All 16 registered scenarios have committed goldens, `credit/rfq-tiles` and `credit/sell-side` included; they were captured 2026-08-02. What remains is only the human lifecycle observation, which the maintainer has explicitly de-prioritised in favour of comparing the re-captured golden set against the prototype corpus. Plan Task 11 needs a booted simulator and a human. It is the ONLY witness for the whole Reanimated worklet class (§5): `pnpm check:worklet-order` is clean, and jest is structurally blind. Until it runs, the countdown ring's 1s glide, the ACCEPT halo loop and the `AWAITING…` pulse are unverified on a device. Its two goldens (`credit/rfq-tiles`, `credit/sell-side`) are registered but **not captured** — the Maestro flows exist, the PNGs do not. Queues into the single serial native tail. |
| T22 | **RESOLVED 2026-08-01 — new-RFQ quantity chips were 1000x too large** | Found by the Task 11 on-device run, and by nothing else. `CreateRfqUseCase:25` multiplies its input by `CREDIT_QUANTITY_MULTIPLIER` (1_000), so its parameter is **UI-scale**; the chips carried the prototype's literal notional values (`1_000_000` …) and a "1M" tap broadcast an RFQ for **1,000,000,000**. jest could not catch it: the unit test asserted `onSelect` was called with `2_000_000`, i.e. it encoded the same wrong expectation the component did. The free-text field these chips replaced took UI-scale input from the operator, so the scaling had never been written down anywhere the chips' author would see it. Chips are now UI-scale and `millionsLabel` scales for display; the tests derive their ids from `RFQ_QUANTITY_CHIPS` so a literal cannot drift again. |
| T23 | **RESOLVED 2026-08-01 — the DIRECTION label did not match its siblings** | INSTRUMENT and QUANTITY render at 8.5px mono with 2px tracking (those labels live inside the new chip components); DIRECTION was left on `NewRfqForm`'s old 12px display-font `fieldLabel` when its text was uppercased, so on device it read a size and a family apart. dc.html:269 styles all three identically. |
| T24 | **UNCONFIRMED — RFQ tiles briefly render nothing after a filter switch** | Observed once during the Task 11 run: with `DONE` selected, the a11y tree held four cards (`rfq-dismiss-240/237/238/235`, two `ACCEPTED` stamps) at sane on-screen coordinates while the screen painted **empty**; it recovered within seconds and a subsequent recorded filter switch could not reproduce it. The suspect is Task 6's `entering={FadeInDown}` not starting under a heavy live quote stream, leaving cards at opacity 0. **Not yet a confirmed defect** — it may equally have been a slow frame under load. Needs a clean reproduction (record video across the tap, with the app under a busy stream) before anyone changes the cascade. |
| T25 | **RESOLVED 2026-08-02 — the accept ceremony was never visible** | Plan Task 5 offered two routes for the linger and the implementation took route 1, "encode it as the exit animation's duration". **Route 1 cannot work.** React drops the card from the filtered list the instant its state turns `Closed`, so the element Reanimated animates out is the **pre-accept snapshot**: it carries no `ACCEPTED` stamp, and it reads the `Open` branch of `exitMsFor`, taking the 220 ms plain exit rather than the 1250 ms linger. Measured on device: a **200 ms** fade of a card still showing ACCEPT buttons — so accepting an RFQ looked like the card simply vanishing. Fixed with the plan's route 2, which is also the prototype's own rule (dc.html:2127-2129): a traded RFQ stays under LIVE until dismissed. Expired and cancelled RFQs still leave at once — only a trade earns an acknowledgement. The linger is now a property of `filterRfqs`, so it is unit-testable and still timer-free. |
| T26 | **RESOLVED 2026-08-02 — sell-side had no `YOUR QUOTES` heading, and buried live tickets** | The heading specified in plan Task 9 step 5 (dc.html:305) was never rendered, and the panel mapped `rfqs` in raw seam order, so a freshly-broadcast RFQ's live ticket appeared **below** the settled history — backwards, since the ticket is the only thing on that screen with a countdown running on it. Live tickets now render first, settled rows under the heading. |
| T27 | **RESOLVED 2026-08-02 — quote prices had ragged decimals** | `displayText` interpolated `state.price` raw, so one column read `$99.47`, `$99.8`, `$100.27` — decimal points not aligned, which defeats the only job that column has. Now `toFixed(2)`. |
| T28 | **`sell-side-ticket-{id}` is invisible to the a11y tree** | **RESOLVED 2026-08-05.** Fixed with T3's marker idiom, NOT by marking the card `accessible` — that is the obvious repair and the wrong one, because an accessible view collapses its subtree (41 nodes → 3 when measured) and would have hidden `sell-side-submit-*` and the stepper pads, the very controls a flow needs to tap. A 1×1 accessible `sell-side-present-{id}` sibling carries the presence signal instead. Deliberately **outside** the `sell-side-ticket-` prefix: the first attempt reused it and broke `SellSidePanel`'s `getAllByTestId(/^sell-side-ticket-/)`, which counted 6 where 3 were expected. The ticket's `SurfaceCard` testID never appears in `idb ui describe-all`, because a plain RN `View` is not an accessibility element unless marked (the same rule that hid `VisualScenarioHost`'s readiness marker until it was given `accessible`). Its children (`sell-side-submit-{id}`, the stepper pads) are reachable, so nothing is blocked today — but the **Maestro visual tier keys on testIDs**, so a `credit/sell-side` flow that waits on the card id would hang. Give it `accessible` before that flow is written. |
| T29 | **RESOLVED 2026-08-02 — every boot scene was frozen at t=0** | `useFrameCallback` registers its argument in an effect keyed on `[callback, autostart]`, so an **inline arrow** — a new identity every render — unregisters and re-registers the callback on every render, and re-registration restarts `timeSinceFirstFrame`. `BootSequence` re-renders on every progress tick, so `elapsedSec` never escaped the first frame or two: every scene drew its **t=0 frame forever** while the progress bar advanced above it. On device it read as an animation stuck flicking between two frames. Fixed with the `useRef` + `current === null` build-once idiom (ADR-003 bans `useCallback`). The `"worklet"` directive must be written explicitly once the function leaves the inline position — the Babel plugin auto-workletizes only the inline argument, and without it the UI runtime throws "Tried to synchronously call a Remote Function" on the first frame. **This is the third distinct way this repo's boot scenes have failed silently** after the blank-scene worklet-ordering class (#439); like those, jest was structurally blind. **It is still not covered by a test** — see T31: the regression test is written and works, but it needs the reanimated mock pinned first, and pinning it broke four existing specs. The simulator remains the only witness. |
| T30 | **`useShellTelemetry` has the SAME frame-callback bug — NOT fixed** | **RESOLVED 2026-08-05.** The callback now detects the clock restarting (`frame.timeSinceFirstFrame < windowStartSv.value`) and resyncs, discarding the frames counted against the old clock. T29's repair — hoisting the callback — trips `react-hooks/immutability` here, which tolerates shared-value writes in an inline `useFrameCallback` argument but not a hoisted one; resyncing fixes the same defect without that fight. One dropped window is invisible, a wrong number is not. **Device-only verifiable** — jest's `useFrameCallback` stub never invokes the callback. It passes an inline arrow to `useFrameCallback` too, so its clock re-zeroes every render; `windowStartSv` then holds a stamp from a previous clock and the published figure is nonsense. Observed on device: **302, 485 and 1264 FPS** in the status strip. Not fixed here because the obvious repair (hoisting the callback) trips `react-hooks/immutability` — the rule tolerates shared-value writes in an inline `useFrameCallback` argument but not in a hoisted one — and working that out deserves its own change rather than being smuggled into a boot fix. The FPS readout is decorative, so this is cosmetic, but it is the same defect and will need the same shape of fix. |
| T31 | **The reanimated jest mock's `useSharedValue` is unstable — NOT fixed** | **RESOLVED 2026-08-05, and it was three small edits — the "reverted, leans on the per-render rebuild" note oversold it.** `useSharedValue` is now `useRef`-backed in `jest.setup.ts`, matching the real hook's lifetime. Re-running the suite reproduced exactly the recorded 4 failures in 2 suites, and both causes were mechanical: `HoldToUnlockRing` called the mock **in the test body, outside any component** (legal only because it was not really a hook — now a plain `{value}` stub), and `LaserSceneHarness` relied on the mock rebuilding its object to propagate a changing prop. That second one is the point: its own docstring said it "turns `elapsedSec` into a fresh shared value on every render", true of the mock and **false of the hook**, so the harness needed the mock to be wrong — T29's defect living inside the instrument built to catch it. It now writes `elapsed.value` through, as production would. 325/325 green. **T29's regression test is unblocked but NOT yet written.** It builds a new `{value}` Proxy on **every render**, where the real hook is `useRef`-backed and stable for a component's lifetime. Any code memoising on a shared value therefore looks broken under jest while being correct on device, and the reverse — so **T29's regression test cannot be written until this is pinned**. Pinning was attempted and reverted here: a `useRef`-backed stub broke four existing specs (`HoldToUnlockRing`, `LaserScene`), which evidently lean on the per-render rebuild. Worth doing on its own, with those four specs re-read first; it would convert an entire device-only defect class into a CI-visible one. |
| T19 | **The sell-side ticket shows no counterparty** | **ACCEPTED — not open work; a deliberate fidelity gap pending a domain change Phase 5 §4 freezes.** The prototype's `CLIENT SELLS · {qty} · ADAPTIVE ASSET MGMT` has three segments; ours has two. `Rfq` carries no client identity in `@rtc/domain`, so the third was omitted rather than invented — a deliberate fidelity gap, not an oversight. Closing it means a domain/wire change (a counterparty on the RFQ), which Phase 5's §4 freezes. |
| T20 | **New-RFQ quantities are now four fixed chips** | **ACCEPTED — not open work; this is the prototype's own design.** `[1M, 2M, 5M, 10M]` (dc.html:2181), replacing free-text entry. That is the prototype's design and matches what the desk broadcasts, but an odd size is no longer expressible on mobile. The web client still takes any quantity. |
| T21 | **Instrument chips wrap; the plan said scroll** | **ACCEPTED — not open work; the deviation was reasoned and is better than the plan's text.** Plan Task 7 specified a horizontal `ScrollView`; dc.html:262 draws a two-column grid. The grid was built instead — with more than six instruments a horizontal rail would push the overflow off-screen, which on a picker is worse than a taller grid. Noted so the deviation is not read as drift. |
| T34 | **RESOLVED 2026-08-03 — ⟳ Replay Boot did nothing, for three stacked reasons** | Reported by the user after an on-device pass; every layer of it was invisible to jest and to the goldens. **(1)** Visibility lived in a `bootDone` `useState` in `app/(app)/_layout.tsx`, so `reboot()` re-raised `BootGatePresenter.visible$` and **nothing on RN subscribed** — the presenter had a passing unit test and sat in the devtools manifest, and the defect was entirely in a consumer that reimplemented the same state locally. `BootGate` now reads `visible`/`dismiss` off the seam, the shape client-react has always used, and the one-shot `shouldPlayBootSplash()` decision is seeded into the presenter through the `bootSplash` port instead. **(2)** That fix introduced a second bug: `BootGate` no longer unmounts, so its `Animated.Value` outlived the splash it had faded to 0 — the replay re-rendered the overlay **at opacity 0**, mounted and ramping and completely invisible, with nothing in the a11y tree to say so. A `useEffect` re-arms it on every raise. **(3)** Even then nothing was visible, because the splash renders *behind* the Appearance sheet — an opaque full-screen overlay — so the whole sequence played and finished unseen. `AppearanceScreen` now takes an `onReplayBoot` slot and the overlay passes its own `onClose`. **jest cannot witness (2) at all**: the fade runs `useNativeDriver: true`, so it never touches the JS-side value and `style.opacity` reads 1 in a test whether faded or not — the regression test asserts the re-arm *call* and says so, and the simulator stays the only witness for the pixels (same shape as §5's worklet class). Verified on device: the splash replays, ramping 2 → 4 → 9%. |
| T35 | **RESOLVED 2026-08-03 — the ACCEPTED stamp was two elements where the design has one** | The prototype's RFQ header slot is exclusive: a countdown ring while live, one animated `kfStamp` state pill once settled (dc.html:238, `'✓ ACCEPTED'` / `'EXPIRED'`, mono 8px, 1px border, `3px 7px`, radius 5, animated only when accepted per dc.html:2173). We rendered a static `Done` there **and** a large boxed `ACCEPTED` banner below the quote rows. The banner had been modelled on the wrong prototype element — **dc.html:543, the *Rates* execution ceremony** (26px, letter-spacing 5, a 2px border) — which is a different component on a different surface. Merged into the header pill: a fidelity fix and a space one, since a whole row was being spent to repeat what the header already said, and vertical space is the scarce resource on a phone. The decision was recorded 2026-08-02 and had not been built. Golden re-captured. |
| T36 | **RESOLVED 2026-08-03 — the Appearance overlay drew its title under the status bar** | `AppearanceOverlay` is `StyleSheet.absoluteFill`, which pins it to the PHYSICAL screen — status bar and dynamic island included — and its header added only its own `paddingVertical: 12`, so "APPEARANCE" rendered straight through the clock. `ShellHeader` already owns the same inset via `useSafeAreaInsets` for the same reason; a full-bleed overlay has to pay it too. **The committed `shell/appearance` golden has shown this collision since Phase 2 and passed every run**, which is the tier's boundary stated plainly: a golden answers "did this change?", never "is this right?". Only a person opening the app could find it, and one did. Golden re-captured. |
| T32 | **RESOLVED 2026-08-02 — `blotter/seeded` re-dated itself every calendar day** | Found while verifying the newly-captured Credit goldens: all five rows were byte-identical to the golden **except every date, each shifted forward by exactly one day** (`2026-07-29` → `2026-07-30`, and so on down the list). `TradeStoreSimulator:94` calls `isoDaysFromNow(-spec.daysAgo)` and `isoDaysFromNow` (`fx/trade.ts:66`) defaults `baseMs` to `Date.now()`, so the "pre-seeded static trades" are static in every field *except* the one derived from the wall clock. The tier's README calls this fixture stable because the trades are "not `Math.random`, not live" — true of the trades, false of their dates. It is the **same defect class as `boot/topo`** (excluded from the harness until `BootSceneProps.now` gave it an injected clock) and `credit/rfq-tiles-empty` (dropped outright), and the fix is the same move: `isoDaysFromNow` **already takes `baseMs`**, so the fixture need only pass a pinned one. It had passed the whole time, because five short date strings are ~0.1% of the frame against a 6% tolerance. **A golden that silently re-dates itself is worse than a missing one** — it looks like evidence. **Fixed** by threading an optional base through the one seam that was already there: `TradeStoreSimulator`'s constructor takes `seedBaseMs = Date.now()`, `PortFactoryDeps` carries an optional `blotterSeedBaseMs`, and `VisualScenarioHost` pins `BLOTTER_SEED_BASE_MS`. Nothing in the app passes one — the running desk still wants a blotter whose history stays a few days old however long the process lives. The golden was re-captured and now reproduces at **0.00%** (it was 0.84%). Two regression tests: one asserts exact dates off a fixed base, and the second builds a store on two different simulated calendar days and asserts the **pinned** one is stable *and* the **un-pinned** one genuinely moves — without that second half the test would pass against an implementation that ignored the argument. The pin is **noon UTC**, deliberately not `fixtures.tsx`'s local-time `PINNED_WALL_CLOCK`: `boot/topo` *renders* a wall clock and must be pinned in the capture timezone, whereas these are date-only strings taken through `toISOString()` and must be pinned in the timezone the derivation happens in. Copying the neighbouring constant because it was also called "PINNED" would have reintroduced the same bug class in a form only visible on a machine east of UTC+9. |
| T33 | **RESOLVED 2026-08-04 — NOT a defect: `boot/static`'s "stale golden" was one phase of a live pulse** | Re-opened with measurements instead of argument, and the original finding does not survive them. **The emblem is pixel-identical to its golden**: over the region that contains it (x 350–860, y 0–300), two independent captures today both report **0 differing pixels, max channel delta 0**. There is no regression and nothing to re-shoot — **PR #456 and the `@rtc/motion-core` dist rebuild are both exonerated.** The cause is visible the moment you stop capturing through the driver: six raw `simctl` screenshots taken back-to-back off a **single mounted scenario** climb monotonically against the first — max channel delta **24 → 61 → 93 → 118 → 133** — and the changing pixels bound to x 499–706, y 13–274, which *is* the emblem. `BootEmblem` is pulsing. The recorded 147 was a phase of that pulse. **The reasoning that got this wrong is the reusable part.** "Two consecutive captures a minute apart were pixel-identical, so the render is deterministic and the golden is stale" — the premise is true and the inference is backwards. The driver waits a **fixed settle delay after mount** (`postReadySettleMs`), so it samples the same point in the pulse every run; cross-run determinism is precisely what a phase-locked sample of an animation looks like, and is no evidence at all that the subject is static. **This was predicted in writing before the golden was ever committed** — `scenarios.tsx` (the `boot/static` bullet) states that `BootEmblem`'s pulse is gated by `AccessibilityInfo.isReduceMotionEnabled()`, an **OS-level** signal, "so it is NOT frozen by anything this harness controls", and asks for exactly this check at Task 11. The harness freezes motion by driving the app's own preferences; this one scene keys off a signal outside that authority, and the exception is invisible from inside because the capture still comes back byte-identical. **And it was already written down as T16, six rows above — which is the sharpest lesson here.** T16 names the same 900 ms pulse, the same OS-level gate, and the right fix (route the emblem through `useBootMotionEnabled()`, since it is really a slice of **P5**), and has been sitting OPEN since 2026-08-01. T33 was opened the next day as a suspected *code regression* by someone who had the answer in the same file and did not connect it. **A findings page only pays off if you search it before opening the next finding** — the cost here was a do-not-touch flag on a golden, two days of an imagined `BootCanvas` regression, and a capture session to disprove it. **Residual (1) is therefore NOT new work: it is T16**, unchanged, and the phase-sample framing above is a sharper statement of the same defect (T16's "captured at a random point in its pulse" understates it — the sample is not random but *phase-locked*, which is worse, because it looks stable). **Residual (2) is new:** Today's full-frame delta is **1.2158%**, and *all* of it is the Expo **dev-menu gear**, which sat against the right edge when the golden was shot and against the left today — the golden bakes in a draggable dev-client control. That one fires on every capture until the gear happens to land where it was in August, and unlike the pulse it is pure chrome: nothing about the app is being asserted by those pixels. |
| **T44** | **`shell/appearance` is the second knowingly-stale golden** | The Appearance surface was rebuilt in this branch as a bottom sheet, replacing the old full-screen overlay the committed `shell/appearance` golden still pins. Same shape as `shell/chrome` (P8, above) and blocked on the same thing — a native dev-client build and a booted iOS simulator — so re-capture both together in ONE session, not one at a time. A diff on that next capture is the expected outcome, not a regression to investigate. |

### Phase 5c Analytics — fidelity gaps found at step 5 (2026-08-04)

**Phase 5c's step 5 — "judge fidelity against the prototype" — ran on
2026-08-04 and is the reason 5c could be closed.** It is the step the plan
reserves for a human ("this task cannot be completed by an agent alone"), and
it had not run: §6 claimed it was signed off, `STATUS.md` said it was
outstanding, and the deliverable test settled it — step 5's output is "record
any gap **here**", and there was not one Analytics fidelity entry in this file.
Steps 1–4 and 6 had run; 5 and 7, the two a human does, had left no trace.
**The plan file's checkboxes are not evidence either way** — all seven are
still unticked including the ones known to have run, so it records intent, not
state. Ask instead what artifact would exist if the step had happened.

Method: the app's committed `analytics/dashboard` golden against
[`reference-shots/analytics/dashboard.png`](design/mobile/v1/reference-shots/analytics/dashboard.png),
every candidate then confirmed in source rather than by eye. **Data differences
are not gaps** — the scenario mounts `AnalyticsDashboard` over a literal book,
so the figures, 4-vs-7 pair rows and 5-vs-8 bubbles are all fixture, by design.

Two notes before the table. **All seven survived a golden reproducing at
0.00%**, because a golden compares the app to *itself*; only the side-by-side
could find them. And the `analytics/dashboard` golden also carries the Expo
dev-menu gear in frame, which **confirms T33's residual (2) is systemic across
the tier rather than particular to `boot/static`**.

| # | Gap | Detail |
|---|---|---|
| **T37** | **The Exposure bubbles were ported from the WRONG PROTOTYPE — the web one.** | **RESOLVED 2026-08-05.** Re-pointed at the mobile design: 11% fill, full-opacity 1px ring, **glow deleted**, currency 9px and amount 7.5px (were 12/15/9). The cascade unwound with it — `AMOUNT_MIN_DIAMETER` drops 40 → **30**, the mobile prototype's real floor, so the smallest bubble regains its value label. The layer test now asserts `SkiaCircle == 2` and `SkiaRadialGradient == 0`; the explicit zero is the point, since a glow creeping back is the regression. `ExposureBubble.tsx`'s constants comment cites `ExposureBubbles.module.css`, and that file lives in **`packages/client-prototype/`** — the **v2 WEB** design port, not mobile-v1. Every constant matches it exactly: fill `0.1` ← `rgba(…, 0.1)`, ring `0.5` ← `opacity: 0.5`, the radial glow ← `radial-gradient(…)`, and label sizes `12`/`15`/`9` ← `12px`/`15px`/`9px`. **The mobile prototype (dc.html:194-197) is different in all four:** fill `color-mix(… 11%, transparent)`, ring `1px solid` at **full** opacity, **no glow at all**, currency **9px weight 600**, amount **7.5px**. So the discs read as heavy glowing blobs where the design draws crisp thin rings with small precise labels. **Same class as T35** — modelled on the wrong source — except here it is the wrong *client*, and the comment asserts the mobile prototype while implementing the web one, which is why review could not catch it. **It cascades:** oversized labels stop fitting small bubbles, so `AMOUNT_MIN_DIAMETER = 40` was invented to suppress the amount — which is why the smallest bubble renders with **no value at all**. That constant's own justification ("40px is the prototype's own floor, its diameter is `40 + sqrt(|M|) * 11`") is the **web** prototype's formula (`positionsData.ts:25`); mobile sizes bubbles `30 + (|usd| / maxExp) * 44`, a **linear** ramp with a **30px** floor, and draws the amount on every bubble unconditionally. Fixing the label sizes may remove the need for the suppression rule entirely. |
| **T38** | **All three card titles are the wrong type, and two dropped their unit** | **RESOLVED 2026-08-05.** Titles are now mono 8.5px on 2px tracking, uppercase, with their unit qualifiers restored (`PROFIT & LOSS · USD`, `PAIR P&L`, `NET EXPOSURE · USD EQUIV`). App (`AnalyticsDashboard.tsx` `widgetTitle`): `fontSize: 12`, `fontFamily: t.fontDisplay`, `letterSpacing: 0.5`, mixed case. Prototype: **8.5px mono (`var(--fm)`), `letter-spacing: 2px`, uppercase** — identical for all three (dc.html:167, 179, 192). The copy differs too: `PROFIT & LOSS · USD` → `P&L` and `NET EXPOSURE · USD EQUIV` → `Exposure`, so **both unit qualifiers are gone** and the reader is no longer told the book is in USD. |
| **T39** | **The P&L delta chip is missing entirely** | **RESOLVED 2026-08-05.** The chip renders top-right of the P&L card. **The window is derived from the history timestamps, not hardcoded** — the prototype's `12S` is its own mock cadence, ours is whatever `AnalyticsSimulator` appends at, and a literal `10S` beside a changed constant is precisely the stale-by-construction copy that produced T32. Absent (not `Δ +0k / 0S`) with fewer than two points. The prototype draws `Δ +4.2K / 12S` in a bordered pill at the card's top right (`pnlDelta`, dc.html:977). Nothing in `AnalyticsDashboard` renders it, and no presenter supplies it — this needs a value as well as a view. |
| **T40** | **The pair bars have no track** | **RESOLVED 2026-08-05.** The track now draws the prototype's recessed rail (`t.panelHead`, radius 3, clipped). `PairPnlBar.tsx`'s `track` style is `{ flex: 1, height: 12, justifyContent: "center" }` — no `backgroundColor`, no `borderRadius`. The prototype's track is a **recessed rail**: `background: var(--panel-head); border-radius: 3px; overflow: hidden` (dc.html:183). Ours are bars floating on the card, which loses the "how full is this" reading the rail gives. |
| **T41** | **The bar geometry and weight diverge** | **RESOLVED 2026-08-05.** Bar is 10px inside the 12px track, radius 2, at 0.85 opacity, and the centre line moved from `t.textMuted` to `t.border`. Prototype: inset `top:1px; bottom:1px` in a 12px track (**10px** tall), `border-radius: 2px`, **`opacity: 0.85`**. App: **8px**, square corners, full opacity, and a centre line on `t.textMuted` where the prototype uses `var(--border)`. Together these are why our bars read heavier and harder than the design's. |
| **T42** | **Pair labels lost their separator** | **RESOLVED 2026-08-05.** A module-private `slashPair` splits 6-character symbols only; anything else passes through rather than being sliced at a position that may not be a boundary. `EURUSD` where the prototype renders `EUR/USD` (dc.html:182). A display-formatting gap, not a data one — the domain symbol is unseparated and nothing re-inserts the slash. |
| **T43** | **The P&L chart is smoothed; the prototype is a polyline** | **ACCEPTED — not open work; the smoothing is 5c's own specification.** The prototype joins raw points (`pts.join(' ')`, dc.html:971), so its sparkline shows per-tick vertices; ours draws a smoothed spline. **Deliberate** — 5c's plan specifies a smoothed line — so this is recorded as an accepted deviation rather than a defect, but it was an implicit choice and is now an explicit one. |

## 5. The worklet hazard (standing, not a bug)

Any function reached from inside a Reanimated worklet must itself carry
`"worklet"`, transitively, including shared `@rtc/motion-core` helpers. A miss
throws `[Worklets] Tried to synchronously call a Remote Function` on a real
device. **jest is structurally blind to the entire class** — its Reanimated mock
runs worklets as plain JS with full module scope — so a green suite is never
evidence of device safety.

Known instances: #334 (`coreGeometry` default-parameter capture), #340
(`ringDashOffset` unmarked, so the lock ring never worked on any real device),
and one caught pre-merge during 6b-1 (`craftGridLines`, a build-once helper,
called per frame from inside a worklet).

The mitigation that actually worked: each geometry module records which exports
are per-frame (marked) and which are build-once (deliberately unmarked), turning
an invisible runtime contract into one the next task can read. Do not add the
directive to a builder to make a per-frame call legal — precompute and scale at
draw time instead.

**The second half of the rule, added 2026-07-31 (T13).** A missing directive is
only one of the two ways a worklet reaches something broken. The other is
**order**: a worklet's closure is captured BY VALUE at module evaluation, and
the Babel plugin rewrites `function foo()` into a non-hoisted binding, so *any
module-level binding declared later in the same file arrives as `undefined`* —
**constants exactly as much as functions**. That second clause is the one that
cost the most: `layers` stayed blank through a complete function reorder because
a single `const CORNER_UVS` sat below its first worklet reader. Imports are
immune (live module bindings, evaluated first), which is why moving a helper to
a sibling module is a permanent fix where reordering is a local one.

Both halves are now gated by `pnpm check:worklet-order` (CI `checks` job +
`/rtc:gauntlet` fast tier). It is necessary, not sufficient: it cannot see an
arrow-function worklet, a function passed as a value, or anything dynamic, and
no static check can prove a Skia scene draws what it should. **A clean report is
not evidence that a scene renders — only a device is.**

## 6. Deferred work with no plan yet

| Item | State |
|---|---|
| **Phase 5** (Credit / Equities / Analytics) | **5c (Analytics) is COMPLETE (2026-08-04)** — golden captured, on-device steps 1-4 + 6 verified 2026-08-01, and **step 5 judged 2026-08-04**. This row previously claimed step 5 was "signed off (2026-08-01)" while `STATUS.md` listed it as outstanding; `STATUS.md` was right, and the deliverable test is what settled it (step 5 writes gaps into this file, and there were none). Step 5 has now genuinely run and found **seven** deviations, recorded as T37-T43 above — one of them structural. They are tracked work, not blockers: the phase closes with its gaps written down, which is exactly what step 5 asks for. Plan: [5c](superpowers/plans/2026-07-26-rn-mobile-v1-rehaul-phase-5c-analytics.md). **5a (Credit) is BUILT as of 2026-08-01** — countdown ring, best-quote tint + ACCEPT halo, `AWAITING…` pulse, accept ceremony with a timer-free linger, new-RFQ chip form, the shared 3-way filter, and the sell-side ticket list; two pinned visual scenarios registered. Plan: [5a](superpowers/plans/2026-08-01-rn-mobile-v1-rehaul-phase-5a-credit.md). **Its Task 11 on-device sign-off and golden capture are NOT done** (see T18). **5b (Equities) is designed but has no plan file and no implementation**, and is the larger of the two. |
| **Phase 6b — COMPLETE (porting)** | **All 8 boot variants are ported and registered** as of 2026-07-26: `core`/`laser` (6a), `docking` (6b-1), `hologram` + `layers` (6b-2a), `geo` + `jarvis` + `topo` (6b-2b). The last five share the `boot3dCamera` seam. Plans: [6b-2a](superpowers/plans/2026-07-26-rn-mobile-v1-rehaul-phase-6b-2a-boot-3d-foundation.md), [6b-2b](superpowers/plans/2026-07-26-rn-mobile-v1-rehaul-phase-6b-2b-boot-3d-remaining.md). **Device sign-off is DONE — corrected 2026-08-04.** This row read "none of the five projected scenes has been run on a device" until today, contradicting **T13** in this same file, which records all eight variants rendering on device on 2026-07-31 after three distinct causes were fixed. T13 is later and specific; this row was simply never updated when it landed. Nothing is open here. The standing caveat is unchanged and belongs to §5, not to this phase: jest is structurally blind to the Reanimated worklet class, so a device remains the only witness for any *future* scene change. |
| **Phase 7** (cross-cutting polish + sign-off) | Untouched, last by definition. |
| `withAlpha()` helper / branded hex type | From #301 — colours are built by string-appending an alpha suffix; safe for today's accent tokens, silently invalid for any future `rgba()` one. |
| Equities workspace alignment | RN holds selection in local `useState`; `useEqWorkspace()` is the shared singleton the web client uses for cross-panel sync. |
| RN visual harness | An inset 3D-card scenario is still wanted, to guard the `overflow:hidden` shadow-clip regression class the current full-bleed scenario cannot catch. |

### Projection per boot scene — measured, do not re-derive

The 6b-1 scope note recorded that all five projected scenes clamp the near plane
at `0.4`. **They do not.** Each was read back from its own web variant while
porting 6b-2a:

| scene | `perspectiveK` | near-plane clamp | source |
|---|---|---|---|
| `hologram` | 0.26 | **none** | `bootHologram.ts:216` — `1 / (1 + depth * 0.26)` |
| `geo` | 0.22 | **none** | `bootGeo.ts:528` — `1 / (1 + depth * 0.22)` |
| `layers` | 0.24 | 0.4 | `bootLayers.ts:202` — `1 / Math.max(0.4, 1 + z2 * 0.24)` |
| `jarvis` | 0.30 | 0.4 | `bootJarvis.ts:166` — `1 / Math.max(0.4, 1 + depthZ * 0.3)` |
| `topo` | 0.26 | 0.4 | `bootTopo.ts:381` — `1 / Math.max(0.4, 1 + z2 * 0.26)` |

**All five rows are now read from source**, each with its line cited. Two of the
five — `hologram` and `geo` — have no clamp at all.

This is why [`boot3dCamera.ts`](../packages/client-react-native/src/ui/shell/boot/scenes/boot3dCamera.ts)
makes `minPerspectiveDenom` optional and never defaults it. Defaulting the clamp
on — the obvious convenience — diverges from the web at depth in the unclamped
scenes, in a way no unit test catches and only a side-by-side pixel comparison
shows.
