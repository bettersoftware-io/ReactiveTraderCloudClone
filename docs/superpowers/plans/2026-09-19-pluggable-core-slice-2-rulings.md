# Slice 2 — rulings ledger (SDD execution, 2026-09-19/20)

Plan: [2026-09-19-pluggable-core-slice-2.md](2026-09-19-pluggable-core-slice-2.md) (PR #790). PR A #793 (Tasks 1–3: constants, folds, harness, suites); PR B (Tasks 4–6: the ports, docs, gate). Every `Ruling:` line from the execution ledger, in the order made, each with what it costs if wrong; the deferred minors follow with the final review's triage. The plan's own thirteen up-front rulings are in the plan file.

## Rulings made during execution

1. `services.test.ts` case 2 asserts `await expect(runtime.dispose()).resolves.toBeUndefined()` in place of `Layer.isLayer(HostLive)` — the case's point is "close then dispose does not throw", and a type-guard assertion witnesses nothing — carried in Task 5's dispatch — cost if wrong: none.
2. (pre-ruling, never fired) if an Effect-core blotter derived-stream case fails ONLY by delivery timing, the suite may `await settle()` twice with a comment naming the three hops; the harness's `settle` doc already scopes two turns to a two-hop chain, so a three-hop chain taking a second call is within the envelope's promise, not beyond it — cost if wrong: a slower suite by two macrotasks.
3. `FakeClock.settle()` MUST NOT move time — it stays two `vi.advanceTimersByTimeAsync(0)` calls; the brief's `clock.test.ts` case 2 is what changes, to assert a macrotask-then-microtask chain (`setTimeout(…, 0)` whose callback queues a `Promise.resolve().then(…)`; both land after one `settle()`), which is the continuation shape the cores actually schedule (Effect's scheduler and the async kernel resume on microtasks; timers are the members' own). A 1 ms settle would fire every `advance(N - 1)` boundary the suites lean on (`rowHighlight`, `priceStream`, `priceHistory`, `tileExecution`) — cost if wrong: a chain needing a nested zero-delay macrotask shows up as a red case and gets an explicit `advance(1)` in that suite.
4. `PENDING_SUITES` ends at 44, not the plan's 43 — the plan's arithmetic was off by one; `registry.test.ts`'s drift check is the truth — cost if wrong: none.
5. the order of several live trades arriving in ONE blotter snapshot is uncontracted (the reducer keeps snapshot order); the cap case drives one trade per snapshot — cost if wrong: none observable (the simulator appends one trade per execution).
6. `IDLE_DISCONNECTED` leaves only via a `reconnect` event; the idle case emits it — a fact of the domain fold, not a suite choice — cost if wrong: none.
7. what a machine does AFTER `dispose()` toward a STILL-ATTACHED subscriber is uncontracted — the RxJS `state()` keeps the fold alive for it, the async/Effect machines end it; the bindings' `useMachine` unsubscribes before disposing, so no consumer observes the difference. Suites assert the fold before dispose, and that a fresh subscription after unsubscribe+dispose yields the current value synchronously. The staleFlag/analyticsStaleFlag `expect(...).not.toThrow()` form is replaced by that fresh-subscription assertion (absence is not a reading) — cost if wrong: a later machine whose post-dispose value matters adds a case in its slice.
8. the relative order of events from DIFFERENT sources driven in one synchronous burst is uncontracted (a native fiber-scheduled source vs a synchronous one — the strangler phase makes that mix real: Effect `connection` is native, `priceStream` still delegated); suites `settle()` between driving different sources. Carried into Tasks 4/5 as a known trap — cost if wrong: none; it is the same rule the "only the first value is synchronous" ruling already implies.
9. for a MACHINE, distinct consecutive states ARE contracted — the RxJS stale flag ends in `distinctUntilChanged`, the async machine's `Store` drops `Object.is`-equal writes, the Effect machines write through `setRefIfChanged`; a boolean flag that re-emitted `false` on every price would fan out renders for nothing. The plan's "equal-state conflation stays uncontracted" line is about presenter FOLDS (an Effect fold conflates where the RxJS `scan` re-emits). `staleFlag`/`analyticsStaleFlag` case 1 keeps its whole-array assertion — cost if wrong: a native machine without the dedupe would need one line, visible immediately as a red case.
10. PR A ships on its three task reviews (Task 3's on opus with the cross-task "RxJS-only detail" lens); the plan's final whole-branch review runs on PR B's branch, where the suites are exercised against native cores — cost if wrong: a cross-task defect in PR A surfaces as a red suite in PR B, where it is fixed against a merged target.
11. `pnpm core:parity`'s denominator (72) is authoritative over the plan's "71"; Task 6 quotes the script — cost if wrong: none.
12. a machine's source failure aborts the machine and is rethrown out of band in both alternative cores; the RxJS `state()` would error its subscribers — uncontracted asymmetry, recorded — cost if wrong: a channel added in a later slice against a suite that asserts it.
13. `conflatedFold` seeds its calm flag from `peekCurrent(calm$)` (implementer's fix: `Stream.merge` drains the tick queue before the flag queue, dropping a whole same-turn burst the RxJS core delivers). The RxJS `conflateWhen` subscribes the flag synchronously inside `switchMap` before any tick can arrive, so a replay-current flag is known before the first value there too; a flag that does not emit on subscribe drops values until it does in BOTH cores — the plan's sentence stands and the Effect fold now matches it — cost if wrong: none; pinned by a regression test.
14. `buildAppLayer` uses `Layer.provideMerge(base)` (the plan's `Layer.provide` hides `HostTag` from the resolved runtime, so `composeWithBase`'s `runSync(HostTag)` failed) — the plan's type note was wrong, the fix is the documented `provideMerge` shape — cost if wrong: none.
15. base-side consumers of members that went native in this slice — the RxJS `AnimationDirector` (captures the base `execution.executions$`, `currencyPairs.pairs$`, `priceStream.price$`) and `NarratorMachine`/Jarvis (base `pairs$`/`price$`) — stay wired to the BASE instances until their own slices port them (slice 6 shell: `animationDirector`; slice 7 jarvis; slice 8 removes delegation). Accepted for slice 2 and RECORDED (STATUS residual + ADR "Decided in slice 2"), not coded around: routing the native `execute()` through the base presenter to feed its private Subject would make the "native" member RxJS with extra steps, and overlaying a second `AnimationDirector` over the native streams would keep the base's alive on the base streams anyway. Observable cost under `VITE_CORE_IMPL=async|effect` (never production): tile fill/reject animations do not play; `referenceData.getCurrencyPairs()` and `pricing.getPriceUpdates(symbol)` carry a second live subscription (simulator tick rate doubles for mounted pairs). Task 6's e2e legs are the check that nothing asserted depends on it — cost if wrong: an e2e scenario reds on the alt legs and slice 2 gains an `animationDirector` overlay task.
16. the final whole-branch review is dispatched over the CODE range (149d6133b..cc5fb8333, Tasks 4+5) while Task 6 runs the gate and e2e, rather than after it — Task 6 changes docs only and gets its own task review; any code change after this point (a red gate, a final-review fix wave) gets the scoped re-review as usual — cost if wrong: a fix wave re-reviewed on a slightly different base, which the scoped re-review handles.
17. three cross-core asymmetries the final review found are RECORDED (ADR-006 "Decided in slice 2" + §22), not coded: (i) when the timeout wins, the Effect tile machine releases the losing execution call at once (`Effect.race` interrupts the loser) where the async and RxJS machines hold it until dismiss/new execute/dispose — both sides gain a witness (`pending` length 0 vs 1 after the timeout); (ii) a subscriber arriving AFTER `app.dispose()`: the async retained topic restarts its producer, the Effect retained fold stays silent, the RxJS core never tore down — uncontracted, the bindings unsubscribe first; (iii) a late joiner of a WARM `priceHistory` period sees two equal emissions (lead + replay) on async/RxJS and one on Effect — content-identical, uncontracted — cost if wrong: a later consumer that subscribes after dispose or asserts emission counts adds a case in its slice.
18. teardown order — one rule, stated once in §22: an alternative core releases ITS OWN resources first (loops, retained topics/periods), then the base app, then (Effect) the runtime; the Effect composition's current base → scope → runtime order is equally safe while `base.dispose()` is a knowing no-op, and slice 8 removes the base anyway, so the Effect order is left as is and its comment points at §22 — cost if wrong: none while `base.dispose()` is a no-op.
19. the missing citation is applied as a two-line comment addendum by the same agent and verified by the controller from the hunks — a review round for two comment lines is cost without information — cost if wrong: none.
20. the 22 `*Live` layers become module-private (drop `export`; the Tags stay exported — tests and the index resolve them); the plan's file map listed them as exports without a consumer outside the file, so knip is right — cost if wrong: a later slice that composes a subset of layers re-exports what it needs.

## Deferred minors (triaged by the final whole-branch review: fix-before-merge items were applied in the fix wave; the rest ship as-is)

- Task 1: minor (deferred): `formatWithCommas` exported from `notionalView.ts` with no consumer outside the file (plan-mandated export; demote if unused after Tasks 4–5).
- Task 2: minor (deferred): `clock.ts` doc sentence "picked up within the same call" makes the second zero-advance read as redundant — reword to "the second turn is for a two-hop chain across schedulers".
- Task 2: minor (deferred): `getRfqQuote` pass-through lacks the explicit return type the brief asked for (contextually typed; no configured rule fires).
- Task 3: minor (deferred): `blotter.ts` cap case drives 45 snapshots in one burst before one `settle()` (loss-free burst delivery is contracted in priceStream's "not calm" case; a comment could point there).
- Task 3: minor (deferred): `portDiscipline` new cases keep the `before > 0` guard outside the `try` (consistent with the pre-existing connection case; a failed guard skips teardown).
- Task 3: minor (deferred): `currencyPairs`/`analytics` assert the port object by reference (`toBe`) — stricter than values; a cloning core would red. Kept: the RxJS test asserts the same and no core clones.
- Task 3: minor (deferred): `execution.ts` failing case does not unsubscribe `c` (already closed by the error).
- Task 4: minor (deferred): `execution.test.ts` "only subscribers present" asserts only the negative half.
- Task 4: minor (deferred): tile too-long sleep is never cancelled on outcome — harmless only while TOO_LONG_THRESHOLD_MS < CONFIRMATION_DISMISS_MS (RxJS reads the scan acc; async reads the live store) — worth a pinning comment.
- Task 4: minor (deferred): `conflatedTopic.ts:48` guard is reachable (timer fires, abort lands in the microtask gap) — the report's "unreachable" note is wrong, the guard is right.
- Task 4: minor (deferred): `priceHistory.test.ts` lacks the per-warm-period port-call assertion its sibling has.
- Task 4: minor (deferred): `composition.dispose.test.ts` uses a `type` alias where the package uses `interface`.
- Task 5: minor (deferred): conflatedFold.test comment credits fromPort ordering, contradicting the source's measured note (the peek seed is the mechanism).
- Task 5: minor (deferred): no witness that the timeout releases the losing rpc subscription (`pendingCount()` after timeout).
- Task 5: minor (deferred): no test where `retain: true` meets a producer failure.
- Task 5: minor (deferred): tileExecution.ts doc overstates cancellation of the too-long child (the terminal guard is what suppresses it).
- Task 5: minor (deferred): composition.machineFactories.test never disposes the machines it builds (a live rowHighlight timer on real timers); three `toBeDefined()` assertions.
- Task 5: minor (deferred): layers.test imports `AppPorts` from client-core rather than core-api.
- Task 5: minor (deferred): the execution PubSub has no lifetime (not attached to host.scope).
