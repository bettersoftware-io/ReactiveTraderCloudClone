# Project Status — Pending Work

> Cross-workstream backlog: what's **not** done yet across the whole repo.
> Finished work is **not** listed here — it's removed as it lands. For history
> see git log; for the clean-architecture phase log see
> [superpowers/STATUS.md](superpowers/STATUS.md).
>
> Speculative, not-yet-planned ideas live in [IDEAS.md](IDEAS.md) (the icebox);
> they move here once they earn a spec or plan. See [README.md](README.md) for
> the full document map.
>
> Maintained via the `tracking-workstream-status` skill. **Last updated: 2026-09-02**

## 🟡 In progress

- **RN mobile-v1 Phase 5a (Credit) — 1st pass BUILT, refinement pass PENDING** — shipped over PRs #451/#454/#455. The first pass is complete and on-device-verified; the user's read is that it is **"getting closer to the prototype but still quite distant"**, so a **second refinement pass is planned and not yet scoped**. Known items for it: **DONE 2026-08-03 — the ACCEPTED stamp became the header pill** (T35), replacing the separate element. The prototype's header slot is exclusive and holds exactly one thing: a countdown ring while live, an animated `kfStamp` state pill once settled (dc.html:237). We currently render a static `Done` in that slot **and** a larger boxed stamp below the quote rows — two elements where the design has one. The deciding argument is not fidelity but **vertical space: it is the scarce resource on a phone**, and the separate stamp spends a row to say what the header slot is already there to say. Nothing in the data model resists the change. Also T19–T21 in [rn-open-items.md](rn-open-items.md). **Task 11 is now HALF closed.** The two goldens `credit/rfq-tiles` and `credit/sell-side` were **captured and committed 2026-08-02** (Tier 1 `simctl`, iPhone 17 / iOS 26.5), each eyeballed in `--scratch` first and then verified: the full 16-scenario pass is green, with both new goldens reproducing at **0.00%**. [DRIFT.md](design/mobile/v1/reference-shots/DRIFT.md) now shows the app half for all 14 paired scenarios, so **the fidelity judgement is finally cheap** — it is a scroll on a phone rather than a laptop, a simulator and a served prototype at once. What the first comparison shows for Credit, to seed the refinement pass: the **sell-side ticket is close** (stepper, SUBMIT BID, countdown and progress rail all land) and deviates in three named ways — the subtitle drops the client name (already recorded as **T19**) and prints a raw `2,000,000` where the design abbreviates `3.0M USD`, and the CTA is flat cyan against the design's cyan→green gradient; the **RFQ tiles deviate further** — dealer rows are `Bank A/B/C` rather than real desk names, prices carry a `$` the design omits, the best row lacks the design's `◂ BEST` marker, and the header still shows `Done` + a separate boxed `ACCEPTED` stamp, which is the stamp→pill change already DECIDED above. **An on-device pass on 2026-08-03 found three shell defects the whole test estate was blind to** — ⟳ Replay Boot did nothing (T34, three stacked causes), the ACCEPTED stamp was still two elements (T35), and the Appearance overlay drew its title under the status bar (T36, visible in a committed golden since Phase 2 and passing every run). All three are fixed and device-verified. That is the argument for the sign-off in one line: none of them were reachable by jest, and one of them was *inside* a golden that passed. **What remains of Task 11 is the on-device sign-off** (T18) — an agent cannot do it alone; it needs a booted simulator and a human, and it is the only witness for the Reanimated worklet class.
- **RN mobile-v1 Phase 5a (Credit)** — **BUILT 2026-08-01**; all ten implementation tasks done (countdown ring over `@rtc/motion-core` geometry, best-quote tint + ACCEPT halo, `AWAITING…` pulse, accept ceremony with a **timer-free** linger encoded as the exit animation's own duration, new-RFQ chip form with the dealer picker dropped, the shared 3-way `useCreditRfqFilterPreference` seam adopted at last, and a sell-side ticket list in the prototype's visual language). **What remains is Task 11: the on-device sign-off, which an agent cannot do alone** — it needs a booted iPhone 17 / iOS 26.5 simulator and a human, and it is the *only* witness for the Reanimated worklet class (`pnpm check:worklet-order` is clean and jest is structurally blind to it — five boot scenes shipped blank for weeks on exactly that blindness). The two visual scenarios `credit/rfq-tiles` and `credit/sell-side` were **captured and committed 2026-08-02** (both reproduce at 0.00%; the whole 16-scenario Tier 1 pass is green). Both pin their data twice over — literal RFQs and quotes (mounting `RfqCard`/`SellSideTicket`, not the seam-reading panels) AND a `pinnedRemainingMs` that overrides the live `useRfqCountdown` clock, the same injected-clock move `BootSceneProps.now` makes for `boot/topo`; without the second pin the ring's seconds readout differs between any two captures, which is why `credit/rfq-tiles-empty` was dropped once already. Four carried-forward fidelity notes (T18–T21) are in [rn-open-items.md](rn-open-items.md) — notably that the sell-side ticket shows **no counterparty**, because `Rfq` carries no client identity in `@rtc/domain` and inventing the prototype's fixture name would have been fabrication. Plan: [superpowers/plans/2026-08-01-rn-mobile-v1-rehaul-phase-5a-credit.md](superpowers/plans/2026-08-01-rn-mobile-v1-rehaul-phase-5a-credit.md)
- **RN mobile-v1 UI rehaul** — total HUD redesign of `@rtc/client-react-native`. **Phase 0 (native foundation) built** (reanimated/skia/gesture-handler/expo-blur/haptics/sensors + babel/jest wiring + `GestureHandlerRootView` + flag-gated `MotionProbe`; zero visual change, verified on the iOS simulator). **Phase 2 (theme/ambient/motion) BUILT + on-device-signed-off + MERGED (PR #228)** — FX-key themes (12 skin×mode), Orbitron wordmark, motion-core tick-flash + countdown-ring, Skia ambient background, rebuilt Appearance sheet (theme cards, segmented mode, ambient + power-saver toggles, replay-boot). **Phase 1 (visual harness) BUILT + MERGED (PR #232)** — harness infra + **simctl capture tier** + 2 on-device goldens (blotter/seeded, shell/connection-banner). **Phase 3 (shell: header/status strip + radial command dock) BUILT + MERGED (PR #285) + ON-DEVICE VALIDATED (iPhone 17)** — replaced the tab navigator with prototype HUD chrome: animated hex-reticle SVG logo (gated rotation loops), Orbitron wordmark + SIM/LIVE env badge + pulsing connection dot + telemetry strip (live FPS via `@rtc/motion-core`, decorative-static LAT/clock), route-derived MODULE label, and a router-backed radial dock (hex FAB w/ active-or-✕ glyph fanning 5 satellites on a 150→30° arc over an `expo-blur` scrim) driving `expo-router` `<Slot/>` nav (deep links preserved). All idle motion gated by reduced-motion ∧ power-saver Freeze; `@rtc/motion-core` wired as the first RN workspace-dep consumer. On-device (iPhone 17) the full shell renders live — header, status strip w/ live FPS, tick-flashing Rates tiles, and the radial dock fanning all 5 satellites over an `expo-blur` scrim — after fixing two jest-invisible blockers (**PR #289**: the worklets Babel `@babel/generator`/`@babel/traverse`@8 leak that crashed the Metro bundle + a `useShellTelemetry` `computeFps`-inside-worklet "Remote Function" crash). **Phase 3 residual (smaller, does NOT gate Phase 4 planning):** pin the `shell` visual golden (chrome + dock-open, with a frozen `ShellTelemetryContext` for deterministic FPS) via simctl/Maestro; spot-check deep links + reduced-motion/Freeze + the FAB/status-strip bottom-inset vs the home indicator. **Remaining follow-ups:** **RN Aurora ambient-style tuning** (folded in from the merged Aurora ambient-style workstream, 2026-07-18) — the Skia curtain approximation (`packages/client-react-native/src/ui/ambient/AmbientBackground.tsx`, `auroraCurtainSpecs`) renders far too faint on-device; raise `t.aurora`/per-band opacities + revisit `AURORA_SWAY_PX`/`AURORA_SKEW`/band geometry, and ideally diagnose via a temporary full-opacity isolated render (no CSS-injection escape hatch on RN like the web has). **Phase 4 split into 4a (Rates) → 4b (Blotter)** (two smaller on-device sign-off gates). **Phase 4a (Rates) BUILT + ON-DEVICE VALIDATED + MERGED (PR #295)** — spot-tile grid rebuilt to prototype fidelity (2-col grid, tick-flash pips via motion-core `nextTickFlash` nonce, `Layout`-transition filter glides, a `@gorhom/bottom-sheet` trade ticket, busy→scan→FILLED ceremony off the real `TileExecutionState` with a once-per-execution haptic), all motion gated by `useShellMotionEnabled()`; data seam unchanged; legacy flat `TileGrid`/`SpotTile`/`TradeTicket` deleted. Live-validated on iPhone 17 / iOS 26.5 against real WS data, which caught 4 defects invisible to jest (filter pills stretching to full-height bars on short content; see-through sheet — `panel` is a translucent token and RN has no `backdrop-filter`, fixed with an `expo-blur` `BlurView`; backdrop neither dimming nor covering the dock — fixed by hoisting `BottomSheetModalProvider` to the shell root, which also resolved the earlier scrim-scope follow-up; stamp detail showing a bare `tradeName`). **Phase 4a remaining:** ~~the `rates` visual golden is not yet pinned~~ — pinned 2026-08-12 (PR #536) and re-pinned FRAMED 2026-08-28 (see the prototype-fidelity entry below); power-saver **Freeze** could not be verified on-device because RN's power-saver control only reaches Calm (pre-existing gap — see the separate "Power-saver Freeze visuals on React Native" entry below) — gating verified at code level only; optional perf follow-up: `makeStyles(theme)` runs per render on the tick-hot `SpotTile` (no React Compiler in the RN package, so a `useMemo(…, [theme])` would help). **Phase 4b (Blotter) BUILT + MERGED (PR #301)** — the module rebuilt to prototype fidelity (`src/ui/blotter/`: pure `blotterFilter` helpers, filter chips + fills summary, 4-column header on shared column ratios, `useRowInsertFlash`, rebuilt `TradeRow` with status pill + `useActivity()`-sourced timestamp, `BlotterModule` with `LinearTransition`/`FadeInDown`/`FadeOut` filter glides; legacy flat `Blotter.tsx`/`TradeRow.tsx` deleted). **4b follow-ups:** (1) ~~recapture of the `blotter/seeded` visual goldens~~ — re-pinned 2026-08-12 (PR #536, over the static fake ViewModel) and again FRAMED 2026-08-28; the on-device sign-off half is folded into the single native session listed under 5b; (2) a `withAlpha()` helper / branded hex type — colours are currently built by string-appending an alpha suffix, which is safe for today's accent tokens but silently invalid for any future `rgba()` one. **Phase 5 (Credit/Equities/Analytics) is DESIGNED and two-thirds BUILT** — 5c (Analytics) and, as of 2026-08-01, 5a (Credit); and **5b (Equities) BUILT 2026-08-10 (PR #518)**, awaiting its on-device sign-off (see its own entry below); **Phase 6 (boot suite + lock screen) jumped the queue ahead of them and split into 6a/6b.** **Phase 6a (boot suite + hold-to-unlock) BUILT + ON-DEVICE VALIDATED (iPhone 17 / iOS 26.5) + MERGED (PR #340):** the shared `@rtc/motion-core` `project3d` yaw/pitch/perspective-divide kernel (consumed by RN now, by Phase 6b's five remaining 3D scenes later); the boot motion gate (`resolveBootMotionEnabled` — Freeze always wins over `forceBootAnimation`/reduced-motion, closing the pre-existing Freeze gap for boot); gyroscope drift (`useGyroDrift`, a leaky self-centring integrator standing in for the web's cursor seam); `BootCanvas` (the Skia host mounting behind the boot chrome, motion-gated, scene-lookup by variant); the `core` scene (imperative `createPicture` — projected globe mesh, meridian/parallel sweep, hub ping ripples, status banner — 5 of the web's 12 elements; 7 deferred to 6b: star-drift backdrop, nucleus glow, latitude scan ring, the two counter-rotating gyroscopic segmented rings, the rotating spotlight callout, order-flow arcs, and the screen-space calibration ticks + corner telemetry readout — plus the whole-frame "holo flicker" wash, deferred as a unit since it modulates every layer); the `laser` scene (declarative JSX — the panel table's trace-in window only; per-kind panel content, completion corner-ticks, post-trace flash, the laser draw-head, and background grid+wash all deferred to 6b); `BootSequence` wired to mount `BootCanvas` behind the existing chrome; and the hold-to-unlock progress ring (`Gesture.LongPress` → `SharedValue` → SVG `strokeDashoffset`, replacing the tap-only AUTHENTICATE button, tap kept as an instant-submit fallback). Four visual goldens captured on iPhone 17 / iOS 26.5 (PR #340) — `boot/core`, `boot/laser`, `boot/static`, `lock/hold` — each pinned to a fixed `elapsedSec`/`progress` (a live frame callback would race the screenshot). **Phase 6a residuals ALL CLOSED (PR #340), each on-device-verified:** (1) boot-variant cycling — RN cold launch always booted `core` because `AsyncStoragePreferencesAdapter` self-hydrated async and lost the race with `BootPreferencePresenter.current()`'s synchronous read; fixed by pre-hydrating preferences before mount (static `hydrate()` + inject via `buildNativePorts` + gate `_layout`, mirroring `AsyncStorageSessionStore`) — cold launch now shows the persisted variant (`SEQUENCE · LASER`); (2) splash visibility — the ported `core` globe read as flickering arcs, so the mesh alpha band was raised `[0.1,0.5]→[0.28,0.78]` + stroke `1→1.5px` (compensating for 6b's deferred glow); (3) the lock ring (see below). **Cross-scene note for 6b (CORRECTED 2026-07-26):** this entry used to record a "regular weight only" cosmetic gap, on the belief that `Skia.Font()` resolves a platform default typeface. It does not — a font with no typeface draws ZERO glyphs on real iOS, silently, so the boot text was not weak, it was absent. Fixed: `bootSceneFonts.ts` builds every site from bundled JetBrains Mono (the web boot canvas's own stack), regular and real 700, in React-land rather than inside the draw worklet. Two things for `layers`/`topo` to inherit: fonts are built once and captured, never per frame; and any NEW symbol in a drawn string needs its codepoint checked against the bundled cmap, because the web renders glyphs a single Skia typeface cannot (U+25C9 `◉` was a tofu box — see `bootGlyphs.ts`). **Lock-ring on-device (PR #340):** the real blocker was a worklet remote-function crash — `HoldToUnlockRing`'s `useAnimatedProps` called `@rtc/motion-core`'s `ringDashOffset`, which lacked the `"worklet"` directive, so the ring redboxed the moment `LockScreen` mounted on ANY real device (jest-blind; the simctl golden capture was the witness). Fixed by marking the ring math `"worklet"`. The harmless (same-password) hold/tap double-fire is documented and the a11y-preserving `Pressable`+`GestureDetector` dual mechanism kept; `Gesture.Race(LongPress, Tap)` stays a future option only if a real double-fire is ever observed. **Phase 6b-1 BUILT + ON-DEVICE VALIDATED + MERGED (PR #353)** — `core` taken from 5 of its 12 web elements to all 12 (star-drift backdrop, nucleus glow, latitude scan ring, the two counter-rotating gyroscopic rings, rotating spotlight callout, order-flow arcs, screen-space calibration ticks + corner telemetry, plus the whole-frame holo flicker threaded as a per-draw-call alpha rather than a `saveLayer`, which would allocate an offscreen surface every frame); `laser` taken from border-trace-only to the full web draw (background grid + wash, per-kind panel content, post-trace flash, completion corner-ticks, draw-head emitter beam); and **`docking` shipped complete and registered** — the last boot scene needing no `project3d` kernel, so 3 of 8 variants now resolve. Order-flow arcs were reformulated from the web's stateful spawner into a closed-form schedule reproducing its exact `hashRandom` sequence, because mutable state survives neither a worklet recorder nor a pinned golden. A device-crash bug was caught pre-merge: `craftGridLines` (a build-once helper, deliberately not worklet-marked) was called per frame from inside a worklet — green in jest, red-box on hardware; fixed by precomputing the unit grid and scaling at draw time. All 8 visual goldens re-captured on iPhone 17 / iOS 26.5 and reproducing at **0.01–0.05%** (previously-"passing" scenarios sat at 1.71–3.02%, so the harness fix removed capture noise as well as false failures). **Phase 6b-2a BUILT (2026-07-26):** `hologram` and `layers` are ported and registered, so **5 of 8 boot variants now resolve** (`core`, `laser`, `docking`, `hologram`, `layers`), on a new shared `boot3dCamera` seam. Plan: [superpowers/plans/2026-07-26-rn-mobile-v1-rehaul-phase-6b-2a-boot-3d-foundation.md](superpowers/plans/2026-07-26-rn-mobile-v1-rehaul-phase-6b-2a-boot-3d-foundation.md). The port corrected a scope-note error worth carrying forward: **not all five projected scenes clamp the near plane** — `hologram` uses `perspectiveK` 0.26 with NO clamp, `layers` 0.24 WITH a 0.4 clamp, both read from their sources. Defaulting the clamp on would have diverged from the web at depth invisibly to unit tests; the per-scene table is recorded in [rn-open-items.md](rn-open-items.md) so 6b-2b does not re-derive it. **Phase 6b-2b BUILT (2026-07-26):** `geo`, `jarvis` and `topo` are ported and registered — **all 8 boot variants now resolve**, so phase 6b is complete on the porting side. Plan: [superpowers/plans/2026-07-26-rn-mobile-v1-rehaul-phase-6b-2b-boot-3d-remaining.md](superpowers/plans/2026-07-26-rn-mobile-v1-rehaul-phase-6b-2b-boot-3d-remaining.md). **The recurring hazard across this phase was per-frame mutable state**: four of five projected scenes accumulate something in the web (`geo`'s trade array, `jarvis`'s `ringZPlane` and `fragment.currentZ`, `topo`'s per-peak tick state) that a worklet cannot carry, because it captures values rather than sharing a live closure. All are now derived from `elapsedSec`; the failure mode is silent divergence, never a crash. **All eight variants now RENDER ON DEVICE (verified 2026-07-31, iPhone 17 / iOS 26.5).** Running them was the whole point: five of the eight painted a blank white screen from the day they merged, and the fix (T13) needed **three** distinct causes — 24 worklet→worklet forward references, a worklet reading a module-level `const` declared later (`LayersScene`'s `CORNER_UVS`, the one that kept `layers` blank through a complete function reorder), and a missing `"worklet"` directive on `topoGeometry.topoHeightAt`. `pnpm check:worklet-order` now gates all three classes in CI. **The golden set is now COMPLETE and CLEAN (2026-08-01):** all **14** simctl goldens captured, eyeballed and verified, **13 of 14 reproducing at exactly 0.00%** (`boot/static` 0.14%, tracked as T16). That session closed T14, T12, T10 and T8, and found four defects no diff could ever have surfaced — because a diff against an equally-wrong baseline is green. T14's recorded scope was too narrow: it was not a `boot/*` problem but **every leaf-mounted fixture**, so `lock/hold`, `boot/static` and `shell/connection-banner` were near-white too. Worse, three scenarios mounted screen content with no safe-area inset, so `blotter/seeded`'s golden asserted a blotter **missing its `PENDING` filter chip entirely**, and `lock/hold`'s ring sat almost wholly behind the dynamic island while its whole purpose was to prove a partial arc (T17). The iOS status bar — in frame, clock ticking — turned out to be T8's unexplained 2.9–4.0% drift and is now pinned by the driver. `boot/topo` is registered at last (T10): `BootSceneProps` gained an optional `now` the harness pins and production omits. **Phase 5c (Analytics) BUILT 2026-07-27, CLOSED 2026-08-04** — PRs #396/#397/#403/#406. Seven of its eight tasks: **M6 fixed** (`AnalyticsSimulator` now drifts positions on a mean-reverting step instead of re-emitting one frozen constant — below both clients, so the web dashboard gains live positions too); P&L formatters corrected; the P&L chart rebuilt on declarative Skia with a smoothed line, area gradient and dashed zero baseline; the pair bars re-cut from an animated `flex` (a **layout** animation, banned by `docs/performance.md`) to a fixed track with a `scaleX` transform, anchored so bars grow *out of* the centre line rather than around their own midpoint; the exposure bubbles ported to Skia with the prototype's three-layer treatment (10% fill, 22% radial glow, hairline ring), an accent-coloured currency label, the previously-absent amount label, and a 900 ms breathing tween; card order corrected to the prototype's P&L → Pair P&L → Exposure; and an `analytics/dashboard` visual scenario registered. Two patterns worth reusing: **bubble motion tweens position AND radius together** (radius is a scale over the whole book, so a drift re-packs the cluster — gliding radius alone while positions snapped would read as broken), and **Skia elements accept no `testID`**, so every draw decision was pushed into pure `buildBubbleDrawModel`/`buildChart` modules and asserted there. Analytics was previously excluded from the visual harness outright as non-deterministic; it qualifies now only because `AnalyticsDashboard` was split out to take its data as a prop (fed a literal book) **and** the scenario seeds power-saver `freeze` — `forceReduceMotion` gates the ambient layer alone and does not stop widget tweens. **Task 8's mechanical half is DONE (2026-08-01)**: the `analytics/dashboard` golden is captured and reproduces at 0.00%, and the screen was driven live on device against real WS data — no worklet red box, Skia bubble labels render (no tofu), and the M6 drift is real motion, not theatre (64,758 px changed over 12 s untouched; JPY moved +8.3M → +8.7M). **5c is now CLOSED (2026-08-04).** Step 5 — the fidelity judgement the plan reserves for a human ("this task cannot be completed by an agent alone") — ran on 2026-08-04, against [`reference-shots/analytics/dashboard.png`](design/mobile/v1/reference-shots/analytics/dashboard.png) with every candidate confirmed in source. It found **seven deviations, recorded as T37-T43** in [rn-open-items.md](rn-open-items.md), of which one is structural: **the Exposure bubbles were ported from the WEB v2 prototype rather than mobile-v1** — fill, glow, ring opacity and all three label sizes trace exactly to `packages/client-prototype/src/fx/Positions/ExposureBubbles.module.css`, while the mobile design has no glow, a full-opacity ring and labels ~30% smaller. The others: all three card titles on the wrong font/size/tracking with both unit qualifiers dropped, the `Δ … / 12S` delta chip absent, the pair bars missing their recessed track, bar geometry and opacity off, `EURUSD` for `EUR/USD`, and the chart's smoothing (deliberate, now explicitly accepted). **All seven survived a golden reproducing at 0.00%**, because a golden compares the app to itself — only the side-by-side finds this class. They are tracked work alongside the 5a refinement pass, not blockers, so the phase closes with its gaps written down, which is what step 5 asks for. Two process notes worth carrying: `rn-open-items.md` §6 had claimed step 5 was signed off on 2026-08-01 while this page said it was outstanding — **this page was right**, and what settled it was asking which artifact would exist if the step had run (step 5 writes gaps into that file; there were none). And the plan file's checkboxes are **not** evidence: all seven sit unticked including the ones known to have run. **Known follow-up:** the CI "Expo bundle smoke" tolerates a react-native-worklets Babel-plugin crash — the ROOT CAUSE was fixed in **PR #289** (the `@babel/generator`/`@babel/traverse`@8 leak into the worklets plugin, which the earlier `@babel/core`-only override missed), so this tolerance in `.github/workflows/ci.yml` can now likely be REMOVED — verify `expo export` is clean on CI x86 first. **Every open RN wrinkle, mismatch and follow-up is now consolidated in [rn-open-items.md](rn-open-items.md)** — read that first. **CLOSE-OUT SWEEP 2026-08-05: the open count went 31 → 18, and 6 of those 18 are the M-series (domain/wire, shared with the web client, frozen by Phase 5 §4) — so RN-specific open work is 12 items, none blocking.** Seven were fixed (T16 the boot-emblem pulse now honouring Freeze, T28 the sell-side a11y marker, T31 the reanimated jest mock, and T38/T40/T41/T42 of the Analytics fidelity set); **six were reclassified rather than fixed** — P2, P3, T19, T20, T21 and T43 were already-accepted decisions still displayed as open work, which is how a findings list turns into noise nobody can act on; and two were stale (T18's goldens already existed, T6 needs scenarios *authored* rather than captured). **The whole 16-scenario golden set was re-captured** against the fixed UI and verified at **0.00%**, bar `blotter/seeded` at 0.06% (a sub-pixel layout settle — content identical; treat that as its noise floor). The set's job is now bulk fidelity comparison against [`reference-shots/`](design/mobile/v1/reference-shots/DRIFT.md), not gatekeeping: **the human PNG-promotion gate is dropped** as costing more than it caught. **(P1) Skia boot text rendered nothing — FIXED 2026-07-26**, verified on device, with `boot/core` and `boot/docking` re-pinned; the same session also found that U+25C9, the telemetry bullet every boot variant uses, is in no bundled face (P1a) and that every scenario after the first in a capture run was driving a dead app (T7). **(T1) the RN visual/e2e tiers gate nothing in CI** because every other workflow is `ubuntu-latest` — but this repo is PUBLIC, so GitHub-hosted macOS runners are FREE, and that is no longer an inference: a `workflow_dispatch` spike proved on 2026-07-26 (6 min 27 s, green) that `macos-26` boots **iPhone 17 × iOS 26.5**, exactly the goldens' pin. Two assumptions died with it — **97 GB free, not the documented 14 GB**, and `python3` is **3.14.6**, above `fb-idb`'s ≤ 3.13 ceiling, so idb really would not have installed and Maestro is the correct CI tier. What remains is pixel parity (expect a second golden bucket, never a wider tolerance) and promotion to a scheduled, non-gating workflow — never a PR gate. Spec: [superpowers/specs/2026-07-16-rn-mobile-v1-rehaul-design.md](superpowers/specs/2026-07-16-rn-mobile-v1-rehaul-design.md); plans: [Phase 0](superpowers/plans/2026-07-16-rn-mobile-v1-rehaul-phase-0-native-foundation.md), [Phase 1](superpowers/plans/2026-07-17-rn-mobile-v1-rehaul-phase-1-visual-harness.md), [Phase 2](superpowers/plans/2026-07-17-rn-mobile-v1-rehaul-phase-2-theme-ambient-motion.md), [Phase 3](superpowers/plans/2026-07-19-rn-mobile-v1-rehaul-phase-3-shell-dock.md), [Phase 4a](superpowers/plans/2026-07-19-rn-mobile-v1-rehaul-phase-4a-rates.md), [Phase 4b](superpowers/plans/2026-07-20-rn-mobile-v1-rehaul-phase-4b-blotter.md), [Phase 6a](superpowers/plans/2026-07-20-rn-mobile-v1-rehaul-phase-6a-boot.md)
- **RN mobile-v1 Phase 5b (Equities) — BUILT, on-device sign-off PENDING** — shipped in PR #518 (plan: [superpowers/plans/2026-08-09-rn-mobile-v1-phase-5b-equities.md](superpowers/plans/2026-08-09-rn-mobile-v1-phase-5b-equities.md), #517). All ten tasks built and reviewed: movers board (RANK BY chips, ranked rows, candle-close sparklines, rank-move glide on motion-core's 560/820ms constants), tick-flashing instrument header, **Skia candle chart replacing the `react-native-svg` `PriceChart`**, order ceremony over all six `OrderTicketState` phases with compiler-enforced exhaustiveness and no timers, blotter status pills. `Watchlist`, `PriceChart` and `buildCandles` retired. **The golden half is DONE — T46 CLOSED 2026-08-12.** All three `equities/*` goldens are captured and committed, and `markets`/`trade` both reproduce at **0.0000%** across three captures. The determinism risk this entry used to carry was resolved *at the seam*, not by the planned `pricingPinMs`-style pin: `VisualScenarioHost` no longer composes simulator ports at all, it renders a static fake `ViewModel` (`tests/visual/buildFakeViewModel.ts`), so there is no live `interval(500ms)` walk left to pin. Measured over three captures per side, 19/21 scenarios reproduced exactly before and 20/21 after; the single holdout is a new finding tracked as **T47** (`blotter/seeded`, not an equities scenario). **What remains is a native session, not code — the behavioural punch list only:** verify a falling row pulses **red** (it shipped inverted once, every falling row flashed green), watch Reanimated's `LinearTransition`/`FadeIn`/`FadeOut` against a key-driven re-sort at 60fps (jest is structurally blind to the worklet class), and check the order ticket's resting state. That is **T44** in [rn-open-items.md](rn-open-items.md), blocked on the same native dev-client build as P8 — do both in ONE session. No CI exposure today: RN golden capture is manual and nothing asserts a golden exists per registered id. Deferred follow-ups: promote the two deliberately-ported helpers (`sortMovers`, `useNewestOrderId`) out of the client once a shared home is agreed, and move `directionFor` beside `computeRankDirections` in `@rtc/motion-core`.

- **Jarvis AI assistant** — **P4 (the `/mcp` MCP endpoint) SHIPPED 2026-08-01 (PR #453)**, on top of **P3 (`@rtc/agent-tools` + the real Anthropic tool-runner loop) SHIPPED 2026-08-01 (PR #444)**, **P2 (`JARVIS_*` WS wire + server `ScriptedAgentLoop`), PR #440 (2026-07-31)** and **P1 (scripted core surface), PR #405 (2026-07-27)**. P3 shipped the nineteenth package `@rtc/agent-tools` (seven SDK-free desk tools — `list_currency_pairs` / `get_price` / `get_price_history` / `get_blotter` / `get_analytics` / `get_service_health` / confirm-gated `execute_trade` — JSON Schema + `run(input): Promise<string>` over injected domain ports, prices as pair-precision **strings**, every failure a descriptive string rather than a rejected promise; `agent-tools-stays-inner` pins it to domain+rxjs), `AnthropicAgentLoop`/`AnthropicAgentSession` behind the existing `AgentLoop` seam (`claude-opus-5` pinned, streaming SDK tool runner, `effort: "medium"`, prompt-cached persona+tools prefix, sanitized refusal/SDK-error surfaces, honest `max_tokens`/iteration-cap truncation notices), **one `AgentSession` per socket** (the effect body itself is the per-connection factory — allocation-only, disposed by the stream's `finalize`, so a session dies with its socket), cost hygiene (four caps: 4,096 max tokens/turn, `medium` effort, 40 turns/session, 30 replayed history messages — plus an 8-iteration per-turn runner ceiling), and `@anthropic-ai/sdk` confined to `packages/server/src/agent/` by an **allowlist-inversion** dep-cruiser rule (`no-anthropic-sdk-in-inner-packages`; the first draft was a blocklist that silently left the browser clients uncovered). **All three P2 accepted limitations are now CLOSED — the old "do NOT fix it at the adapter layer" instruction is spent and has been removed:** (1) turn correlation — every turn-scoped frame carries a client-minted `turnId`, the adapter filters on it, and the straggler/orphan case is a silent drop; (2) cancel — `JARVIS_CANCEL {turnId}` fires on **every** teardown path (early unsubscribe, offline timeout, normal completion), with an effect-layer `inFlightTurnId` gate so a *stale* cancel can't kill the next turn; (3) availability — `JARVIS_SUBSCRIBE`/`JARVIS_AVAILABILITY` (the responder registers even with no loop), re-queried per `gatewayConnected` so a reconnect re-asks, folded into `JarvisMachine.available`, which hides the orb, disarms the ⌘/Ctrl+J hotkey and makes `send()` a silent no-op. Session history also landed (client replays up to 20 entries; server caps at 20 on the wire and 30 in-session). Env precedence is **`RTC_JARVIS_FAKE=1` wins over `ANTHROPIC_API_KEY`** — a deliberate rehearsal override so the offline demo is one env var away without unsetting a key; `ScriptedAgentLoop` therefore now serves three jobs (CI path, offline demo, deliberate fallback). **The receipt is architecture [§18.13](architecture/18-jarvis-ai-agent-surface.md#1813-phase-3-shipped--the-real-loop)** (package shape, the per-socket session seam, cost hygiene, availability end-to-end, the turnId/cancel closure, the five review-only defects, the ownership-guard doctrine, the live-smoke witness) — **source material for the presentation**, alongside [§18.11 (P1)](architecture/18-jarvis-ai-agent-surface.md#1811-phase-1-shipped--the-receipt) and [§18.12 (P2)](architecture/18-jarvis-ai-agent-surface.md#1812-phase-2-shipped--the-wire). **Open P3 items and accepted decisions, logged deliberately:** (a) **`get_app_context` DEFERRED to P4** — the eighth tool from §18.3 depends on a client→server app-context channel the chat payload doesn't carry and the UI doesn't yet produce; shipping it against an unpopulated field would be a tool that lies (`appContext` stays off `JARVIS_CHAT` for the same reason). (b) **The deployed Fly server is a user-approval decision, not yet taken** — `fly.toml` sets neither `RTC_JARVIS_FAKE` nor `ANTHROPIC_API_KEY` (secrets aren't in the repo, so the live state can't be verified from here); it can stay on `RTC_JARVIS_FAKE=1` (free, deterministic, still demos the whole surface) **or** gain an `ANTHROPIC_API_KEY` secret for the real loop — the latter is metered spend on a publicly reachable demo, so it needs an explicit go, plus a thought about who can burn tokens through it. With neither set the deployed server answers `available:false` and the orb correctly hides itself. (c) **`crypto.randomUUID()` is now on the per-turn hot path** in `WsJarvisAdapter.ask()` (it was already used for confirmation ids in the shared engine): it is `undefined` in **non-secure contexts**, so a plain-`http://` LAN dev origin against `dev:ws` would throw per send, and Hermes/React Native availability is still unverified — check before any RN Jarvis surface or non-localhost http demo. (d) **Errored-turn text is fed back to the model as its own prior utterance** — an errored turn appends nothing to the *session's* history, but the client's offline/error copy ("Jarvis is offline, sir…", "The desk link faltered, sir…") lives in `JarvisMachine.entries` and so is replayed inside wire `history` on a later fresh session; accepted (it reads as a truthful transcript and the alternative is filtering by message text), recorded so it isn't rediscovered as a bug. (e) **Multi-orphan reconnect burns tokens** (minor, no correctness impact) — only the first orphaned turn's cancel lands, since later `jarvis.chat`/cancel frames queue behind the effect's `concatMap`; the remaining orphaned Anthropic turns run to completion server-side. (f) Smaller deferred minors: `toolEvent "done"` fires at `content_block_stop` (call *emitted*, not tool *completed*), so the "trade" chip reads done while the confirm card is still open — confirm the UI reading before P4; the truncation notice is folded into `finalText` and thus replayed as the model's own words on later turns; tool `symbol` inputs aren't normalized (`.trim().toUpperCase()`) before exact-match lookup; `get_price`/`get_price_history`/`execute_trade` each re-snapshot the full pairs list to validate one symbol (three refdata round-trips where one would do); `AgentSession.dispose()` completes `cancel$` so a post-dispose `runTurn` would be uncancellable (unreachable today; a `disposed` flag would harden the seam); `ownedConfirmationIds` is never pruned on cancel (bounded); `@rtc/agent-tools` has no README (the only package without one); and the persona's voice tension (wry asides vs. a terse 2–4 sentence cap) wants observation under live traffic. **Two items from this list were promoted and CLOSED in the P3 final-review fix round:** `console.warn` no longer logs raw malformed-frame payloads (an authenticated log-flood lever) — the three server-side sites (`jarvis.chat`/`jarvis.confirm`/`jarvis.cancel`, `jarvis.effects.ts`) now log only the frame type + best-effort `turnId` + a "payload omitted" marker; and a `TestScheduler` test now pins "availability flips false MID-turn → the in-flight turn still completes" (`JarvisMachine.test.ts`), guarding against the tempting `takeWhile` refactor that would silently tear a live turn down. (g) **No negative-path fullstack witness for availability** — `tests/fullstack/browser/fullstack.spec.ts`'s orb-visibility assertion does NOT prove the subscribe→availability round-trip (`JarvisMachine.INITIAL.available` is `true`, so the orb renders regardless of whether the handshake ever completes); the real wire-drives-UI witness would be a fullstack case that boots the server with `loop === null` (no `RTC_JARVIS_FAKE`, no `ANTHROPIC_API_KEY`) and asserts the orb actually **hides** within the ~15s `JARVIS_AVAILABILITY_TIMEOUT_MS`-ish window — not yet written. (h) **The ask-path first-event deadline was too tight for the real loop** — the original 10s `JARVIS_FIRST_EVENT_TIMEOUT_MS` predated the Anthropic loop and was tuned against the scripted stand-in, which never thinks; adaptive thinking can legitimately exceed 10s before the first visible event, which would fabricate a client-side offline error AND cancel an already-billed turn. FIXED in the final-review round: the constant split into `JARVIS_TURN_FIRST_EVENT_TIMEOUT_MS` (30s, `ask()`'s deadline) and `JARVIS_AVAILABILITY_TIMEOUT_MS` (10s, unchanged, `availability$()`'s deadline — a same-process non-LLM lookup with no thinking phase to wait out). `scripts/jarvis-live-smoke.ts` now prints per-turn time-to-first-event so a live run produces the real latency datum. **The only surface that touches a real key is `scripts/jarvis-live-smoke.ts` (`pnpm jarvis:smoke:live`)** — manual, key-gated, never run by CI (every turn is a real metered call); it boots the real server, logs in, and drives raw frames through availability, a quote turn, a declined-confirmation trade turn, and a **fresh-socket** history replay that is the only witness for the assistant-first-history 400 guard (the session consults wire `history` only on its first turn, so reusing the earlier socket made the check pass even with the guard deleted). **P4 (the `/mcp` MCP endpoint) SHIPPED 2026-08-01 (PR #453)** — `packages/server/src/mcp/` (`buildJarvisMcpServer`: the SDK-free registry over the SDK's **low-level** MCP `Server`, raw JSON Schema passed through verbatim rather than round-tripped through the Zod-first `McpServer`; `createMcpRequestHandler`: Bearer auth over the existing `AuthService` session tokens → stateless Streamable HTTP, a fresh `Server`+transport pair per POST, no session table), mounted at `/mcp` on the same `node:http` server `index.ts` already runs — unconditionally, not behind the orb's availability gate, since MCP brings its own model and needs no `ANTHROPIC_API_KEY` on this server at all (auth stays mandatory regardless). The tools close over the same process-wide `ServiceContainer` the WS effects use, so an `execute_trade` from Claude Code lands in the running app's real blotter — pinned by a same-process test that trades through MCP, then reads the blotter back through the same services. `execute_trade` is ungated at this layer (`approveWithoutPrompt`, built at the composition root with a doc comment naming the decision): the external client's own tool-approval surface (Claude Desktop/Code asking their user before every write tool) is the architecturally honest HITL layer for this transport, matching parent §3.4. Unknown-tool calls return `ErrorCode.InvalidParams` rather than the plan's literal `MethodNotFound`, matching the SDK's own `McpServer` — `MethodNotFound` would tell a strict client that `tools/call` itself is unimplemented. No input validation happens at this boundary (registry convention, not oversight): `@rtc/agent-tools` already reports a bad argument as an ordinary success string, the same contract the Anthropic loop's tool runner sees, so MCP is not silently laxer than WS. The verified `{username}` from `verifyToken` is checked then discarded — no tool sees who called it; that lands with the still-un-spec'd Phase 2 per-user isolation, on both transports together, not with this phase alone. RFC compliance details: the Bearer scheme is matched case-insensitively (RFC 9110 §11.1), a non-POST method gets 405 with `Allow: POST` (§15.5.6), and every 401 body is generic and never echoes the presented token. **Pre-existing, widened by this phase, not introduced by it:** `AUTH_SECRET` already defaulted to `""` under a bare unconfigured boot (`index.ts`); `/mcp` raises what a forged token under that default can do, from reading the data feed to calling every desk tool including `execute_trade` — Fly and the `dev:*` scripts always set a real secret. **The receipt is architecture [§18.14](architecture/18-jarvis-ai-agent-surface.md#1814-p4--the-mcp-endpoint-second-transport)** (shape, same-process proof, auth, the demo recipe, all recorded deviations from parent §3.4, the token-TTL caveat). **`get_app_context` does NOT ride along after all** (revising the P3-era note): it is a WS-chat-surface tool — an external MCP client has no app attached, so app context is meaningless over MCP — and bundling the client→server app-context wire change into a transport phase would couple two unrelated deliverables; it stays deferred to the phase that builds the app-context channel. **Both P3-era pending user actions CLOSED 2026-08-02:** (b) the deployed Fly server now carries a real `ANTHROPIC_API_KEY` (imported from the local Keychain via stdin; no `RTC_JARVIS_FAKE` set), verified end-to-end by a wire probe against the deployed server (`jarvis.subscribe` → `{available:true}`) — billing gated Anthropic-side per the interim PAYG decision in the ⚪ governance entry; and the live-smoke witness ran for the FIRST time with a real key (2026-08-02): all five checks PASS, and the real latency datum is in — **time-to-first-event 1.6s / 2.8s / 2.4s** across the three turns, comfortably inside the 30s `JARVIS_TURN_FIRST_EVENT_TIMEOUT_MS` (and even the old 10s deadline it replaced). `pnpm jarvis:smoke:live` remains manual and key-gated, never run by CI. The operational runbook for both (key handling without history leaks, Fly secrets, deploy pairing, bill-safety knobs) is [running-real-jarvis.md](running-real-jarvis.md). **The per-user brain picker + usage-metering round (usage governance items (1)/(3)/(4), a separate ⚪ entry below) shipped 2026-08-02 — receipt at [architecture §18.15](architecture/18-jarvis-ai-agent-surface.md#1815-the-brain-picker--usage-display-round--the-receipt)**, superseding this entry's `claude-opus-5` pin with a per-turn, user-selected brain defaulting to Haiku. **Deploy pairing DONE 2026-08-07** — the post-P5 `deploy.yml` dispatch (+ paired Vercel client deploy) succeeded, so the deployed site now runs the current images: the Haiku default, the Preferences brain picker, the footer chip, the Admin usage card, live panels, app-driving, and the narrator. The full real-key live smoke (6 turns, incl. panel-authoring and drive) passed against local source 2026-08-08. **RE-PAIRED 2026-08-13 — the deployment is CURRENT, and this entry claimed the opposite for a day.** Run [31713096984](https://github.com/bettersoftware-io/ReactiveTraderCloudClone/actions/runs/31713096984) dispatched `deploy.yml` with all three targets ticked — server → Fly, `client-react` and `client-solid` → Vercel — all four jobs green, on `d1b15899`, which `git merge-base --is-ancestor` confirms **contains** the #505 governance-R2 merge (`073021fb6`). So the live server's real key now runs **with** the in-app budget ceiling, and the paired clients carry the gated picker / footer chip / Admin budget UI. The Console-side prepaid backstop is no longer the only spend gate. **The stale-claim trap to avoid next time:** the superseded text was written the day #505 merged and never revisited, so for four days this page advertised an urgent open user action that a later dispatch had already closed. `gh run list --workflow=deploy.yml` alone cannot tell you — a run that deployed only *one* client renders identically to a full three-target run. Read the **job list** (`gh run view <id> --json jobs`; skipped jobs are the tell) and check the deployed SHA against the commit you care about with `git merge-base --is-ancestor`, before either trusting or repeating a staleness claim here. **Follow-up candidate from the round's final review (not built):** echo the server's RESOLVED brain on `jarvis.done` — the wire currently carries no served-brain echo, so `jarvis-live-smoke`'s brain assertion is structurally weak and the footer chip trusts the client-side resolution rather than what actually served the turn; one wire field closes both. → **P5+** scripted breadth + roadmap (sentinels, desk control, generative widgets…) per the parent spec's §10. Jarvis voice out of scope indefinitely; Jarvis open/close SFX arrive with the separate desk-SFX workstream (see ⚪). Parent spec (§10 roadmap, still authoritative for P5+): [superpowers/specs/2026-07-12-jarvis-ai-assistant-design.md](superpowers/specs/2026-07-12-jarvis-ai-assistant-design.md); P1 spec: [superpowers/specs/2026-07-26-jarvis-phase-1-scripted-surface-design.md](superpowers/specs/2026-07-26-jarvis-phase-1-scripted-surface-design.md); plans: [P1](superpowers/plans/2026-07-26-jarvis-phase-1-scripted-surface.md), [P2](superpowers/plans/2026-07-31-jarvis-phase-2-ws-wire.md), [P3](superpowers/plans/2026-08-01-jarvis-phase-3-anthropic-loop.md), [P4](superpowers/plans/2026-08-01-jarvis-phase-4-mcp-endpoint.md)

- **Layout-management port — Dockview engine SHIPPED (PR #534)** — the workspace engine is now swappable at runtime via a new `LayoutEngine` preference (`"inhouse" | "dockview"`, default in-house; both clients' Preferences modal gained a "Layout engine" row). New leaf package `@rtc/layout-dockview` wraps `dockview@7.0.4` (the supported vanilla entry point, not the internal `dockview-core`) behind `createDockEngine`: seed-tree → `SerializedDockview` conversion (`api.layout()` called before `restore()` so proportions hold), opaque-blob persistence through a new `DockLayoutStore` port (localStorage-backed per client, keyed `rtc-dock-layout-<tab>`, debounced serialisation + a dispose-time final flush), a maximize bridge mirrored from Jarvis/the layout state machine, and a close-button-free tab renderer (no close affordance in v1 scope). Content/placement stays separated exactly as ADR-002 designed: panel content is still addressed by the existing `appPanelRegistry`/`appHeadRegistry`, Dockview owns placement/chrome only, via a per-client `DockviewLayoutEngine.tsx` bridge that portal-mounts registry content so the `ViewModel`/`FxView`/`CreditView` context keeps flowing. Two dependency-cruiser rules confine `dockview` to the new package (`layout-dockview-stays-pure`, `dockview-only-in-layout-dockview`) — swappable by replacing one package, per the ADR. Verified by a shared `DockviewEngine.contract.spec.ts` (10 cases, both clients), 38 package-level unit tests, a Playwright e2e journey (switch engine → drag-dock → reload persists → revert), and a `shell/layout-dockview` visual scenario (10-combo matrix) + re-pinned prefs-modal goldens. **ADR-002 carries an "As implemented (2026-08)" section** for this round (`docs/adr/ADR-002-layout-management-port.md`); its overall status is **Superseded in part**, set by the concurrent GenUI L3 round below — the two rounds merged together, so the ADR now records the in-house split-tree engine as the system of record and Dockview as a switchable second engine behind the preference, default in-house. **What remains pending:** the custom free-floating adapter (magnetic auto-docking, masonry reflow) is still unbuilt and unplanned; the thin `LayoutPort` interface the ADR sketched was deliberately **not** extracted — the engine branch lives inside the existing `WorkspaceEngine`, not behind a port each engine implements symmetrically, deferred until a *third* engine exists to generalise the contract from (the two shipped engines are deliberately asymmetric — six in-house capabilities Dockview does not have — so a port over just those two would either leak the in-house vocabulary or collapse back into the two-branch conditional `WorkspaceEngine` already is). **Jarvis's collapse intent is WIRED as of 2026-08-14** — the entry previously read "collapse/resize intents no-op under dockview", which was half wrong in each direction. **Resize was never a gap:** the LayoutMachine's `resize` is the in-house sash-drag callback, and under Dockview the user drags *Dockview's own* sashes, which fire `onDidLayoutChange` and persist through the existing blob — so resize already worked natively and had nothing to map. **Collapse genuinely was one,** and dockview's dock has no collapse primitive to map onto: `setCollapsed`/`isCollapsed` exist in dockview-core but only for EDGE groups (shell-docked sidebars), which these grid groups are not. So `createDockEngine` emulates it the same way dockview's own edge groups do — remember the group's pre-collapse width + constraints, clamp both to the 38px strip, restore exactly on expand. One semantic mismatch had to be resolved explicitly: in-house `collapsed` names a **panel**, dockview sizes a **group**, and a group can hold several panels as tabs — so a panel sharing a group is **ejected into its own group first**, keeping collapse meaning exactly the panel it names rather than stripping its tab siblings. **Chrome parity landed 2026-08-28 (PR #587)** — the first cut only mapped colour tokens, so the Dockview engine read as a different design (no inset/gutters, flat groups, Dockview's own 28px tab bar stacked above the app head strip, no collapse/maximize controls, the 360px rail as a 27% fraction, no body scroll container); now the tab bar *is* the in-house header (each client portals its shared `PanelHead` pieces into `mountTab`/`mountActions` hooks), groups are the in-house card with the 7px gap + 10px inset, the seed honours `initialPx`/`fixedPx` gap-compensated, and collapse strips along the group's reclaim axis with the header hidden — see ADR-002's "Chrome parity" bullet. **Whole-app goldens under Dockview added 2026-08-29 (PR #590):** `app/fx-dockview`, `app/credit-dockview`, `app/equities-dockview`, `app/admin-dockview` (the `LayoutEngine` preference seeded to "dockview" through the visual fixtures) sit beside their in-house siblings so the two engines' design parity is pixel-pinned per workspace, on both clients; generating them surfaced a crash that had shipped with #534 — dockview's `fromJSON` rejects a leaf root, so the single-panel Admin tab threw "root must be of type branch" and rendered nothing under Dockview (fixed in `toSerializedDockview`, pinned through a real `fromJSON`). **Card fill made skin-proof and the first frame painted 2026-08-29 (PR #594):** comparing those goldens against the in-house set showed transparent panel bodies in the 3D skins (dockview applies its background variables via `background-color:`, and the 3D `--panel`/`--panel-head` tokens are gradient *images*, invalid there) plus a double-composited fill under translucent skins (dockview paints its root with the group colour; the in-house root is transparent) — `dockview-hud.css` now paints card/head through the `background` shorthand and keeps the root transparent, pinned by a stylesheet-text test; and the x86 classic `app/fx-dockview` goldens turned out to be a BLANK workspace (the React bridge mounted dockview in a passive effect, so the first frame was empty and the screenshot stabiliser captured it in the only skins with no ambient animation — React passed against its own blank golden, Solid's pre-paint `onMount` failed, and post-merge `visual.yml` went red), fixed by creating the engine in `useLayoutEffect` and giving the FX twin a body-text wait. **Residual, pending: ≤1px half-pixel edges under Dockview** — dockview rounds every restored view size to an integer and renders `size − 3.5px` for the 7px gap, so each edge lands on `.5` and Chrome snaps it 1px past the in-house engine's fractional flex edge (0.3–3.5% of pixels per whole-app golden after this round, all of it glyph/border antialiasing at a 1px shift; measured in ADR-002's chrome-parity bullet). Not seedable away; the exact fix is a gap-0 model with the gutter emulated as a trailing 7px inset per non-last view, replacing the gap-compensation logic rather than extending it — a refactor of its own. **Intent glide added 2026-08-29 (PR #602):** collapse / expand / maximize / restore now glide over the in-house 0.34s under Dockview too — dockview positions groups and sashes through inline geometry styles, so it is one CSS transition in `dockview-hud.css`, gated on a `data-dock-glide` marker `createDockEngine` sets around the four intents (drags and resizes rewrite the same styles and must stay instant, as in-house), reduced-motion and freeze gated. **Fully-stripped column fixed 2026-08-30 (PR #629):** collapsing both FX rail panels under Dockview left two horizontal bars atop an empty full-width column; the engine now applies the in-house `stripDir` walk (a strip reads against the nearest enclosing split that is not itself fully stripped), so the last strip completing a column flips the column to 38px vertical strips sharing the rail, with the column's width remembered for the first expand — orientations now reach the bridges through `onStripsChange` since a collapse can re-orient siblings. **Maximize made the in-house strip policy 2026-08-31 (PR #648):** the Dockview engine had used dockview's native `maximize()`, which HIDES every other group and knows no scope, where in-house maximize is a policy over strips (every leaf under the boundary — the dock, or a `nearest-column` panel's own column — except the maximized panel becomes a strip); `createDockEngine` now emulates exactly that over the collapse records (so the `stripDir` flip applies, the siblings glide into their bars, and restore puts back only the strips the maximize made — a user's strip survives), scope reaching it through a `maximizeScope(panelId)` hook both bridges read from `PanelSpec`, and a maximize-forced strip's restore bar dispatches `restore` while a user strip's dispatches `expand`, as in-house. **Layout-state goldens + engine-parity report 2026-08-31 (PR #649):** the matrix now shoots the four layout shapes (root maximize, rail maximize, lone strip, fully-stripped column) in the real FX workspace under BOTH engines (`app/fx-{maximized,rail-maximized,collapsed,rail-collapsed}` + `-dockview` twins, seeded via new `AppData.layoutMaximized` / `layoutCollapsed` since the visual host's layout intents are no-ops), and `pnpm visual:engine-parity [--set …]` diffs every `X` / `X-dockview` golden pair with Playwright's metric into a scenario × skin table (first reading p50 0.002 / p90 0.007 / worst 0.016 — the layout states sit in the resting workspaces' band). The first goldens exposed a React bridge bug: under StrictMode the double-mount rebuilt the engine from the disposed one's STRIPPED blob and the stale applied-collapse list left it un-collapsed (restore bar stretched across a 97px group) — fixed by re-pushing `maximized`/`collapsed` on every engine build (`engineVersion`), pinned by a StrictMode unit test. **Residual, pending — the persisted blob carries strip geometry:** reloading with a collapsed panel restores its group at dockview's ~100px minimum, the re-applied collapse records THAT as the size to restore, and the first expand after a reload lands at ~100px instead of the natural size; fix = serialise the un-stripped layout (substitute recorded sizes, or snapshot before clamping) in `serializeLayout`. **Still pending under Dockview:** Jarvis-docked panels are in-house-only (the Dockview seed is the static tree); and, unchanged from v1: pop-out OS windows, floating groups, and panel close/reopen. ADR: [adr/ADR-002-layout-management-port.md](adr/ADR-002-layout-management-port.md); research: [research/2026-06-22-layout-management-landscape.md](research/2026-06-22-layout-management-landscape.md); spec: [superpowers/specs/2026-08-11-dockview-layout-engine-design.md](superpowers/specs/2026-08-11-dockview-layout-engine-design.md); plan: [superpowers/plans/2026-08-11-dockview-layout-engine.md](superpowers/plans/2026-08-11-dockview-layout-engine.md).

## 🔴 Designed, not built (plan/spec merged, no implementation)

- **Code-quality gate-scope round (2026-09-01 audit — four of seven plans remain)** — the audit found the doctrine written but the gates scoped narrower than it everywhere outside the contract/e2e tiers. Shipped 2026-09-02: the RN styling doctrine doc (#653), the jest-rationale docs (#651), and the devtools state doctrine + gates 38–40 (#654). Remaining, roughly independent (4 must precede 6): **(1)** inline-style ban extended to RN + extension, `inlineStyleBan` joins `check:react-policies` — [superpowers/plans/2026-09-01-inline-style-ban-rn-scope.md](superpowers/plans/2026-09-01-inline-style-ban-rn-scope.md); **(4)** `rtc/name-jsx-handlers` inline-callback ban + full ~190-site migration (react 71 / solid 82 / RN 38) — [superpowers/plans/2026-09-01-name-jsx-handlers.md](superpowers/plans/2026-09-01-name-jsx-handlers.md); **(6)** solid/reactivity disable reduction (26 die with plan 4's named wrappers; 45 setup-scope triaged; ledgered warnings fixed-or-justified; this page's stale "11" corrected there) — [superpowers/plans/2026-09-01-solid-reactivity-disable-reduction.md](superpowers/plans/2026-09-01-solid-reactivity-disable-reduction.md); **(7)** `rtc/no-framework-calls-in-specs` page-object isolation for the co-located tiers, warn→ledger→per-package error, ~240 specs in four waves, client-prototype excluded — spec [superpowers/specs/2026-09-01-spec-page-object-isolation-design.md](superpowers/specs/2026-09-01-spec-page-object-isolation-design.md), plan [superpowers/plans/2026-09-01-spec-page-object-isolation.md](superpowers/plans/2026-09-01-spec-page-object-isolation.md).
- **Prototype ↔ data-model mismatches (adapt the model to the UI, not only the UI to the model)** — the Phase 5 design records **seven** places where the mobile-v1 prototype asks for something the domain/seam cannot express, and each is currently resolved by contorting the UI. That is the right default for a presentation rehaul but not automatically the right answer: several exist only because the domain was shaped for the desktop product's information architecture. Ledger (spec §8, with per-item workaround vs. model-change): **M1** sell-side has no "quotes I submitted" history projection; **M2** `CreateRfqInput.dealerIds` is required though the prototype has no dealer-picker; **M3** RFQ window is 120 s in the domain vs 45 s in the prototype (a 120 s ring barely moves on a phone); **M4** equities has no tick-history stream, so sparklines must be derived from OHLC candles; **M5** a fill is only a binary animation intent, with no order-lifecycle event carrying qty/price/outcome; ~~**M6**~~ **FIXED 2026-07-26 (5c Task 1, PR #397)** — `AnalyticsSimulator` used to emit the same frozen `STATIC_POSITIONS` constant on every emission, and **two of the Analytics screen's three widgets read it**, so both the bars' 800 ms tween and the bubbles' 900 ms breathing fired once on mount and never again. Positions now drift on a mean-reverting step; the web dashboard gained live positions at the same time, since the simulator sits below both clients. **M7** analytics history appends every 10 s against the prototype's 1 s mock cadence. None of these are in Phase 5's scope; each is a candidate follow-up that would let a later pass *simplify* the UI rather than keep working around the gap. **All seven are SHARED, not RN-only** — every one lives in `@rtc/domain`/`@rtc/client-core`, below the UI and above both clients, so a model fix serves web and RN at once (the web analytics bars read the same frozen `STATIC_POSITIONS`; web merely never animates them, so nothing visibly fails to move). By contrast the *rendering* wrinkles — Reanimated worklets, regular-weight-only Skia text, unported `shadowBlur` bloom, the dev-client/simulator dependency — are RN-only with no web analogue. **Separately tracked as R1 (a gap against the REFERENCE implementation, not the prototype):** Adaptive's ReactiveTraderCloud analytics **exposure bubbles are draggable** (force-directed sim + drag behaviour); ours are inert in **both** clients — verified: no pointer/mouse/touch handler anywhere in `client-react`'s (or `client-solid`'s) `PositionsPanel`, and RN's are a static shelf-packed layout. Dragging is interaction, not decoration — it is how a user pulls a crowded bubble out to read it, most valuable exactly where our layouts are weakest. Cheaper than first recorded: an earlier draft claimed drag threatens pinned goldens — **that was wrong**. What threatens a golden is a force-directed simulation *settling over time*, not drag; a golden never drags, so an un-dragged layout is exactly as stable as today's (and the harness could disable drag outright). Keep the deterministic shelf-packed layout and add drag on top. Spec §8: [superpowers/specs/2026-07-25-rn-mobile-v1-rehaul-phase-5-design.md](superpowers/specs/2026-07-25-rn-mobile-v1-rehaul-phase-5-design.md) Spec §8: [superpowers/specs/2026-07-25-rn-mobile-v1-rehaul-phase-5-design.md](superpowers/specs/2026-07-25-rn-mobile-v1-rehaul-phase-5-design.md)
- **Feature flags** — OpenFeature + Flagsmith. Plan: [superpowers/plans/2026-07-01-feature-flags.md](superpowers/plans/2026-07-01-feature-flags.md)
- **Fullstack observability & distributed tracing** — **spec merged 2026-08-05, nothing built.** Vendor survey done and decided: **Sentry** as the backend, reached through a repo-owned `ObservabilityPort`, with **W3C Trace Context + OTLP as the wire formats** so the vendor stays an adapter (pre-vetted fallback: Grafana Cloud — 50GB traces, 3 users, no card). Datadog disqualified on cost/lock-in; self-hosted SigNoz rejected as a second production system to operate. **The finding that shaped it: this app is WebSocket-native, and neither Sentry nor OTel auto-instruments WebSockets** — the whole trading path is `WsAdapter` → `ws` → `ws-effects`, so every vendor's out-of-the-box tracing yields page loads and nothing else. Propagation is therefore hand-written into the existing `{ type, payload, correlationId? }` frame as an optional `traceparent`, which makes the vendor choice a backend/cost decision rather than a capability one. **Two numbers drive the design:** `PricingSimulator`'s ~511ms mean tick = ~2 frames/sec/symbol, so **one open tab would exhaust the 5M-span monthly free tier in under four days** if ticks were spanned (hence head-sampling by *frame class* — RPCs and subscribe-handshakes traced 100%, stream ticks never spanned, only metered); and a throw inside a per-frame rAF loop is 60 errors/sec, so **one animation-loop bug would burn the 5,000-error monthly budget in ~83 seconds** (hence a client-side circuit breaker *before* the SDK, not Sentry's server-side spike protection, which bills first). **Sourcemaps resolve without touching the existing flag:** production moves to `sourcemap: "hidden"` — generated, uploaded to Sentry with Debug IDs, then deleted before deploy — so Vercel's 403-on-served-`.map` becomes irrelevant (Sentry symbolicates from its own copy) and the `RTC_SOURCEMAPS=1` inline debug build stays untouched for DevTools profiling. **Already shipped (the one lightweight slice):** server-side WS connection logging (`packages/server/src/observability/connectionLog.ts` — one structured line per accept / disconnect / rejected-upgrade, reason-tagged, token-safe), deployed and verified live on Fly 2026-07-25; all four branches emit, and it doubles as the end-to-end oracle for the auth gate. Note `active`/`total` are per-process, so Fly's idle autostop resets them — "who's connected now" is reliable, lifetime totals are not; that same autostop is why the server adapter needs a **`SIGTERM` flush** or loses everything buffered. See the server README → [Connection observability](../packages/server/README.md#connection-observability). **Six phases, none started:** (1) port + contract + dep-cruiser allowlist *with a falsification test* (dep-cruiser package rules sat dormant until #327, so a negative gate that never runs looks identical to one that passes) + server OTLP + structured logs; (2) web errors + hidden-sourcemap pipeline; (3) the WS distributed trace; (4) Jarvis span tree with token/cost attributes — the only instrumentation with a dollar sign attached; (5) RN (EAS Build auto-uploads sourcemaps, **EAS Update does not** — needs an explicit CI step); (6) optional Session Replay + Fly Prometheus dashboards. **Known constraint: Sentry's free tier is 1 user** — anyone else means the $26/mo Team plan. Spec: [superpowers/specs/2026-08-05-observability-design.md](superpowers/specs/2026-08-05-observability-design.md)

- **RN frame-clock defects — two follow-ups from the frozen-boot fix (PR #456)** — **T30:** `useShellTelemetry` has the identical `useFrameCallback` identity bug the boot canvas had, so its rolling window is measured against a clock that keeps restarting and the HUD status strip reports **302 / 485 / 1264 FPS**. The obvious repair (hoisting the callback) trips `react-hooks/immutability`, which tolerates shared-value writes in an inline frame callback but not in a hoisted one — so it needs its own change, not a smuggled one. Cosmetic, but it is the same defect. **T31:** the reanimated jest mock rebuilds `useSharedValue` as a fresh Proxy every render, where the real hook is `useRef`-backed and stable; any code memoising on a shared value therefore looks broken under jest and correct on device, or the reverse. **This is why the frozen-boot fix has no regression test.** Pinning the mock was attempted and reverted — a `useRef`-backed stub broke four existing specs (`HoldToUnlockRing`, `LaserScene`) that lean on the per-render rebuild. Doing it properly would convert an entire device-only defect class into a CI-visible one. Background: [rn-motion-architecture.md](rn-motion-architecture.md) §4.

## 🟠 Planned but gated / not executed

- **Test bake-off — deferred follow-ups** (the retirement itself **SHIPPED 2026-07-20**, PRs #294/#296/#297/#299/#300/#303/#304; verdicts and measured before/after live in [superpowers/plans/2026-07-19-test-bakeoff-retirement.md](superpowers/plans/2026-07-19-test-bakeoff-retirement.md) and each tier's docs). Of the four items explicitly scoped out, three remain open — the fourth, a paths filter on `visual.yml` so doc-only merges skip the run, shipped 2026-07-25:
  - **Gate 21's mechanism is still fragile** — the highest-value item. Its `FEATURE_NAMES` list in `tests/scripts/grep-gates.ts` is **hardcoded**, with nothing scanning `tests/specs/**` in reverse; that is exactly how `admin/incident.feature`'s `@presenter` scenario sat with zero presenter coverage while the gate reported green (found and closed in PR #300 — but only the *instance*, not the mechanism, so the next feature file can fail the same way). It also doesn't match `Scenario Outline:` and compares counts only, never scenario names.
  - **Trim the ×10 theme matrix** — the remaining visual-time lever. The post-retirement visual job is now ~15 min (~52 → ~29 by retiring two tiers, then ~29 → ~15 via the PR #311 job-matrix parallelization that runs react + solid on separate runners); what's left is 1282 scenarios (≈130 base × 5 skins × dark/light), not tier count. Full cross-product on App-level scenarios only would cut it again; costs a full golden regen.
  - **Revisit the parked Gherkin layer (~2026-09)** — `playwright-cucumber` (react + solid) is off the PR gate and kept alive by `.github/workflows/e2e-gherkin-weekly.yml`. Decide then whether it earns its keep as the BDD/living-docs showcase or gets deleted. Two accepted reductions to re-examine at the same time: a Gherkin/step-tree regression is now caught weekly rather than per-PR, and **no presenter test runs under real timers any more** (`RealAwaitHelpers` went with the cucumber peer), so a bug that only manifests with real `setTimeout` has lost its witness.
- **RN visual tiers — the three-way comparison is UNFINISHED, not decided** (re-framed 2026-08-06; the harness itself is done and shipping). The harness route was repaired so `__visual` renders in isolation (outside `AuthGate`, root sibling of the `(app)` group) and hardened (`__DEV__` + env flag), and the **simctl** tier carries all 18 goldens on `ios-iphone17-26`. What was called a "bake-off" was a **3-scenario viability spike on one device with no Android run** — enough to establish which tiers can run here, not to rank them, and the Playwright-vs-Cypress precedent it invites ran both suites over the same specs for *months*. **No tier is chosen and none is retired**; `simctl` is `xcrun`-based and can never take Android shots, so Maestro stays load-bearing regardless. **The JDK blocker closed 2026-08-08** — `openjdk@21` is installed and pinned in `ios-visual-spike.yml`, so the tier runs for the first time; the recorded "JDK 17" turned out to be a *floor* mis-transcribed as a pin, and 21 (not the newest) is the newest JDK Maestro runs warning-free. Three open items, in order ([BAKEOFF.md](../packages/client-react-native/tests/visual/BAKEOFF.md), [rn-open-items.md](rn-open-items.md) T9):
  - **Pin Maestro's device** — the runner calls `maestro test` and lets Maestro pick, while its goldens sit under a path claiming `ios-iphone17-26`; a second booted simulator can be shot against the pinned baseline. Cheap, independent, and it hands the runner the UDID `hideDevMenuFab` needs (Maestro's 3 goldens still carry the dev-menu gear the simctl set shed). **Now the only thing between here and a working Maestro tier.**
  - **Capture every tier at full coverage, then judge** — gated on the mobile UI's **visual fidelity settling**, so goldens are pinned against a UI that has stopped moving. Deliberately deferred: capturing 15 more Maestro goldens now buys churn, not a baseline. Judge on wall-clock, flake rate over repeated runs, and what each tier catches.
  - **Inset 3D-card scenario for the shadow-clip class** — the injected-bug proof showed the current full-bleed `SurfaceCard` scenario can't catch the PR #147 `overflow:hidden` shadow-clip (its drop shadow is off-screen). A *scenario-matrix* gap that handicaps all three tiers equally, so worth closing **before** the comparison. Plan: [superpowers/plans/2026-07-19-rn-visual-harness-repair.md](superpowers/plans/2026-07-19-rn-visual-harness-repair.md)
  - **owl** remains blocked, not outscored — it never took a screenshot. Two of its three blockers are ours (a wrapper script; `expo prebuild` for the gitignored `ios/`); only the React 19 / new-architecture gap needs an upstream release.
- **Atomic test-ID renames** — plan written, never executed. Plan: [superpowers/plans/2026-07-10-atomic-testid-renames.md](superpowers/plans/2026-07-10-atomic-testid-renames.md)

## ⚪ Optional / next step (no plan file yet)

- **RN prototype-fidelity residuals** — the module-by-module pass against the mobile-v1 prototype closed on 2026-08-30 (#588–#626: framed goldens, blotter, trade, sub-nav, banner, HUD grid, tokens, font weights, lock, rfq-tiles, markets, analytics, appearance, boot chrome, typography + the visual-harness font gate, and app scenarios for the three prototype-only surfaces — the showcase report reads Close 21 / Moderate 0 / Far 0). A residuals round on 2026-08-31 shipped every pixel-moving item: the last faux-bold mono pairings got their faces' real 600/700 cuts (#630), the spot ticket's SELL / BUY pads took the design's accent tint / border / outward alignment (#641), the radial dock moved to the design's true-bottom geometry with the open scrim covering the FAB (#642, 13 goldens re-pinned — nine module scenarios mount the chrome), `weightedFont`'s cut-less fallback keeps the platform mono under weighted classic labels (#644), the exposure-bubble diameter rounds to whole px as the design's does (#645), and `LoginScreen` — which had no design frame — was restyled as `LockScreen`'s sibling (emblem, Orbitron wordmark, tracked-mono inputs and pill) and given its own `shell/login` golden, the 22nd scenario (the PR carrying this edit). What remains is data, not fidelity: the domain simulator's 6-symbol equity roster vs the design's 8, the fake's sine sparklines, `Acme 5.5% 2030` where the design seeds tickers, `JP MORGAN` vs `J.P. MORGAN` — each a `@rtc/domain` / fixture change whose blast radius reaches the web clients' goldens and e2e text matchers, so none is a mobile-only edit.
- **`@rtc/client-react-native` — coverage MEASURED for the first time 2026-08-14; still ungated, unmerged, and with NO UI contract tier.** Found 2026-08-05, when the headline was that its coverage had never been computed even once — not "coverage is low" but **unmeasured**, no `test:coverage` script of any kind, so no number existed for anyone to be alarmed by. A number exists now, and it is two numbers, because this is the only package running two runners. Against the whole package (`src/**/*.{ts,tsx}`): **jest** (109 suites / 507 tests) **63.89%** statements, 76.38% branches, **53.69% functions**; **vitest** (58 files / 540 tests) **26.48%** statements, 28.44% branches, 34.37% functions. **Do not add or average them** — the denominators disagree (7731 istanbul statements vs 5599 v8, same source tree), and each tier's denominator is the whole package while it runs only half the tests, so neither is "RN's coverage" and the sum is not either. The real figure is the *union*, which needs a merged report that does not exist yet. Full reading guide: [`packages/client-react-native/README-COVERAGE.md`](../packages/client-react-native/README-COVERAGE.md). For scale, both tiers sit far below the ≥95%/≥85% bar the two web clients are held to, and **functions at 53.69% is the weakest axis** — worth noting the old "326 assertions" figure above was itself stale by 3x (1047 tests today). Separately it owns **0** of the 99 shared `@rtc/ui-contract` behavioural specs — only `client-react` and `client-solid` depend on that package at all — so RN's UI behaviour is asserted solely by hand-written colocated tests with hand-rolled ViewModel fakes.

  **Why no gate caught this**, which matters more than the gap itself, because three separate mechanisms each look like they cover it:
  1. **The ≥95% bars are hardcoded per package, not derived.** `ci.yml` runs exactly two literal filters — `pnpm --filter @rtc/client-react test:ui:contract:coverage` and the `@rtc/client-solid` twin. A third client adds nothing; there is no rule that says "every client package carries a coverage bar."
  2. **`check:react-coverage` was NOT about test coverage, and it was the near-miss that actively misled. RENAMED 2026-08-14 → `check:react-policies`.** Its CI step was labelled *"React package coverage (every React package has an explicit policy)"*, and `client-react-native` **is** declared in its `POLICY` map — so the step was green and read as if RN's coverage were handled, when what it asserts is the three React *lint* policies (`compiler`, `memoBan`, `reactHooks`). A reader auditing "do all React packages have coverage?" got a green tick for the wrong question. Now `check:react-policies` (script `scripts/check-react-package-policies.mjs`, CI step *"React package policies (compiler / memo-ban / react-hooks all explicit)"*), per [handler-naming.md](handler-naming.md)'s name-the-effect rule, which applies to gates as much as functions. One stale mention of the old name survives in the completed plan [superpowers/plans/2026-08-01-jarvis-phase-3-anthropic-loop.md](superpowers/plans/2026-08-01-jarvis-phase-3-anthropic-loop.md) and was left alone deliberately — it is a dated record of what ran that day, not live instruction.
  3. **`check:scripts` only requires `typecheck` and a `test*` script.** RN has both. Coverage is not in its `REQUIRED` list, so "every package wired to the gates" is true and says nothing about coverage.

  Also absent from the published report: `coverage-report.yml` has **8** tiers (`domain`, `server`, then app / ui-contract / ui-visual-reach for each of react and solid) and none is RN.

  **The work, in the order that de-risks it:**
  1. **Measure first, gate second.** Add `test:coverage` to the RN package and find out what 326 assertions actually cover before committing to a number. RN runs **two** runners (`vitest run --passWithNoTests && jest`), unlike every other package, so this is not a copy of an existing config — jest needs its own coverage provider and the two reports must merge or be reported as separate tiers.
  2. **An RN UI contract tier.** The existing one cannot simply be extended: `MountedRoot.root` and `PageContext.root` are typed `HTMLElement`, and **86 of 92** shared page objects query the DOM (`@testing-library/dom`, `user-event`, `textContent`, `dataset`). Notably the **specs** are nearly clean — only **5 of 99** touch the DOM — so the portable half is the specs and the unportable half is the page-object layer beneath them. Two routes: extract a platform-agnostic query interface behind the page objects (large, and it buys cross-platform specs), or give RN its own contract tier in `client-react-native` mirroring the *shape* but not the code (smaller, no shared assertions). **Decide that explicitly** — and note RN implements a *different design* (mobile v1) over the same core, not the same UI as web, so a large fraction of the web specs assert screens RN deliberately does not have.
  3. **Gate it in `ci.yml`** at the react/solid bar (≥95%, branches ≥85%), once (1) says what the starting point is.
  4. **Publish it** as a 9th (or 9th+10th) tier in `coverage-report.yml`.

  Worth stating plainly: the `ui-contract` tier exists to prove **two implementations of the same UI agree** — it is what makes the React↔Solid replaceability claim real. There is only one RN client, so that specific justification does not transfer; the case for RN is the ordinary one (behavioural regression safety, a measured floor), which is why measuring before gating is the right first move. No spec or plan file yet — needs its own brainstorm.

- **The web FPS meter cannot report its own failure — reported from the field 2026-08-05, both web clients.** On slow Citrix Windows workspaces with no GPU the status-strip FPS "more often than not stayed frozen with no fps actually shown". Read the code and the mechanism is plain: `useLiveMetrics.ts` (identical in `client-react` and `client-solid`) is a bare `requestAnimationFrame` loop that publishes **only after ~1s of frames have actually arrived** (`PUBLISH_MS = 1000`). Where rAF is starved — no GPU, a remote/occluded session, a background tab — the loop never reaches its publish branch, so the readout sits at its seed **forever**. The defect is not the missing number, it is that **a frozen meter is indistinguishable from a healthy one that has not ticked yet**: the single most useful fact on that machine ("frames are not arriving") is the one state the instrument is structurally unable to display. Same shape as the RN telemetry bug (T30 in [rn-open-items.md](rn-open-items.md)) and as `boot/static` (T33): an instrument that goes quiet on failure reads as success. Fix direction: a wall-clock watchdog independent of rAF — if no publish has happened in N×`PUBLISH_MS`, publish a degraded state explicitly rather than leaving the last value standing. Small and self-contained (two files plus tests); worth doing because a genuinely low frame rate is exactly what this readout exists to surface. No plan file yet.

- **A node-against-dist boot smoke for CI (surfaced by the Jarvis P2 final review, 2026-07-31).** Boot `packages/server/dist/index.js` under plain Node in CI — the exact command the Fly Dockerfile runs — because **no gate executes the server's own dist bundle today**: unit tests and the fullstack tier both start the server via `tsx src/index.ts` from source. (Workspace-package imports are NOT the gap — `@rtc/shared` et al. resolve through their real `dist` builds via package exports even under vitest, so a broken specifier there fails existing suites as soon as an external consumer imports it. The near-miss that motivated this was narrower: a window where a package's `dist` was broken before anything outside it consumed it — an extensionless ESM specifier that tsc accepts and only plain Node rejects.) Caught by review, not by any test. No plan file yet.

- **Vercel CLI: unpin the deploy workflows from `vercel@57` once 58's `vercel pull` regression is fixed (pinned 2026-07-28, PR #419).** CLI 58.0.0 (published 2026-07-27T20:26Z) breaks `vercel pull --environment=production` under env-var project linking (`VERCEL_ORG_ID`/`VERCEL_PROJECT_ID`, no `.vercel/` directory) with "Could not retrieve Project Settings" — Deploy run 30337004044 failed both client jobs this way, while the previous day's run passed on an identical tree with 57.0.0, so the CLI major is the only delta. The 58.0.0 release notes document **no** breaking change to `pull`/linking, and no upstream issue existed as of 2026-07-28 → treated as an unintentional regression, so there is nothing on our side to adapt to; all four install sites (`deploy.yml` ×2, `deploy-proto.yml`, `deploy-cd-proto.yml`) are pinned to the last proven major instead. To unpin: install a later 58.x locally, verify `VERCEL_ORG_ID=… VERCEL_PROJECT_ID=… vercel pull` resolves the project (or check the upstream fix landed), bump the four sites in one commit, and prove it by dispatching Deploy. The local dev-machine CLI is independent of this pin (deploys are CI-only) and can track latest separately. No plan file.

- **"Login wait style" ships with NO description — revisit (PR #410, 2026-07-27).** Every other described row in the Preferences modal carries a one-line explanation; this one carries none, and that was a layout concession rather than a copy decision. The original text ("Which sign-in waiting animation to show. Auto alternates between them.") widened the label column enough to force the segment's three options — `Auto | Handshake | Reactor` — to stack vertically, which pushed the row below it ("Login wait delay") clean out of the modal's visible body. Dropping the description let both rows sit inline and the columns finish level. So the row currently relies on its label plus the option names to explain itself, and **"Auto" in particular does not communicate that it alternates per attempt** — the one fact a user needs to understand why the animation changes between sign-ins. Fix directions, in rough order of preference: widen the label column (or narrow the segment) in `PreferencesModal.module.css` so a described 3-option row fits; add a `title`/tooltip rather than a block description; or shorten to something like "Auto alternates." and re-measure. **Whatever the fix, re-render and LOOK at the golden** — the pixel tier cannot see this class of change (see the visual-golden blind-spot entry above: the whole restructure moved 0.02 of pixels against a 0.06 threshold). Not urgent; the control works and is contract-tested either way.

- **RN golden `boot/static` asserts one frame of an animation, and bakes in the dev-menu gear (measured 2026-08-04).** The scenario was tracked here from 2026-08-02 as a possible **regression** — a dimmer hex ring than the golden, cause unestablished, do-not-re-capture. **Measurement closed that: there is no regression.** Over the emblem's own region two independent captures report **0 differing pixels vs the golden**, so PR #456 and the `@rtc/motion-core` dist rebuild are exonerated. What remains is two harness defects the original framing hid. **(1)** `BootEmblem` pulses — six raw screenshots off one mounted scenario climb 24 → 61 → 93 → 118 → 133 in max channel delta — and its pulse is gated by `AccessibilityInfo.isReduceMotionEnabled()`, an **OS** signal, so it is the one scene the harness's preference-driven freeze cannot reach. The golden is therefore a *phase sample*: reproducible only because the driver's fixed post-mount settle re-samples the same phase, and liable to "change" on any timing perturbation. **This is not new work — it is `T16`**, open since 2026-08-01 with the right fix already named (route the emblem through `useBootMotionEnabled()`; it is a slice of **P5**, RN's missing Freeze catch-all). T33 was opened the next day as a suspected *code* regression by someone who had the answer six rows above it in the same file — the reason to search the findings page before opening the next finding. **(2)** Today's whole 1.2158% frame delta is the Expo **dev-menu gear**, right-edge in the golden and left-edge now — the golden asserts a draggable dev-client control that says nothing about the app. **The transferable lesson is the inference that was wrong**: "identical across runs ⇒ the render is static ⇒ the golden is stale" reads a phase-locked sample of a live animation as proof of a still one. `scenarios.tsx` had predicted this exact failure in prose before the golden was committed. Detail + measurements: **T33** in [rn-open-items.md](rn-open-items.md).
- **Visual goldens: a PASSING run is not evidence that layout is unchanged (found 2026-07-27, PR #410).** Two compounding defects, both measured. (1) The regen script used bare `--update-snapshots`, whose default mode is **`changed`** — it rewrites a golden only when the comparison FAILS. The *full*-refresh path never hit this because it wipes the tree first, so the bug sat latent in the **targeted** path: a `prefs/modal` refresh reported "10 passed", committed nothing, and left the golden showing the OLD layout. Fixed by pinning `--update-snapshots=all`. (2) The comparison passed because a complete two-column restructure of `PreferencesModal` — a new section, ~13 rows relocated, two rows added — moves only **0.02** of pixels against the `maxDiffPixelRatio: 0.06` threshold. The config's claim that "0.06 still catches layout/structure regressions (which move >> 6%)" is **false for text-sparse dark surfaces**, where the background dominates the capture and glyphs are a small fraction of it. Measured by temporarily setting the threshold to 0: `35343 pixels (ratio 0.02)`. **The threshold was deliberately NOT changed** — its cross-microarch AA-jitter rationale is sound and settled (see the config comment); tightening it globally would reintroduce the flake it was raised to fix. The durable mitigation is procedural and is now written into the config: after any deliberate layout change, regenerate the goldens and **look at the PNG** — never infer "no visual change" from a green tier. Open question deliberately not answered here: whether a *structural* assertion (DOM-shape or per-column row-count contract, as `PreferencesModalPage.rowCountInColumn` now does for this one modal) should be generalised, since pixels are the wrong instrument for layout on sparse surfaces.

- **React-local golden housekeeping — the linux-arm64 bucket** — the darwin-arm64 latent-drift backlog is RESYNCED (the 0.005 solid parity gate turned ~162 under-tolerance drift files into real local-assert failures, forcing the dedicated resync the old entry prescribed; solid local visual is 1493/1493 green again). Residual: decide the fate of the partial `react-local/linux-arm64` bucket (~95 files incl. `app-equities.png`, stale since 2026-07-19 — native arm64 linux renders ~29.6% off the CI set, which is why the emulated-amd64 route won; likely the right move is deleting the bucket rather than regenerating it). No plan file.

- **Desk SFX port (v5 synthesized audio engine)** — port the prototype's sound engine to `client-react` + `client-solid`: one `AudioContext` with master gain + feedback-echo bus + in-code convolution reverb (no samples), all SFX synthesized (`click / buy / sell / fill / reject / ping / alarm / maximize / restore / Jarvis open-close / comm chirps`), the capture-phase auto-click provider (one listener walks ancestors for `cursor:pointer`), and the pre-gesture audio unlock. Gate everything behind a **Preferences → "Sound effects" toggle, default OFF** (persisted preference — budget for the ~10-site preference blast radius). Decided 2026-07-26: **Jarvis voice is out of scope indefinitely** (feasibility unclear) and **boot splash music is explicitly not planned** — logged here so the decision isn't re-derived. Source anatomy: [design/web/v5/dev-handoff/HANDOFF.md](design/web/v5/dev-handoff/HANDOFF.md) §6. No spec/plan file yet — needs its own brainstorm when picked up.
- **Canvas chart renderer productionization — SHIPPED 2026-08-10.** The TradingView tier itself was already complete (indicator panes, log scale, drawing tools 3a+3b, comparison series — 2026-08-08). This closes the recorded escape hatch: a switchable `ChartSubstrate` preference (`"dom" | "canvas"`, default `"dom"`) swaps the plot's per-datum geometry (candles, volume bars, drawings, indicator-pane series, crosshair lines) for one `SceneCanvas` per region, drawn by a structural-`Canvas2D` engine now living in `@rtc/motion-core` (`drawPlotScene`/`drawVolumeScene`/`drawPaneScene`, a 16-key `CHART_PALETTE_TOKENS` map) — text (labels, readouts, chips, pills) stays DOM permanently. Witnessed by 5 shared contract cases (node-count pin measured **287 DOM-mode → 28 canvas-mode** nodes, identical on both clients), the `equities/chart-canvas` composite golden through the 10-combo skin×mode matrix, and one e2e journey (first Preferences-modal page-object pair) proving a canvas-mode drawing survives a DOM round-trip. Spec: [2026-08-09-canvas-substrate-design.md](superpowers/specs/2026-08-09-canvas-substrate-design.md) (receipt in §8); architecture: `docs/architecture/17-web-client-up-close.md` §17.8. Residue deliberately out of scope, recorded so it is not re-derived: **canvas text stays DOM permanently** (the design's central simplification, not a gap); and **RN/Skia rendering** was never attempted (mobile has no chart-canvas host). Two former residue items were since FIXED (2026-08-11): the `Canvas2D` `fillStyle` type hole — closed by divergent read/write accessor types (unconstrained `unknown` read, strict write), which made the real `CanvasRenderingContext2D` structurally assignable and deleted both `SceneCanvas` double-casts while making a `CanvasPattern` assignment a compile error inside the engine; and the composite golden's sub-pixel MACD histogram — the pane now draws from a host-local zigzag close series (the `EquitiesChartPanes` fixture pattern), so histogram/crossovers are multi-pixel in all 10 combos.
- **Motion-audit off/calm roster is inventory, not a gate** — `/rtc:perf-audit` hard-asserts only freeze (empty `document.getAnimations()` census + zero rAF; also CI-gated by `powerSaver.spec.ts` and the visual tier's freeze contract). At `off`/`calm` it just *prints* the animation roster — a newly-added expensive animation at `off` is only caught if a human reads the report or the `docs/performance.md` pre-merge checklist catches it in review. The hardening step, if ever wanted: commit a baseline roster snapshot (animation name + target + duration per view per level, hashed identities normalised across the CSS-modules hash) and have the audit diff against it, so a new/changed steady-state animation fails loudly with the delta. Deliberately NOT built yet — the roster still changes with normal feature work and a churn-y gate would train people to ignore it; revisit once the animation surface is stable.

- **Merge queue — CLOSED 2026-07-26, unavailable to this repo. Do not re-plan without an org transfer.** GitHub merge queue requires an **organization-owned** repository; this repo is owned by a *user* account (`owner.type: "User"` — public visibility is not sufficient). Both unknowns previously recorded here are now answered: (1) user-owned public repo → **not supported**; (2) the `403` on check-rollup APIs → **misattributed**, it was the sandbox's under-scoped fine-grained PAT, not the repo or the API (`gh pr checks` exits 0 on the host OAuth token). The catch-up rate is **no longer a trigger** — re-measuring cannot unblock an unavailable feature, so the ~2026-08-09 revisit is cancelled. The stale-merge hole therefore stays open by design (`strict_required_status_checks_policy` is `false`; post-merge `main` CI is the backstop, and this has bitten once), and Rule 3's manual triage — sharpened in PR #370 so prose-only overlap merges as-is — is the **permanent** mechanism, not an interim one. Evidence limit: rests on ownership type + GitHub's docs, not an API rejection; the decisive `POST /rulesets` probe writes repo settings and was not run. Design + full amendment: [spec](superpowers/specs/2026-07-26-catchup-triage-and-merge-queue-design.md); background: [IDEAS.md](IDEAS.md).

- **Rotate or delete the revoked PAT at `.github/.token` — breaks every sandboxed session.** Verified 2026-07-26: that fine-grained PAT returns `401 Bad credentials` on even `gh api user`. The `claude-sandbox` launcher **prefers that file over the host keychain** and injects it as `GH_TOKEN`, so a sandboxed run currently fails *all* `gh` calls (PR creation, CI polling, merging) — not just the check-rollup endpoints. Host sessions are unaffected; they use the keychain OAuth token. **Simplest fix: delete `.github/.token`** — the launcher then falls back to `gh auth token`, whose `repo` scope also covers `gh pr checks` / `statusCheckRollup`, retiring the original 403 as well. Alternative: reissue with *Checks: read* + *Commit statuses: read* granted. The file is gitignored and untracked, so nothing leaked.
- **Adopt some form of mobile CI testing** — today the RN visual + Maestro tiers gate nothing ([rn-open-items.md](rn-open-items.md) T1: iOS pixels need macOS, every job in `ci.yml` runs `ubuntu-latest`). The full landscape — GitHub-hosted macOS runners, self-hosted Macs, cloud Mac providers, managed mobile CI, real-device farms, Maestro Cloud, and the device-free react-native-web route — with dated costs, what teams actually gate on, and the cross-machine golden-drift trap, is now written up in [mobile-ci-testing-options.md](mobile-ci-testing-options.md). **Stage 2 STARTED 2026-07-26** — [`ios-visual-spike.yml`](../.github/workflows/ios-visual-spike.yml), `workflow_dispatch` only. Its environment probe is **green in 6 min 27 s** and settled the question the item was blocked on: `macos-26` instantiates and boots **iPhone 17 × iOS 26.5**, an exact match for the committed `ios-iphone17-26` pin. It also killed two assumptions — **97 GB free rather than the documented 14 GB**, and `python3` **3.14.6** vs `fb-idb`'s ≤ 3.13 ceiling, confirming idb would not have installed and that routing CI through Maestro was right. Numbers in [mobile-ci-testing-options.md §7.0](mobile-ci-testing-options.md). **Remaining:** capture-vs-golden pixel parity (§5 expects drift; the answer is a second golden bucket, *never* a wider tolerance), then promotion to `schedule:` + `push: main`, modelled on `visual.yml`. **Never a PR gate** (§4). **Still recommended and still unbuilt:** the Android Maestro job on free `ubuntu-latest` (KVM-accelerated emulator, and the Maestro tier is already cross-platform) — the options doc rates it Stage 1, ahead of iOS, and it remains the cheapest way to make the RN UI gate anything. **No plan or spec file yet.**
- **React Native inspector — evaluate / adopt a tool** — no inspector is a hard dependency yet; the options (React Native DevTools built-in, Reactotron, Radon IDE, and the repo's own RTC devtools relay) are documented with 2026 guidance in [react-native-inspectors.md](react-native-inspectors.md). Decision input: pick a day-to-day default (recommendation: the built-in RN DevTools; add Reactotron if a standing network/state monitor is wanted) and, if adopted as a dep, wire it `__DEV__`-only. Note: an inspector is a manual aid — it complements, not replaces, the automated `app.config.test.ts` guard that catches silent WS-mode regressions.
- **Devtools timeline UX — post-ship polish** — the timeline-first inspector rework (spec: [superpowers/specs/2026-07-20-devtools-timeline-ux-design.md](superpowers/specs/2026-07-20-devtools-timeline-ux-design.md)) and the store-first v3 rework (spec: [superpowers/specs/2026-08-29-devtools-store-first-navigation-design.md](superpowers/specs/2026-08-29-devtools-store-first-navigation-design.md)) both shipped; the non-blocking follow-ups their final whole-branch reviews triaged were resolved by the devtools-follow-ups Phase 2 sweep (2026-08-30) — `LiveHistory` bounds frames as well as events, the mount seed is exact, the nav tree accounts for evicted machines, `ContextPane` is split, and `rows` is re-tracked by `check:compiler`. One new item surfaced by that sweep: `MachineRow.intents` is uncapped in `InspectorStore` — same failure class as the log cap, harden when it bites.

- **`uuid` 7 → 11 — fixed, but never yet exercised by a native build.** The advisory (GHSA-w5hq-g745-h8pq) is **fixed**: the `uuid@<11.1.1` override in [pnpm-workspace.yaml](../pnpm-workspace.yaml) removed 7.0.3 from the lockfile, Dependabot alert #8 flipped to `fixed`, and the vulnerable path was never reachable anyway — the sole consumer `xcode@3.0.1` calls **only** `uuid.v4()` (`lib/pbxProject.js:90`), never the v3/v5/v6 generators the advisory names, and never with the `buf` argument that triggers it. Compatibility was checked, not assumed: uuid@11 still ships CommonJS (`xcode` resolves `uuid@11.1.1/…/dist/cjs/index.js`) and `pbxProject.generateUuid()` — the function Expo prebuild uses to write `.pbxproj` entries — returns valid unique 24-hex ids under it. **The one thing no test can cover:** `xcode` only ever runs *during a native iOS build*, and this repo has no CI that performs one, so no `expo prebuild` / `run:ios` / EAS build has consumed it yet. That gap is why the `@xmldom/xmldom` 0.9.x override slipped through and broke `expo run:ios` on SDK 57. **Action: none, unless a native build fails** — if the next `dev:ios` or EAS build is clean, delete this entry.
- **Outstanding lint warnings (11)** — ESLint *warnings* are non-blocking by design, so they linger. The committed ledger [lint-warnings.md](lint-warnings.md) (kept honest by the `check:lint-warnings-drift` CI gate) tracks every one so it can't accrete untracked. Currently 11 `solid/reactivity` warnings in `packages/client-solid` awaiting cleanup (fix or justified `eslint-disable`; each fix drops the ledger by one). Regenerate the ledger with `pnpm sync:lint-warnings`. Spec: [superpowers/specs/2026-07-18-lint-warning-ledger-design.md](superpowers/specs/2026-07-18-lint-warning-ledger-design.md). **Future tightenings (not started):** (1) once the backlog is drained, add a monotonic count-ratchet so the number can only decrease (noted in the spec); (2) optionally extend the collector to cover Biome + stylelint warnings, not just ESLint.
- **`createWorld` → options object** — the ui-contract test harness factory `createWorld` (`packages/ui-contract/src/shared/harness/world.ts`) takes **~14 positional params** (one per preference seed; the Aurora workstream appended `ambientStyleSeed` as the latest). Several equities specs already call it as `createWorld(undefined ×9, { … })`, which is exactly the error-prone smell. Refactor to a single options object (`createWorld({ themeSkin, ambientStyle, … })`) so it's order-independent and self-documenting, and adding the next preference is one key rather than a positional + every-caller edit. ~28 call sites across `ui-contract/src/specs/**` + `mount.ts`. Pre-existing (not Aurora-specific); deferred from the Aurora whole-branch review as a self-contained cleanup.
- **Devtools v2 extensions (spec'd, out of v1)** — the devtools workstream shipped, but its merged specs name concrete deferred next steps: presenter-level intent injection + a full intent-name catalogue, a non-dev **token gate** to enable injection in staging behind a provisioned secret ([intent-injection design](superpowers/specs/2026-07-15-devtools-intent-injection-design.md)), and **full app replay** of recorded port inputs into a fresh composition root (needs seeded clocks/timers + deterministic ports; [record-replay design](superpowers/specs/2026-07-15-devtools-record-replay-design.md)). Designed-for, none built.
- **Classic-skin fonts are host-environment-sensitive in CT visual crops** — classic is the only theme skin whose font tokens resolve to OS-generic keywords (`system-ui`/`ui-monospace`) rather than embedded `@fontsource` faces, so element-cropped screenshots of text-width-driven surfaces vary with the runner's system fonts — and GitHub-hosted runners resolve them NON-DETERMINISTICALLY across runs (observed 2026-07-17: react's own CT tier rendered classic chrome/header 1218px in three runs and 1177px in another, flaking against its own goldens; likely since the ubuntu24 runner-image rollout). Surfaced by the SolidJS-port Phase 4 assert-only tiers (PR #230), which skip 4 classic CT assertions ON CI ONLY in solid's ct-fallback tier (still verified by the other two x86 tiers and all darwin tiers). Real fix: back classic's font tokens with a deterministic embedded face in the VISUAL HARNESS (app appearance unchanged for users) — or app-wide as a design change — then regen classic goldens across tiers/platforms. Until then react's post-merge visual.yml can flake on classic CT crops.
- **Auth Phase 2 — per-user server state isolation** — not yet spec'd. Phase 1 (login + genuine server-side auth) shipped PR #210; the RN login screen + [authentication.md](authentication.md) shipped PR #226; the three deferred follow-ups (client-tracked server `exp`, RN AsyncStorage session persistence, RN sign-out control) shipped 2026-07-17. Phase 2 would add a per-connection `ctx` keyed by the authenticated user — per-user blotter/orders/positions with shared hot market-data streams — replacing today's process-wide shared simulator singletons. Phase 1's signed token + `AuthPresenter` are the seam.
  - **Blocked (not actionable as-is):** `gatewayUnauthorized → auto-logout` — when a session token expires mid-session the WS reconnect fails its `?access=` upgrade check, but a browser cannot observe the 401 on a WebSocket *upgrade* rejection (the handshake just fails opaquely), so the client can't distinguish "server asleep" from "token rejected" and auto-return to the login screen. No clean fix without a protocol change (e.g. a server close-code or a pre-connect token-validity ping). Today an expired session surfaces as a stuck reconnect until the user manually signs out; the stored-session `exp` check (PR #234) bounds how long a stale token is even offered. **Narrowed by the auth-gated transport (PR #306):** a never-authenticated client no longer connects at all, so an opaque reconnect loop now unambiguously means "authenticated token went stale / server unreachable" rather than "never had a token" — only the mid-session-expiry half remains blocked on a protocol signal. Server-side the two are now distinguishable in the logs (`upgrade-reject reason=no-token` vs `invalid-token`, see the observability item below), which is a diagnosis aid but not a client-side fix.
- **`pnpm dev` → `dev:web` with feature-flag client selection** — `pnpm dev` is currently a plain alias of `dev:react` (React web client, simulator mode). The intended end state is to rename it to `dev:web` and have it pick the web client implementation (React vs Solid) from a feature flag, while `dev:react` / `dev:solid` remain the explicit per-implementation entry points. Documented in [CLAUDE.md](../CLAUDE.md) (Build Commands → "Future direction"); not spec'd or built. The SolidJS-port workstream has now reached full client parity (contract + visual + e2e), so this item is unblocked — it awaits only its own feature-flags spec (see the Feature flags item above).
- **Power-saver Freeze visuals on React Native** — the three-state power-saver ladder (Off/Calm/Freeze) shipped for React + Solid (design: [superpowers/specs/2026-07-18-power-saver-freeze-tier-design.md](superpowers/specs/2026-07-18-power-saver-freeze-tier-design.md); docs: [power-saver-mode.md](power-saver-mode.md)). RN has the `PowerSaverLevel` enum plumbed end-to-end (preference, persistence, presenter) but renders Freeze the same as Calm today — no CSS-catch-all equivalent or RN-side motion gating yet. Tracked as a follow-up in the [RN mobile-v1 rehaul spec](superpowers/specs/2026-07-16-rn-mobile-v1-rehaul-design.md) §4.
- **Freeze visual goldens — the full-matrix cross-product stays parked (deliberate, non-gating).** A frozen UI’s static end-states are already covered by the existing goldens plus the assert-only `@rtc/ui-contract` tier (`shell/power/`), and Playwright e2e proves `data-power-saver="freeze"` neutralises real animations in a browser, so cross-producting Freeze across the whole scenario set stays low-value. The lock-screen counterparts (`lock/wait-*-freeze`) are judged redundant for the same reason — same treatment components, same freeze CSS. Everything else once tracked here has landed: the `animation-delay` + `backwards` root cause, the harness-fidelity fix, and why the witness must assert computed style rather than pixels are all documented in [power-saver-mode.md](power-saver-mode.md).
- **Coverage-gap sweep — the long tail that remains.** Driven off a freshly dispatched `coverage-report.yml` run (the report is dispatch-only, so it had been 10 days / 48 commits stale — see the "Published Reports Are Stale By Default" section in `CLAUDE.md`). Closed in that sweep: `client-react` app tier **87.78% → 95.55%** (`buildBrowserPorts.ts` 50% → 93.75% — the entire ws-real branch plus `parseDevAuth`'s degrade paths were untested, including the `autoConnect: false` pre-login gate that shipped as a regression once); `ui-contract` **98.45% → 99.14%** (`appHeadRegistry.tsx` 0% → 100%, asserting panel-id→head component *identity*, which is invisible to any rendering test since every head looks plausible); `domain` **96.09% → 96.86%** (`EventLogSimulator`'s 500ms generator + `errorBurst` perturbation); `server` **96.48% → 97.65%** (`parseAuthUsers` rejection paths). **The domain/server tail is now CLOSED (2026-07-26).** `server` **96.48% → 100.00%** (`token.ts`'s payload-shape and non-JSON guards, reachable only by forging a token that is *correctly signed* but structurally wrong; `AuthService`'s roster-miss fail-closed path and its default wall clock; `connectionLog`'s default clock; `credit.effects`'s workflow-SoW switch arm — the one arm that drops `payload` entirely). `domain` **96.86% → 99.44%** (`preferences`' power-saver cycle + guard, `ErrorRateSimulator`'s live tick past the 60-sample seeded history and `clearPerturbation`, `ServiceTopologySimulator`'s incident *recovery*, `EquityPositionSimulator`'s sell-relieves-at-average rule / once-per-symbol marking / flat-position drop, `EquityMarketDataSimulator`'s three unknown-symbol errors, `EquityOrderSimulator.cancel`, and `CreditRfqSimulator`'s two "the RFQ was cancelled while this timer was in flight" arms plus its stale-quoteId guard). `__testUtils__/**` is now EXCLUDED from the domain denominator rather than tested — same reasoning as the existing `__contracts__` exclude (asserting that a test helper throws is measuring the ruler). **One gap was DEAD CODE and is now deleted:** `aggregatePositions`'s `if (span === 0) return POSITION_MIN_RADIUS` could never fire — `scaleRadius` only runs over a non-empty `data` (zero amounts are filtered out first), and the preceding `minValue = rawMin !== maxValue ? rawMin : 0` collapses `minValue` to 0 exactly when every magnitude is equal, so `span > 0` always. It even contradicted live behaviour: the all-equal case yields MAX radius, not the MIN the dead guard promised. (Holds for `NaN` too — `NaN` passes the `!== 0` filter but `NaN === 0` is false.) The invariant that makes the removal safe was *already* pinned by an existing spec, "assigns max radius to all currencies when all magnitudes are equal (minValue collapses to 0)". File now 100%. **That "5 unreachable statements" claim was too broad — corrected 2026-07-26, and two of the five are now resolved.** `CreditRfqSimulator:197` (`if (!dealer) continue`) is in fact **reachable from the public API**: `dealerIds` is caller-supplied and nothing validates it against the catalogue, so an unknown id must degrade to "no quote from that dealer" — now tested (`CreditRfqSimulator.unknownDealer.test.ts`). `:212` (`throw "Internal: no quote list"`) was genuinely dead — `rfqQuotes.set(rfqId, [])` runs ~25 lines above in the same synchronous block — and existed only to narrow `Map.get`'s `T | undefined`; **deleted** by holding the array by reference instead of re-reading it. Domain 99.44% → 99.66%. **The remaining 3 stay uncovered on purpose:** `:375` is dead for the same map-consistency reason but is load-bearing for type narrowing (removing it needs a `!` assertion, which is worse than the dead branch); `:283` catches an `applyQuote` that never throws, but a catch around a `setTimeout` callback is a legitimate safety net — an uncaught throw there takes down the process; `EquityOrderSimulator:98` forwards an error from a source that never errors, the idiomatic Observable passthrough. **Each of those three now carries an in-code comment stating why it is unreachable and ending "Expect it to show as uncovered"** (2026-07-26), so the next coverage sweep re-derives the verdict from the code rather than from this entry — a reason recorded only in STATUS.md is a reason the next reader of the file will not find. **The client-app tail is CLOSED too (2026-07-26): 95.56% → 98.33%.** `LocalStorageSessionStore` 86% → 100% (valid-JSON-but-not-a-session inputs, which reach the shape guards that `"{not json"` never does — the difference between rejecting a corrupt entry and booting with a session whose `user` is undefined); `MediaQueryColorSchemeAdapter` 83% → 100% (the OS change listener — without it "system" theme seeds right at boot and then never follows the OS again, a bug invisible at startup — plus the no-matchMedia fallback); `devtoolsHub` 83% → 100% (`pagehide` disposal, and the two environment guards that keep the module importable where `BroadcastChannel`/`window` are absent — the node fullstack smoke imports it exactly that way, which needs `vi.resetModules()` + dynamic import since the wiring is an import side effect). **`buildBrowserPorts.ts` is now CLOSED too: 93.8% → 100% (zero uncovered statements AND branches), tier 98.33% → 99.44%.** The residue was callback bodies that composition-time assertions can never reach, because each one runs only when something later *drives* it: the token supplier (invoked by `WsAdapter.openSocket`, which the auth gate triggers after login — so the very pin that asserts no eager connect is what leaves it unexercised), the idle-lifecycle `tap` (runs only while something is subscribed to `connectionEvents.events()`), and the simulator branch's `browserOnline` ternary (needs a real `window` online/offline dispatch, which no test did). Each is now driven the way the app drives it: `transport.connect()` with a stubbed `WebSocket` recording connect URLs, `incident$`/`reconnect$` pushed through a live subscription, and `window.dispatchEvent(new Event("online"|"offline"))`. Two of the new assertions are behavioural rather than coverage-shaped and were falsification-verified: baking the token in at composition time (instead of re-reading per connect) fails the freshness test — the real bug would be a rejected upgrade after re-login; and deleting the `tap` fails the routing test — the real bug would be idle disconnects never reaching `closeForIdle()`. **The `ui-contract` tail is now CLOSED too (2026-07-26) — react 99.14% → 99.30%, solid 99.56% → 99.62%.** It was a tail of 1–3 statements each across ~12 files, and reading all twelve rather than sampling them changed the verdict on every one of the four that looked like real behaviour. Only **one** was: `CreditBlotter`'s `next.delete(column)` arm, reached when a set filter is re-selected to all-inclusive — the filter panel is built from the UNFILTERED rows, so both values are still on offer, and `SetFilter` applies `null` rather than an all-pass filter. Keeping an all-pass entry instead of deleting it would leave the "Filtered: …" summary claiming a filter the user just removed. Now pinned by a shared contract spec (so it covers BOTH clients), mutation-verified: stubbing out the `delete` fails exactly one test — the new one, which is what proves the arm wasn't already covered incidentally. The other three were **not** behaviour, and two of them are worth recording because the coverage gap was pointing at something real that wasn't the missing test: (1) `TileConfirmation`'s `default: return "unknown"` was **actively harmful and is DELETED** — with all four `ExecutionStatus` members cased and no `default`, TypeScript narrows `"finished"` out of the trailing `return state.status`, so a *fifth* enum member becomes a compile error (verified by temporarily adding one: `TS2322` at the return). The `default` arm absorbed that check and would have silently rendered any new status as "unknown". A compile error beats a runtime fallback nobody would notice, so this is one of the rare cases where deleting dominates testing. (2) `NewRfqPanel`'s `if (!canSubmit || !selectedInstrument) return` is unreachable — the SEND button carries `disabled={!canSubmit}`, which the three "keeps SEND RFQ disabled until …" specs already pin — but it is load-bearing for null-narrowing (`tsc` errors `TS18047` without it), so deleting it only trades the line for a `!` assertion. Left in place with a comment saying so. (3) `creditBlotterColumns`'s malformed-`tradeDate` guard is unreachable through `creditTradesVm` (which always produces a valid ISO slice) but its FX sibling `formatFxCell` has had exactly this test since the beginning; a colocated `creditBlotterColumns.test.ts` now mirrors it in both clients, per the existing duplicated-per-client convention for pure formatters. The remaining 8 statements — null-ref guards, `cancelAnimationFrame` cleanup, `pointerId` match, double-`animationend`, the jsdom `setPointerCapture` guard — stay uncovered, deliberately. **Method note worth keeping:** the four "worthwhile" items were scoped from a quick read and three of the four did not survive close reading. Scoping a coverage tail by skimming line numbers systematically over-counts real behaviour, because an uncovered line looks identical whether it is untested behaviour, a type-narrowing guard, or a fallback that should not exist. **The report now measures BOTH web clients (fixed in this branch).** An earlier draft of this entry claimed Solid's coverage was "unknown outside its own CI gate" — that was wrong and worth correcting precisely, because the real failure mode is subtler. The shared `@rtc/ui-contract` specs DO run against `client-solid`, and `ci.yml` gates it at the same >=95% bar (branches >=85%, since Solid's compiled reactive-effect guards inflate v8's branch denominator). The problem was never that Solid was ungated; it is that **a gate asserts an AGGREGATE and therefore cannot surface a single weak file**. Solid sat at 99.35% overall while `appHeadRegistry.tsx` was 69.23% and `appPanelRegistry.tsx` 56.25% — the exact mechanism that let react's `appHeadRegistry` sit at 0% inside a *passing* 98.45% tier. Worse, Solid had **no app-tier coverage config at all**, so `buildBrowserPorts.ts` was never measured and was sitting at 50%/30% branches (uncovered lines 40-54, 74-113 — the whole ws-real branch), the identical gap its react twin had. Closed here: added `packages/client-solid/vitest.app.coverage.config.ts` + a `test:app:coverage` script, mirrored the ws-branch tests (solid app **87.77% -> 95.55%**, `buildBrowserPorts` 50% -> 93.75%), and wired two Solid tiers into `coverage-report.yml` (`solid/app`, `solid/ui (contract)`) with framework-prefixed tier names. React slugs are unchanged so existing report URLs still resolve. **Solid's registries are now CLOSED (2026-07-26): `appHeadRegistry.tsx` 69.23% → 100%, `appPanelRegistry.tsx` 56.25% → 100%, tier 99.35% → 99.56%** — and the presumed "new seam" turned out not to be needed. The earlier sizing said React's identity trick "does not port to Solid", because React thunks return inert element *descriptors* (`element.type` IS the component) while Solid's JSX compiles `<Head />` to `createComponent(Head, {})`, which CALLS the component. True — but the seam already exists one level down: **`vi.mock`ing `solid-js/web`'s `createComponent`**, the exact helper the transform emits, hands back the component reference untouched. That recovers React's cheap identity assertion with no production change and no ViewModel provider, and it is the same kind of coupling react's test already takes on `element.type`. Falsification-verified: pointing `eq-blotter` at `WatchlistPanel` fails both the per-id identity assertion and the all-distinct assertion. Worth recording *why* the two frameworks diverged in coverage at all: react's `appPanelRegistry` reaches 100% from the shared contract specs alone because React evaluates `registry[panelId]?.()` eagerly during render, whereas Solid's fine-grained JSX defers it — so panels the specs never reveal left their thunk uncalled. **That also means react's 100% there is incidental and proves nothing about wiring** — a swapped pair would still pass. A react `appPanelRegistry` identity test (same shape as its head twin) is the remaining symmetric gap; not written, since react's number already reads 100% and nothing surfaces it. **Correction (same day):** an earlier version of this entry called the `ui-visual` tier's 77.03% *incidental* coverage and suggested dropping it. That was wrong, and the mistake is instructive — the tier's NAME invites it. `ui-visual` is not the pixel tier's own coverage. It is a purpose-built vitest-browser instrument that walks **the same shared scenario matrix** as the playwright golden tier (`@ui-visual-shared/scenarios`) with the pixel assert compiled out via `__RTC_VISUAL_SKIP_DIFF__`, so istanbul sees exactly what the visual scenarios render. Its number answers *"how much of `src/ui` does the golden matrix actually exercise?"* — i.e. it is the guarantee that the pixel tier is not silently testing less than we think. It has a documented track record: `EqDepthDock` read 0% here, which is why the `equities/depth-dock-empty` scenario exists (see `packages/client-react/tests/ui/visual/COVERAGE-GAPS.md`). So the 77.03% should be READ as "~23% of `src/ui` is never rendered by any golden scenario" and worked down by adding scenarios — not dropped. The only fair criticism is the label: `ui-visual` reads like "the visual tier's coverage"; something like `visual-scenario-reach` would not have misled a reader (or an agent) into dismissing it. **Both of these are now DONE (2026-07-26).** The tiers are renamed `react/ui (visual reach)` / `solid/ui (visual reach)` — display name only; the URL slugs stay `ui-visual` / `solid-ui-visual` so previously-shared report links keep resolving. And client-solid now HAS a `tests/ui/visual/vitest-browser/` harness (spec + 2 configs, mirroring react's), so the report covers **eight** tiers with the two clients symmetric; a new assertion in `coverage-report.smoke.test.ts` pins that symmetry, since the old "both clients are present" check passed while solid was missing a whole instrument. First run: **1342/1342 scenarios, 85.43% statements** (react's reads 77.03%). **Do not compare those two numbers directly** — each denominator is its own compiled `src/ui`, and Solid's compiler emits a different statement count for byte-equivalent JSX; the comparable signal is which FILES sit at 0% on one side only. Two implementation notes: `client-solid` needed `@vitest/coverage-istanbul` added explicitly (browser mode requires istanbul, and its absence produced a **green 1342/1342 run reporting 0% coverage** — a passing tier measuring nothing); and the solid tier carries no `__RTC_VISUAL_SKIP_DIFF__` flag, because unlike react it never had a pixel-assert role to compile out. **2026-07-26 — the packages the published report cannot see.** `pnpm coverage:gaps` (added #365, fixed #376) measures **12** tiers locally; at the time, the gh-pages report covered **8** and included none of `boot-splash`, `client-core`, `devtools-core`, `motion-core`, `ws-effects` or `devtools-relay` — so a gap in those was invisible there no matter how fresh the report was. (**Updated 2026-08-29:** the report now has **ten** tiers, including `devtools/core` and `devtools/app`; `boot-splash`, `client-core`, `motion-core`, `ws-effects` and `devtools-relay` remain unreported.) Three sweeps had polished the *reported* tiers (domain 99.66%, server 100%, react contract 99.30%) while `client-core` sat at 93.18% and `devtools-core` at 91.30%. Closed here: `client-core` **93.18% → 95.98%** — `portFactory.ts` 83.1% → 92.83% (the ws-real candles/place/cancel RPC paths: nack handling, the `cancelled` guard, the order-id filter on the lifecycle stream, and completion on filled/cancelled/rejected — all silent from the UI's side) and `composition.ts` 78.9% → 89.83% (`createMachineFactories`, a wiring table where `staleFlag` and `analyticsStaleFlag` differ only in which stream they read — the `appHeadRegistry` shape again). `src/adapters/__tests__/**` is now excluded from client-core's denominator: `FakeWsAdapter` (82.9%) and `awaitPendingRpc` (71.4%) are scaffolding, and closing them would mean testing the fake rather than the thing it fakes. `boot-splash` 0.29% → 100% separately in #379 (canvas drawing excluded with the reason in-file; its pure `hexToRgba`/`ease` helpers tested instead). **`devtools-core` closed 2026-07-26: 91.33% → 94.77%** (89 → 118 tests). The gaps were all bounds, guards and containment — the branches a happy-path suite never creates: serialize's Map/Set/object truncation caps plus bigint/symbol and the throwing-getter envelope; parseRecording's three malformed-file guards; diff's entry cap and array-shrink branch; the duplex's post-dispose drop; WsRelayDuplex's no-global-WebSocket error; and the instrumentation's fail-safe paths — a hub that throws on registration must hand back the RAW machine, and a circular presenter argument must not throw inside the app. On the hub itself, dormancy is now shown to be REVERSIBLE (bye releases the sources, a re-hello resends welcome + snapshot) rather than only true of the first attach. `serialize`, `recording`, `diff`, `channel`, `projectSnapshot` and `instrument/machines` are at 100%. **Still open (small, diminishing returns):** `InspectorStore` 96.9%, `LiveHistory` 95.3%, `Recorder` 96.4%. (`DevtoolsHub.ts` and `WsRelayDuplex.ts` were closed by the 2026-08-29 devtools-follow-ups sweep — 100% stmts / 98.64% branch and 98.21% stmts / 95% branch respectively.) Also open: `motion-core/reducedMotion.ts` reads 0% but is a single exported query-string constant — a test would copy the literal and assert it equals itself, so it is left alone deliberately.
- **Power-saver: instant refresh on toggle** — after the `conflateWhen` resubscribe fix, flipping power saver *on* no longer emits an instant "leading" replay of the current price — the first throttled emission waits for the next real tick. Arguably more correct, but if a future UX wants an immediate refresh on toggle it would need an explicit replay-on-flip. Detail in [power-saver-mode.md](power-saver-mode.md) → "Future iterations".
- **LiveRates sparkline-retention e2e guard** — the "filter a pair out then back in keeps its accumulated chart" fix (PR #242) is covered by `PriceHistoryPresenter` unit tests (retention + first-frame seed), but there's no DOM-level regression test driving the actual filter toggle. A small Playwright case in `tests/browser/playwright/fxLiveRates.spec.ts` asserting the sparkline point-count survives a filter-out/in cycle would lock in the reported behaviour end-to-end. Not spec'd; low priority (the presenter tests guard the seam).
- **Solid first-frame paint of retained sparkline (unverified)** — the retention fix lives in shared `client-core`, so all clients keep the accumulated buffer; the ~10ms first-frame *paint* on remount (via the outermost `startWith` seed) was verified in-browser on React (react-rxjs) but not on `@rtc/client-solid`, whose `solid-bindings` surface synchronous on-subscribe values differently. Correctness holds either way (buffer is retained); only the "instant vs one-tick" nicety is unconfirmed on Solid. A quick browser check would close it. Not spec'd.
- **Boot splash ordering vs login (UX decision — needs brainstorm).** Today the boot splash plays *before* the login screen. Question: should we log in first, then play the splash → app? Touches the same pre-auth boot sequence as the now-shipped auth-gated WS transport (PR #306), which no longer connects until sign-in — so the splash is the only remaining pre-auth work. No decision yet — brainstorm the intended order (and whether it differs per platform), decide, then implement if the decision changes current behaviour.
- **Single container-canonical golden set — revisit ONLY if a fast native quick-loop is preserved first.** The collapse to one `react/` set (delete `react-local/<arch>`, configs → constant `react`) was implemented and merged (#272/#273/#274) then **reverted** (#275): it destroyed the fast native inner loop — `pnpm test:ui:visual` went from instant-and-passing to failing on any non-x86 host, and the only *passing* local check became `pnpm goldens:verify` (Docker + ~1–2 min container overhead per run). Losing instant native feedback was not worth deduplicating goldens. **The 3-bucket dual-set (`react/` + `react-local/{darwin,linux}-arm64`) is retained deliberately.** The Phase-1 efficiency wins were KEPT (wrapper #265, `scenario_pattern` filter + auto-commit #269, image-tag drift guard #270). Only revisit the single-set idea if a *fast native quick-loop* is preserved first (keep `react-local`, or add a non-committed native "directional" mode with the container as the commit gate). Container byte-identity to CI x86 is proven (spec [superpowers/specs/2026-07-18-single-container-golden-set-design.md](superpowers/specs/2026-07-18-single-container-golden-set-design.md)) — the mechanism works; the workflow-loss is the blocker.
- **Login-wait feedback — React Native UI port pending.** The web clients (`client-react`, `client-solid`) now show one of two alternating "waiting for the server" treatments during a sign-in or unlock request — `HandshakeConsole` (monospace telemetry readout) and `ReactorWait` (counter-rotating arcs + indeterminate bar around the hex emblem) — alternated via a persisted `LoginWaitVariant` round-robin (`rt-login-wait-variant`, the same pattern as the boot-splash scene rotation), plus a `LockScreen` unlock in-flight state (`unlocking: boolean` on `AuthViewState`). **RN is UI-only outstanding:** the web implementation is CSS keyframes, none of which transfers — RN needs a Reanimated (and possibly Skia) implementation of both variants, touching `packages/client-react-native/src/ui/shell/auth/LoginScreen.tsx`, `packages/client-react-native/src/ui/shell/lock/LockScreen.tsx`, and `packages/client-react-native/src/ui/shell/lock/useHoldToUnlock.ts`. The RN **preferences adapter already ships** (`AsyncStoragePreferencesAdapter`, with its own hand-written tests) — don't redo it, only the UI remains. **Trap for whoever picks this up:** `PreferencesPortContract` does not run against the RN adapter (its consumers are `client-react`, `client-solid`, and the domain simulator only), so RN preference code needs hand-written tests — this cost a review cycle on this branch. **Also note:** five existing RN test fakes construct `AuthViewState`/`ViewModel` literals lacking `unlocking` and `waitVariant` — `AuthGate.test.tsx:58-64`, `auth/LoginScreen.test.tsx:83`, `auth/LogoutButton.test.tsx:23`, `lock/LockScreen.test.tsx:157` and `:183`, `lock/LockButton.test.tsx:23` — and still typecheck only because each ends in `as unknown as ViewModel`, a double cast that defeats even the structural-literal fix applied to the two bindings packages (the same `as T` masking pattern noted elsewhere in this file). Harmless while the RN UI is deferred, but they'll need updating alongside the real implementation. No RN plan file yet. Spec: [superpowers/specs/2026-07-25-login-wait-feedback-design.md](superpowers/specs/2026-07-25-login-wait-feedback-design.md).
- **RN preference-adapter tests assert pre-hydration state, not guard behaviour.** In [`AsyncStoragePreferencesAdapter.test.ts`](../packages/client-react-native/src/app/adapters/AsyncStoragePreferencesAdapter.test.ts)'s "falls back to the default when the stored value is invalid" cases for `bootVariant` and `themeMode`, the test subscribes synchronously at construction, so it captures the `BehaviorSubject`'s pre-hydration default regardless of whether `hydrate()`'s invalid-value guard ever ran against the stored value — the assertion passes identically whether the guard is correct or entirely absent. The equivalent `loginWaitVariant` case was fixed on the login-wait-feedback branch (it awaits hydration via `vi.waitFor` before asserting); these two pre-existing peers were deliberately left as out of scope for that branch. This matters more than usual for this adapter because `hydrate()` reads from a positional `Promise.all`, where a misaligned index would silently assign the wrong stored value to the wrong preference — and neither of these two tests would notice. Not spec'd.
- **`as T` assertions can silently mask a missing required field at a construction site.** On the login-wait-feedback branch, adding a required field to `AuthViewState` did not fail `pnpm typecheck` at two production sites — [`createViewModel.ts`](../packages/react-bindings/src/createViewModel.ts) and [`createViewModel.ts`](../packages/solid-bindings/src/createViewModel.ts) (react-bindings and solid-bindings respectively) — because each ended its default-state object literal with `as AuthViewState`. A type assertion only requires the two types to sufficiently overlap, and a narrower literal missing one property still overlaps the wider interface, so TypeScript accepted it silently. Both assertions were deleted on that branch (replaced with a direct structural literal, which does catch the omission there), but the same pattern may exist at other construction sites across the codebase, and wherever it does, a green `typecheck` is not proof every site was updated when a required field is added. Worth a repo-wide survey and possibly a lint rule or grep-gate; not attempted, not spec'd.
- **Blank line between adjacent multi-line "block" statements — PARKED (investigated, no rule adopted).** Spun off from the `rtc/newspaper-order` extension (PR #287) that now pushes `jest.mock`/`jest.unmock` below the tests like `vi.mock` (both are hoisted above imports by their runners, so position is behaviour-neutral). The newspaper autofix already *separates* the mocks it moves (it joins them with a blank line), but there is **no rule that enforces a blank line between adjacent multi-line blocks in general** — e.g. two `jest.mock(…, () => {…})` factories authored already-at-the-bottom, packed with no separator. Goal was a *generic, size-based* rule ("if a statement spans more than ~2–3 lines, separate it"), **not** an identifier-based mock-only one. Findings: **(1) Biome cannot do it** — the maintainers deliberately don't implement `padding-line-between-statements`; blank-line *insertion* is classed as formatting, and the Biome formatter only preserves/collapses blank lines, never inserts required ones ([biomejs/biome#3016](https://github.com/biomejs/biome/discussions/3016)). **(2) ESLint can, via the existing `padding-line-between-statements`** (already in `eslint.config.mjs`): the `multiline-expression` selector matches exactly the missing case — multi-line call statements ending in `)` (like `jest.mock(…)` / `describe(…)`) that the current `multiline-block-like` clause misses because they don't end in `}`. Config would be one clause: `{ blankLine: "always", prev: "multiline-expression", next: "multiline-expression" }`. **(3) But its blast radius is large and imprecise** — measured on the repo it flags **350 spots across 152 files** (120 test, 32 production), all autofixable, with two false-positive modes: **(a)** simple statements that merely *wrapped* because they were long (e.g. a 3-line `expect(headers.get("CT")).toBe("application/json")`) get split from the next assertion — same spirit as the "don't separate one-liners" objection; **(b)** production RxJS assignment chains (`this.x$ = src.pipe(…)`) get separated field-by-field. Root cause: no built-in selector expresses *"a call whose **argument** is a real block / object-literal body"* — `multiline-block-like` keys on a trailing `}`, `multiline-expression` on any 2+-line span; the granularity is binary (single- vs multi-line), with no line-count threshold. **Options if revived:** (i) accept the generic `multiline-expression` clause (repo-wide, or scoped to `**/*.{spec,test}.{ts,tsx}` to spare production RxJS) and live with the wrapped-statement noise; (ii) write a small autofixable custom `rtc/` rule that fires only between multi-line statements whose body is an actual block/object-literal (exempting merely-wrapped ones) — precise, matches the "block" intent, but new code to maintain; (iii) do nothing and rely on the newspaper-order autofix for the common (moved-mock) case. **Parked** until it recurs / annoys enough to justify option (i) or (ii).
- **ViewModel seam defeats React Compiler.** Measured 2026-07-26: 118 of `client-react`'s 123 compiler bails are one diagnostic, and every one is the ViewModel seam (`const { useX } = useViewModel()`) — the compiler requires *static* hook identity, the seam ([ADR-004](adr/ADR-004-viewmodel-seam-and-feature-flags.md)) supplies hooks dynamically for DI/swap-trio/framework-replaceability, and the two are mutually exclusive by construction (verified exhaustively: destructuring, member-calling, and reading the bundle from `useContext` all bail; only statically-imported hooks compile). Fixing it would touch ADR-004, both bindings packages (`react-bindings`, `solid-bindings`), the `@rtc/ui-contract` swap-trio, and every UI file reading a hook off the seam — deliberately deferred, not attempted here. See [ADR-003's measured-coverage section](adr/ADR-003-react-compiler-and-manual-memoization.md#measured-coverage-2026-07-26) and [superpowers/specs/2026-07-26-manual-memoization-ban-design.md](superpowers/specs/2026-07-26-manual-memoization-ban-design.md).
- **Revisit `useHoldToUnlock` with a clean architecture.** It is the one exception to the repo's memo ban (`packages/client-react-native/src/ui/shell/lock/useHoldToUnlock.ts`, file-scoped `no-restricted-imports` lint exemption), and the exception is a symptom rather than a design: the gesture needs a stable identity the compiler cannot give it, because `runOnJS(fireComplete)()` closing over a ref bails the hook, and because dropping the ref instead makes the memo's key `onComplete` — a callback declared in `LockScreen`, which bails on the ViewModel seam, so `onComplete` churns every render regardless. A clean solution (a stable-callback seam, building the gesture outside the render path, or a `SharedValue`-mediated completion signal) was not attempted here — it is a design change, not a cleanup. See the hook's header comment, [ADR-003](adr/ADR-003-react-compiler-and-manual-memoization.md#measured-coverage-2026-07-26), and Task 3 of [superpowers/specs/2026-07-26-manual-memoization-ban-design.md](superpowers/specs/2026-07-26-manual-memoization-ban-design.md).
- **`client-prototype` retains 34 manual memo sites (explicitly descoped).** It is the deliberately-isolated readable port of the v2 design prototype (`react`/`react-dom` only, no `@rtc/*` imports, no compiler) — churning it to match the compiler ban works against its purpose as a faithful, framework-minimal reference port. Revisit only if it stops being a faithful port. See [superpowers/specs/2026-07-26-manual-memoization-ban-design.md](superpowers/specs/2026-07-26-manual-memoization-ban-design.md) (Non-goals).
- **MCP SDK 2.0 — revisit at GA (decided 2026-08-02, no action until then).** `@modelcontextprotocol/sdk` is pinned `^1.30.0` (npm `latest`; no beta dist-tag exists — 2.0 is an unpublished alpha on the SDK repo's main). Assessed: v2 brings web-standard transports + package split + handler factories — ergonomics/portability, not capability; nothing our `/mcp` endpoint lacks, and MCP clients speak the protocol, not the SDK. Upgrade path is deliberately cheap (two files behind our own seam; SDK-free registry) — see the SDK-version-posture note in [architecture/18-jarvis-ai-agent-surface.md §18.14](architecture/18-jarvis-ai-agent-surface.md#sdk-version-posture-assessed-2026-08-02). Re-evaluate when a GA (or at least npm-tagged beta) ships.
- **Jarvis discoverability round deferred findings (SHIPPED 2026-08-09 — spec: [superpowers/specs/2026-08-09-jarvis-discoverability-design.md](superpowers/specs/2026-08-09-jarvis-discoverability-design.md), plan: [superpowers/plans/2026-08-09-jarvis-discoverability.md](superpowers/plans/2026-08-09-jarvis-discoverability.md)).** The round shipped the shared guide catalog (chips/guide/demo single source, intent-conformance gated), the ⓘ demo-guide panel + footer demo controls in both web clients, rotating seeded hint chips (seed = the new `JarvisState.openCount`), the `JarvisDemoMachine` hands-free demo (always-scripted via per-turn `sendScripted` — works even mid-budget-gate; auto-declined trade; 30s availability watchdog), and the persona per-tab panel-id roster + FX worked example (guard raised 3000→3600, deliberate) closing the P5 drive-targeting prompt-side findings. Final review SHIP with zero blockers; stays-deferred with rulings: `sampleGuideChips` latent divide-by-zero if a section ever goes all-liveOnly (conformance CI would redden loudly first); chip specs use the sampler as oracle rather than literal seed pins (mutation-kill proven, adequate); the e2e `DEMO_STEP_TIMEOUT_MS` const name-collides with the machine's at a different value; `turnHasStarted` could theoretically match a user-typed identical command mid-demo (UI halt wiring closes the practical window); third `readLatest` copy; no bindings-level `useJarvisDemo` test (matches `useJarvisDriver` precedent); "maximise/maximize" UK-prose/US-op-id mix in the persona FX example (arguably correct). RN surfaces for guide/demo/chips stay on the RN Jarvis backlog. The correction-signal wire gap (client-side skipped drive outcomes never reach the model) remains ledgered under the P5 findings entry above — prompt-side fixed this round, wire-side still open.
- **Jarvis generative UI — round-1 follow-ups (captured 2026-08-05; round 1 SHIPPED 2026-08-05, PR #488 — receipt [architecture §18.16](architecture/18-jarvis-ai-agent-surface.md#1816-the-generative-ui-surface-round-1); real-key live smoke green 2026-08-08).** Roadmap items deferred by design (spec §7): **L3** docking/persistence — **SHIPPED 2026-08-11** on the in-house engine (the "Dockview first" gate in ADR-002 was found stale, since it predates the shipped split-tree engine; ADR-002 rewritten in-round, status → Superseded in part, [adr/ADR-002-layout-management-port.md](adr/ADR-002-layout-management-port.md) — note the separate Dockview round above landed concurrently, so Dockview *does* now exist as an opt-in engine; docked Jarvis panels remain an **in-house-engine** feature, since the dockview seed tree is still the static default): a Jarvis-generated panel docks into the active tab's rightmost column as an ordinary tile, the whole per-tab workspace (splits + docked panels) survives reload via one opaque `workspaceLayoutV1` domain preference, drive gains `dockPanel`/`undockPanel`, and a Preferences "Reset workspace layout" row restores defaults. Spec: [superpowers/specs/2026-08-11-genui-l3-pinned-panels-design.md](superpowers/specs/2026-08-11-genui-l3-pinned-panels-design.md) (implementation addendum records the plan-time deviations and in-round rulings). Two durable residuals from the round: **driver `dockPanel` reports `"applied"` for a static-id collision while composition silently no-ops** — needs a 5th `DriveCommand` skip reason in a future round (this round's result type pinned exactly 4); **a genuine dock/undock remounts every mounted leaf in the affected Solid tab** — an accepted membership-change re-key (Solid has no VDOM reconciliation over the id-set-keyed registry memo), disclosed and accepted at review, not a regression path. **L4** multi-panel dashboards + linked crosshair/time-axis/click-through interactions; **RN panel rendering** (needs its own Skia render path per renderer, the same class of work as the RN boot-scene ports); **drag-repositioning** (still out of scope — L3's own spec deferred user drag-rearrange to a future round, in-house extension or Dockview adoption per the rewritten ADR-002's honest costing). Plus the round's own deferred findings, none blocking merge: **timers grep gate matches comment text, a gate defect not an instance** — the `src/ui` no-`setTimeout` grep gate false-positived on a *doc comment* naming `setTimeout` in `JarvisPanelLayer.tsx` (both clients); worked around by rewording the comment, but the gate itself still can't distinguish code from prose, the same class of fragility already logged against gate 21 above. **Panels per-file coverage is a `/rtc:backfill-test-coverage` candidate** — `ui/shell/jarvis/panels` sits at ~76%/59% (react, stmts/branch) and ~86%/70% (solid), well under the aggregate gate everything else clears; `PanelSparkGrid` alone is the worst single file at 56%/25%, mostly the ≥2-point path-drawing branch and the zero-row early-return. **`render_panel` has no `JARVIS_TOOL_FRIENDLY_NAMES` chip entry** — that map's own test asserts an exact 7-entry object derived from `@rtc/agent-tools`, which `render_panel` deliberately isn't part of, so its tool-event chip shows the raw name `"render_panel"` instead of a short label; a one-line follow-up once anything else touches that map. **A hallucinated `targetPanelId` has no correction signal** — an edit turn naming an id the server never minted silently spawns a fresh panel instead of erroring, so a model that hallucinates an id gets no feedback that it did; closing this needs a per-session minted-id `Set` the tool checks against. **The heatmap intensity scale is P&L-calibrated and saturates on `blotter`** — `HEATMAP_INTENSITY_SCALE = 10_000` is deliberately generous for a P&L dollar figure (a desk panel is meant to pop), but a `blotter` heatmap (notional trade sizes, routinely well past $10k) saturates every cell to the same extreme, losing gradation the constant was tuned for. **`JarvisMachine`'s warm `state$` subscription has no error handler** — found while pinning the panels-machine `catchError` guard (§18.16): a synchronously-throwing `JarvisPort.ask()` now leaves `JarvisPanelsMachine` unharmed, but the *same* throw still takes the entire chat session down with an unhandled error, pre-existing and outside this round's file scope — panels now degrade more gracefully than the chat state they're derived from.

- **FX sub-tab / quick-filter promotion (deferred by design, captured 2026-08-05 alongside the P5 🟡 entry above).** `FxViewProvider`'s rates/blotter sub-tabs and quick filter (`packages/client-react/src/ui/fx/FxViewProvider.tsx`, Solid counterpart identical) remain three bare `useState` calls in React (`createSignal` in Solid) — the second such pocket the P5 app-driving round found and *deliberately did not promote*, unlike the workspace-tab pocket in `App.tsx` it did promote to `WorkspaceNavMachine`. Kept as the showcase's "still-unreachable" exhibit ([`docs/showcase/machine-boundary-agent-reach.html`](showcase/machine-boundary-agent-reach.html)): Jarvis cannot drive FX sub-tabs or the quick filter today, on purpose, so the machine boundary stays visible in what the agent can and cannot do. Promoting it would be the same one-line-per-callsite fix as the workspace tab — a `FxViewMachine` singleton + a rewired `FxViewProvider`/`useFxView` — and would extend `DriveCommand`'s vocabulary with the FX sub-tab/filter kinds the P5 spec explicitly scoped out (§1, decision 1). No plan file; pick up when FX-surface app-driving earns its own round.
- **Jarvis P5 deferred findings (SHIPPED 2026-08-06, PR #498 — receipt [architecture §18.17](architecture/18-jarvis-ai-agent-surface.md#1817-app-driving--the-proactive-narrator-p5)); small follow-ups ledgered at review time, none merge-blocking (final-review triage 2026-08-06).** Code: `stripNarrationPrefix` is one-way (an unprefixed `narrate()` caller would silently misroute the scripted brain — today's only caller always prefixes); `evaluateCrossing`'s docstring in `anomalyDetector.ts` is stale (claims mutation; it's pure); RN silently gains the narrator (capped 4/session) with no preference UI and no narrator styling — the one user-visible deferral, belongs on the RN backlog; `drive_app`/`render_panel` lack `JARVIS_TOOL_FRIENDLY_NAMES` entries (raw tool ids in the chat chip, pairs with the R1 deferral); client-core `vitest.config.ts` lacks the `dist/**` exclude its sibling packages have (a stale local build double-counts tests — bit twice during the round). Tests: no conformance pin ties `render_panel` to the persona's `[narration]` bar; the driven-pulse page-object disambiguates carriers by tagName (brittle on a third carrier); narrator e2e rides on a 30s flare wait (the one load-sensitive spot if CI ever flakes there) and the default-on narrator adds a small background chance of an unsolicited narration turn perturbing `lastReplyText` mid-suite. Process: the persona's 3000-char guard is binding at ~3% headroom — the next tool section forces another prose-erosion round unless the guard is raised deliberately. **Drive-targeting findings (observed live 2026-08-09** — "maximise the Live Rates pane" got an affirmative reply and no action): the persona never enumerates the per-tab layout panel ids, so the model must guess that "Live Rates" is `fx-rates` (derivable never-drift from `defaultLayoutPort`, the same pattern the drive schema already uses on its enums); its only worked drive example is equities (`eq-chart`), biasing FX asks; a guessed-wrong `panelId` is rejected client-side as a `skipped` outcome that **never travels back to the model** — the drive-side sibling of the generative-UI entry's `targetPanelId` correction-signal finding — so the model believes it succeeded; and Haiku (the default brain) can acknowledge a drive request conversationally without emitting the `drive_app` call at all (no `drive:` row = no call was made; a `drive:` row with no effect = wrong panelId).


- **Jarvis LLM usage governance + model preferences (captured 2026-08-02; ROUND 1 SHIPPED 2026-08-02, PR #472 — spec: [superpowers/specs/2026-08-02-jarvis-brain-picker-usage-display-design.md](superpowers/specs/2026-08-02-jarvis-brain-picker-usage-display-design.md)).** Round 1 built items **(1)/(3)/(4)** below plus a brain picker with Scripted as a first-class choice, display-only, as designed — full receipt at [architecture §18.15](architecture/18-jarvis-ai-agent-surface.md#1815-the-brain-picker--usage-display-round--the-receipt). **Item (2) auto-gating SHIPPED 2026-08-09 (round 2, PR #505 — spec: [superpowers/specs/2026-08-08-jarvis-usage-auto-gating-design.md](superpowers/specs/2026-08-08-jarvis-usage-auto-gating-design.md), plan: [superpowers/plans/2026-08-08-jarvis-usage-auto-gating.md](superpowers/plans/2026-08-08-jarvis-usage-auto-gating.md)): estimated-USD budget on the 5h `UsageMeter` window (default $1.00/window, `RTC_JARVIS_BUDGET_USD`, `off` disables), two-stage degrade (80% soft drops Sonnet+Opus; 100% hard leaves Scripted) pushed as live `JARVIS_AVAILABILITY` collapses with timer-armed lift, per-turn enforcement in `resolveBrain`, full-transparency UX (disabled picker rows with reset time, always-on gated chip state, Admin budget line, one in-chat system line on a mid-conversation downgrade), `RTC_JARVIS_FORCE_GATE` zero-token demo/e2e seam. Deferred findings from the round's reviews, none blocking (final-review triage 2026-08-09): shared-meter semantics mean N concurrent sockets can each straddle the trip with one in-flight real turn (accepted for the demo roster); no visual-tier scenario covers any gated state; the two-socket availability fan-out test exists only as a verified final-review experiment (adopt into jarvis.effects.test.ts); `formatGateResetTime`'s rendered HH:MM has never been eyeballed in a real browser (2-min check: `RTC_JARVIS_FORCE_GATE=soft pnpm dev:ws` + `dev:react:ws:local`, also eyeball the gateHint-below-divider placement); overlay CSS orders the system-line rule before the jarvis-role rule at equal specificity (latent un-mute risk); ms-boundary flake risk in the budget effects tests' paired Date.now() reads. Also deferred by the round's spec (§9): **RN surfaces for the gate** — the mobile client gets no disabled picker rows, no gated chip state, and no system downgrade line; joins the existing RN Jarvis backlog (RN narrator UX in the P5 entry above, RN panel rendering in the generative-UI entry). Item (5) multi-provider/BYOK remains pending**, unchanged from the original design intent recorded below; §18.15's closing subsection records exactly what (2) inherits from round 1's plumbing (the `brains`/`defaultBrain` collapse `RTC_JARVIS_FAKE` already demonstrates, driven by `UsageMeter`'s numbers instead of an env flag). Now that a real key can sit behind the deployed server, the missing layer is *aggregate* cost control and its UI surfacing — today's caps are per-session only (4,096 tokens/turn, 40 turns/session, 8 iterations/turn), so nothing bounds total spend in-app; the only global backstop is an Anthropic-Console workspace spend limit (see [running-real-jarvis.md](running-real-jarvis.md)). **INTERIM DECISION 2026-08-02 (now the RESIDUAL backstop behind the shipped in-app gate): billing is gated on the Anthropic side, prepaid-PAYG style.** The deployed key's org runs on prepaid credits with **auto-reload OFF**, so spend physically cannot exceed the loaded balance — when it depletes, the API rejects calls until a manual top-up (phone-operator PAYG; the accepted posture while the project has few users and the committed demo roster remains the deployed login). Two knobs, both Console-side, zero code: the credit balance (Settings → Billing; credits expire 12 months after purchase, so buy small blocks) and optionally a workspace spend limit as a second monthly-shaped gate. KNOWN ROUGH EDGE, accepted: availability only tests "is a loop configured", not "can the key bill" — on depletion the orb STAYS visible and every turn fails with the sanitized error copy ("The desk link faltered, sir…"); safe but not graceful. The shipped round-2 gate makes true depletion unlikely (the $1/5h ceiling trips first, with the honest scripted fallback + chip/picker/admin surfacing); this rough edge now matters only when the budget is set `off` or the balance dies mid-window. The original captured design intent, in rough dependency order, annotated with round-1 outcomes: **(1) Server-side usage metering — SHIPPED round 1** as `UsageMeter` (in-memory, 5h rolling windows, static price table; §18.15) — the SDK's per-message `usage` is accumulated per rolling window in the server process, exactly as designed. **(2) Claude-Code-style usage windows — SHIPPED round 2 (2026-08-09, see the head of this entry).** Built as an availability-collapse (gate narrows `brains` per level) rather than the loop swap originally sketched here — round 1's per-turn routing made the swap obsolete; degradation is honest via the `gate` field on `JARVIS_AVAILABILITY` (level + reset time + which brains the gate removed), distinct from env-removal and from unavailability. **(3) Surfacing — SHIPPED round 1** as `JarvisStatusChip` (footer, both web clients: live brain name or scripted) and `JarvisUsageCard` (Admin tab: current-window + since-boot per-brain rows, est. cost, "resets on server restart" caveat) — round 2 added the gated chip state + picker reset-time hint + Admin budget line; a per-session breakdown remains unbuilt (per-user attribution stays out of scope while the meter is server-wide). **(3a — free input for (1), noted 2026-08-02, still unused):** every API response already carries `anthropic-ratelimit-*` headers (limit/remaining/reset for requests + input + output tokens, reflecting the most restrictive active limit, workspace or org) — the metering layer can read live quota state from these without any extra API call; round 1's `UsageMeter` accumulates from `usage` alone and does not yet read these. **(4) Model + effort preference — SHIPPED round 1**, superseding the interim env-selectable step this entry originally proposed: the Preferences modal now carries a real brain picker (`jarvisBrain`: scripted / Haiku 4.5 / Sonnet 5 / Opus 5, Haiku the cost-sane default — assessed 2026-08-02, ~5× cheaper both sides than the formerly-pinned `claude-opus-5`) and an effort control (`jarvisEffort`: low/medium/high, disabled on scripted), both persisted `@rtc/domain` preferences riding the wire as validated `JARVIS_CHAT` fields against the server's own brains allowlist — never a raw model string from the client. The `RTC_JARVIS_MODEL` env-selectable interim step was never built; the picker superseded it directly. **(5) Later, explicitly gated on real accounts replacing the committed 5-user demo roster — STILL PENDING:** multi-provider support (e.g. OpenAI) behind the same `AgentLoop` seam, and bring-your-own-key (a user-supplied key bypasses the shared budget — their spend, their key; requires real per-user auth to be meaningful, and keys must never be logged or echoed, matching the existing sanitization posture).
