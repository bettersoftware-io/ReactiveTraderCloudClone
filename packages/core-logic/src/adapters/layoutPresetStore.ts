/** Lives in `@rtc/core-api` (Phase 6b layout presets, Task 4) — re-exported
 * here so every `import … from "@rtc/client-core"` can name it unchanged,
 * mirroring `dockLayoutStore.ts`'s own re-export of `DockLayoutStore`. */
export type { LayoutPresetStore } from "@rtc/core-api";
