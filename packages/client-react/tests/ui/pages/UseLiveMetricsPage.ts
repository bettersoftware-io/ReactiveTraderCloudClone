// The framework surface for `useLiveMetrics.test.tsx`: the spec drives its
// own bespoke wrapper components and rAF-mock harness, so this page owns
// only the render/act mechanics (the sanctioned "spec-side harness
// composition" placement — see task-2-brief.md's worked example).
export { act, renderHook } from "@testing-library/react";
