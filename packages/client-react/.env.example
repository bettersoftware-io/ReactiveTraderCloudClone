# WebSocket server base URL (leave unset for local simulator mode)
VITE_SERVER_URL=wss://your-server.example
# Local simulator-mode dev credentials as JSON (username -> password). Ask the team for real deployed credentials.
VITE_DEV_AUTH={"demo":"localpass"}
