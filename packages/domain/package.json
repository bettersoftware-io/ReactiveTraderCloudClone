{
  "name": "@rtc/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dev": "tsc --build --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "rxjs": "^7.8"
  },
  "devDependencies": {
    "vitest": "^3"
  }
}
